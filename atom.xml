<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>wudaijun&#39;s blog</title>
  <icon>https://www.gravatar.com/avatar/eb575f4256a9fe4728b794090470955c</icon>
  <subtitle>Done is better than perfect</subtitle>
  <link href="http://wudaijun.com/atom.xml" rel="self"/>
  
  <link href="http://wudaijun.com/"/>
  <updated>2021-06-28T12:20:13.246Z</updated>
  <id>http://wudaijun.com/</id>
  
  <author>
    <name>wudaijun</name>
    <email>wdjlost@gmail.com</email>
  </author>
  
  <generator uri="https://hexo.io/">Hexo</generator>
  
  <entry>
    <title>软件设计思维导图</title>
    <link href="http://wudaijun.com/2021/06/software-design-mind/"/>
    <id>http://wudaijun.com/2021/06/software-design-mind/</id>
    <published>2021-06-27T16:00:00.000Z</published>
    <updated>2021-06-28T12:20:13.246Z</updated>
    
    <content type="html"><![CDATA[<p>最近几个月一直忙着重构和实践基于MQ的分布式服务器框架，同时借此机会相对系统地学习了一些软件设计知识，将之前在这方面零散的知识点整合起来，按照自己的思考框架整理了一张知识脉络图。</p><p><img src="assets/image/202106/软件设计.png" alt=""></p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;最近几个月一直忙着重构和实践基于MQ的分布式服务器框架，同时借此机会相对系统地学习了一些软件设计知识，将之前在这方面零散的知识点整合起来，按照自己的思考框架整理了一张知识脉络图。&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;assets/image/202106/软件设计.png&quot; </summary>
      
    
    
    
    <category term="mindmap" scheme="http://wudaijun.com/categories/mindmap/"/>
    
    
    <category term="programing" scheme="http://wudaijun.com/tags/programing/"/>
    
  </entry>
  
  <entry>
    <title>聊聊GS引入MQ的一些实践</title>
    <link href="http://wudaijun.com/2021/02/gs-mq-practice/"/>
    <id>http://wudaijun.com/2021/02/gs-mq-practice/</id>
    <published>2021-02-17T16:00:00.000Z</published>
    <updated>2021-04-16T09:09:22.202Z</updated>
    
    <content type="html"><![CDATA[<p>在目前这套项目架构诞生初期，基于当时的游戏类型和项目需求，架构做得相对简单，设计上尽可能通过goroutine而不是节点来并发，节点间用ETCD做服务发现，用gRPC做节点通信，在单向依赖，弱藕合的情况下，基本能够满足需求。对于个别强耦合的节点交互，使用gRPC Stream来建立双工连接。</p><p>随着游戏类型和业务需求的变更，跨服功能增多，节点划分越来越细，藕合越来越重，网络拓扑也越来越复杂。gRPC Stream不再能很好地胜任。</p><p>因此我们考虑用一套新的节点交互方案，大概有两个思路:</p><ol><li>写一套完备的TCP网络库(包含服务发现，自动重连，编解码，心跳，流控等)，用于统一节点间甚至Gateway与Client间的网络交互</li><li>使用MQ解耦集群内节点交互，将网状网络化为星形网络，简化网络拓扑</li></ol><a id="more"></a><p>在对方案一进行几天的尝试后，我们最终放弃了TCP方案，主要有以下几个问题:</p><ol><li>网络拓扑完全交给了应用层去维护，开发者需要谨慎规划和约束，避免形成全联通</li><li>由于不是全联通网络，A-&gt;B的消息可能需要经由一个甚至多个中间节点路由才能到达，并且这类路由信息只能逻辑层维护</li><li>某些业务场景下，节点路由和依赖可能是动态的，如跨服匹配战场，此时需要动态建立/销毁连接以维护动态路由</li></ol><p>就前面几个问题来说，MQ是更好的解决方案，相比TCP，它有以下优势:</p><ol><li>将 0-N 跳的路由网络的模型，统一为一跳，即通过中间件即可直达任意节点，在路由和全联通之间找到一个平衡点</li><li>发布订阅模型，为应用层提供了非常大的灵活度: 单向依赖/双向依赖，扇入/扇出，负载均衡，批量发布(主题匹配)等</li></ol><p>消息中间件能够比较好地向应用层屏蔽节点路由的问题，但它并不能完全替代ETCD+gRPC，两套方案可以在不同的应用场景搭配使用。</p><p>在对几种主流消息中间件进行评估之后，我们目前选定<a href="https://github.com/nats-io/nats-server">nats</a>，它的优点是基于golang编写，轻量级，高性能，低延迟，缺点是不支持消息持久化，即最多一次投递语义，<a href="https://github.com/nats-io/nats-streaming-server">nats-streaming</a>基于nats增加了消息持久化，即最少一次投递语义，相应的也有更完备的流控机制。由于游戏服务器对消息时延敏感，并且大部分消息有状态和时效性，因此目前打算直接用nats，关键逻辑自己做消息QoS或容错机制(官方也推荐nats，由应用层而不是中间件去做QoS)。</p><p>从设计上而言，在nats之上封装应用层MQ API，不依赖nats特有功能(如Request-Reply)，只使用消息中间件的通用语义(Publish/Subscribe)，解耦组件以实现必要时透明替换，应用层对MQ API的使用主要分两种:</p><ol><li>节点通信: 应用在藕合较强的逻辑节点间，在这里Topic类似于节点公开通信地址，MQ起到的作用类似TCP。这情况情形下，对MQ封装通用消息语义: Send / Request / Async-Request，前两种语义容易理解，即异步投递和同步请求，第三种是异步请求，通过回调的方式处理响应，并统一保存请求上下文，适用于异步RPC情景。更进一步，这一层消息语义封装不应该依赖于MQ封装，即可以透明将这层语义的底层实现由MQ换成TCP或其它传输层，同时应该向应用层屏蔽掉Topic，提供类似EndPoint或Peer的抽象概念。</li><li>发布订阅: 消息中间件的常规应用，但是也需要封装，一方面是为了解耦屏蔽nats，另一方面是构建应用层对订阅分发的优化，就我们目前的实践而言，建立了一个固定大小的mq worker pool，以Topic Hash为请求分配worker，每个worker内部做二级分发(优化同一个节点对同一个Topic的多次订阅)，序列化/反序列化等，worker底层复用同一个nats client，对逻辑层提供足够易用的异步发布订阅接口。</li></ol><p>关于MQ的进一步实践我们还在摸索，目前的体会是，消息中间件和网关一样，都是游戏服务器架构的基础设施，前者对内简化服务器节点网络拓扑，后者对外屏蔽服务器内部网络拓扑，共同提升服务器的可扩展性。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;在目前这套项目架构诞生初期，基于当时的游戏类型和项目需求，架构做得相对简单，设计上尽可能通过goroutine而不是节点来并发，节点间用ETCD做服务发现，用gRPC做节点通信，在单向依赖，弱藕合的情况下，基本能够满足需求。对于个别强耦合的节点交互，使用gRPC Stream来建立双工连接。&lt;/p&gt;
&lt;p&gt;随着游戏类型和业务需求的变更，跨服功能增多，节点划分越来越细，藕合越来越重，网络拓扑也越来越复杂。gRPC Stream不再能很好地胜任。&lt;/p&gt;
&lt;p&gt;因此我们考虑用一套新的节点交互方案，大概有两个思路:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;写一套完备的TCP网络库(包含服务发现，自动重连，编解码，心跳，流控等)，用于统一节点间甚至Gateway与Client间的网络交互&lt;/li&gt;
&lt;li&gt;使用MQ解耦集群内节点交互，将网状网络化为星形网络，简化网络拓扑&lt;/li&gt;
&lt;/ol&gt;</summary>
    
    
    
    <category term="gameserver" scheme="http://wudaijun.com/categories/gameserver/"/>
    
    
    <category term="gameserver" scheme="http://wudaijun.com/tags/gameserver/"/>
    
    <category term="mq" scheme="http://wudaijun.com/tags/mq/"/>
    
  </entry>
  
  <entry>
    <title>Go2 泛型特性速览</title>
    <link href="http://wudaijun.com/2020/12/go2-generics/"/>
    <id>http://wudaijun.com/2020/12/go2-generics/</id>
    <published>2020-12-19T16:00:00.000Z</published>
    <updated>2020-12-23T02:39:29.067Z</updated>
    
    <content type="html"><![CDATA[<p>之前我在<a href="https://wudaijun.com/2019/05/programing-paradigm/">编程范式游记</a>中介绍了OOP中的子类化(subtype，也叫子类型多态subtype polymorphism)和泛型(generics，也叫参数多态parametric polymorphism)，关于两者的区别和比较可以参考那篇文章，在其中我吐槽了Go目前对泛型支持的匮乏，随着<a href="https://github.com/golang/go/wiki/Go2">Go 2</a>提上日程，Go2泛型的设计细节也越来越清晰，我们从最新的<a href="https://go.googlesource.com/proposal/+/refs/heads/master/design/go2draft-type-parameters.md">Go泛型草案</a>来了解下Go2泛型设计上有哪些考量和取舍。</p><h4 id="1-最简原型"><a href="#1-最简原型" class="headerlink" title="1. 最简原型"></a>1. 最简原型</h4><p>先从最简单的泛型定义开始:</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// Define</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">Print</span>[<span class="title">T</span>] <span class="params">(s []T)</span></span> &#123;</span><br><span class="line"><span class="keyword">for</span> _, v := <span class="keyword">range</span> s &#123;</span><br><span class="line">fmt.Println(v)</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// Call</span></span><br><span class="line">Print[<span class="keyword">int</span>]([]<span class="keyword">int</span>&#123;<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>&#125;)</span><br></pre></td></tr></table></figure><p>语法上和其它语言泛型大同小异，泛型的本质是将<strong>类型参数化</strong>，Go2中用函数名后的<code>[]</code>定义类型参数。以上声明对C++开发者来说非常亲切的(只是换了一种语法形式)，实际上这在Go2中是错误的泛型函数声明，因为它没有指明类型参数约束(constraints)。</p><a id="more"></a><h4 id="2-类型约束"><a href="#2-类型约束" class="headerlink" title="2. 类型约束"></a>2. 类型约束</h4><p>与C++不同，Go2在一开始就确定要引入泛型的类型参数约束(bounded generic types，subtype与generics的有机结合)，并且借机吐槽了C++的无约束泛型类型参数(模板类型)，因为这会带来非常难调试的编译时报错。在上例中，即使Print内部没有调用T的任何方法，也需要通过新引入的<code>any</code>关键字来表示任意类型约束(考虑下不能未显示指定约束则缺省即为<code>any</code>的原因)。因此正确的Print声明方式为:</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">Print</span>[<span class="title">T</span> <span class="title">any</span>] <span class="params">(s []T)</span></span> &#123; ...</span><br></pre></td></tr></table></figure><p>PS: 任意类型不代表不能执行任意操作，如声明变量，赋值，取地址(<code>&amp;</code>)，取类型(<code>.(type)</code>)等。</p><p>那么除去<code>any</code>，如何表示一个有效的类型约束，参考其它支持bounded generic types语言的做法，如C#/Java，自然go interface是不二之选，因为go interface本质就是做subtype，而subtype本身主要就是服务于静态语言的type checker的。因此subtype也可以辅助编译器完善对类型参数的检查。使用interface做类型参数约束的函数看起来是这个样子:</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">Stringify</span>[<span class="title">T</span> <span class="title">fmt</span>.<span class="title">Stringer</span>]<span class="params">(s []T)</span> <span class="params">(ret []<span class="keyword">string</span>)</span></span> &#123;</span><br><span class="line"><span class="keyword">for</span> _, v := <span class="keyword">range</span> s &#123;</span><br><span class="line">ret = <span class="built_in">append</span>(ret, v.String()) D</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">return</span> ret</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这里有一个有意思的问题，为什么Go2编译器不直接用Stringify函数中对T的各种函数调用，自动推敲生成一个匿名interface呢，如此对Stringify来说，外部满足<code>fmt.Stringer</code>的类型，仍然能够使用Stringify，并且这本身也是Go隐式接口的一大便利(不依赖于subclass来实现subtype，Go没有subclass)，其它像C++/C#/Java依赖于显示接口/基类继承声明得语言，是无法做到的。关于这一点，Go官方的解释是，如果接口隐式推敲，少了显式接口这层”契约”，那么Stringify的一个很小的改动都可能导致上层调用不可用，这不利于构建大型项目。调用方只需要关心它是否满足接口约束，而不应该也不需要去阅读Stringify的代码来知道它可能调用T的哪些函数。</p><p>既然选定了用interface来做类型参数约束，那么再来看<code>any</code>，它实际上就和<code>interface&#123;&#125;</code>没有区别的，任意类型都满足<code>interface&#123;&#125;</code>接口，因此实际上Print也可以声明为 <code>func Print[T interface&#123;&#125;] (s []T)</code>，但是官方觉得在写任意类型的泛型的时候，每次写<code>interface&#123;&#125;</code>太麻烦了(嗯，符合golang的极简思维)，因此还是觉得应该保留<code>any</code>关键字，作为<code>interface&#123;&#125;</code>的别名，但是在除了泛型类型约束之外，常规空接口仍然用<code>interface&#123;&#125;</code>而不能用<code>any</code>(嗯？说好的极简？)，解释是不希望新增的泛型给以前的代码带来影响…</p><h4 id="3-泛型类型"><a href="#3-泛型类型" class="headerlink" title="3. 泛型类型"></a>3. 泛型类型</h4><p>除了泛型函数外，基于泛型也可以构建新的类型，如:</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 定义一个可保存任意类型的切片</span></span><br><span class="line"><span class="keyword">type</span> Vector[T any] []T</span><br><span class="line"><span class="comment">// 实现泛型类Vector方法</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(v *Vector[T])</span> <span class="title">Push</span><span class="params">(x T)</span></span> &#123; *v = <span class="built_in">append</span>(*v, x) &#125;</span><br><span class="line"><span class="comment">// 实例化泛型类型 Vecter[int] t</span></span><br><span class="line"><span class="keyword">var</span> v Vector[<span class="keyword">int</span>]</span><br><span class="line"></span><br><span class="line"><span class="comment">// 与前面的Vector[int]等价，事实上编译器也会生成类似的类</span></span><br><span class="line"><span class="keyword">type</span> VecterInt []<span class="keyword">int</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(v *VectorInt)</span> <span class="title">Push</span><span class="params">(x <span class="keyword">int</span>)</span></span> &#123; *v = <span class="built_in">append</span>(*v, x) &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 定义一个可保存两个任意类型值的Pair</span></span><br><span class="line"><span class="keyword">type</span> Pair[T1, T2 any] <span class="keyword">struct</span> &#123;</span><br><span class="line">val1  T1</span><br><span class="line">    val2  T2</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 定义Pair List</span></span><br><span class="line"><span class="comment">// 注意next字段引用了自身，目前这种相互直接或间接引用，要求参数类型(以及顺序)一致，后面可能会放宽此要求</span></span><br><span class="line"><span class="keyword">type</span> List[T1, T2 any] <span class="keyword">struct</span> &#123;</span><br><span class="line">next *List[T1, T2] <span class="comment">// 如果改成 List[T2, T1] 则不行</span></span><br><span class="line">val  Pair[T1, T2] </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="4-类型列表约束"><a href="#4-类型列表约束" class="headerlink" title="4. 类型列表约束"></a>4. 类型列表约束</h4><p>前面提到Go2通过interface完成对参数类型的约束，理论上来说已经是完备的了(毕竟interface用作subtype已经证明了这点)，但是还不够方便，比如:</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// This function is INVALID.</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">Smallest</span>[<span class="title">T</span> <span class="title">any</span>]<span class="params">(s []T)</span> <span class="title">T</span></span> &#123;</span><br><span class="line">r := s[<span class="number">0</span>] <span class="comment">// panic if slice is empty</span></span><br><span class="line"><span class="keyword">for</span> _, v := <span class="keyword">range</span> s[<span class="number">1</span>:] &#123;</span><br><span class="line"><span class="keyword">if</span> v &lt; r &#123; <span class="comment">// INVALID</span></span><br><span class="line">r = v</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">return</span> r</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在<code>Smallest</code>函数中，我们希望求出一个切片中的最小元素，但是这个函数声明本身是无效的，因为不是所有的类型都支持<code>&lt;</code>运算符，事实上，在Go中，仅有限的内置类型支持<code>&lt;</code>，其它自定义类型均不支持通过方法定义自己的<code>&lt;</code>运算(此处开始怀念C++的运算符，但是其带来的”一切皆有可能”的代码理解负担也确实头疼…)，诚然这里可以通过定义类似的Comparable interface来进行类型约束和比较，将<code>v &lt; r</code>替换为<code>v.Less(r)</code>，但你也需要为原生支持比较的类型(int/float/string)定义一个新的类型并实现Comparable接口，反而让Smallest使用起来更复杂。因此，这里Go2有必要为基础运算符定义一套泛型类型约束，使得调用方可以直接通过<code>Smallest[int]([]int&#123;3,1,4&#125;)</code>即可使用。</p><p>这里有两种实现方式，一种方案是预定义基础运算的约束，并且让满足条件的基础类型自动适配而无需手动实现，如<code>&lt;</code>，<code>&gt;</code>，<code>==</code>，<code>&lt;&lt;</code>，<code>&amp;&amp;</code>，<code>range</code>，<code>size</code>等。另一种方式是基于Go几乎所有的逻辑运算符(唯二的例外在后面会讨论)都仅支持内置基础类型，并且内置基础类型是有限的这两点事实，从另一个角度出发: 让类型约束可以直接指定其需要包含的基础类型。Go优先选择了第二种方案，提供所谓<strong>类型列表约束(type lists in contraints)</strong>机制:</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// Ordered 包含所有可直接比较的类型，以及基于其定义的类型，如 type String string</span></span><br><span class="line"><span class="keyword">type</span> Ordered <span class="keyword">interface</span> &#123;</span><br><span class="line"><span class="keyword">type</span> <span class="keyword">int</span>, <span class="keyword">int8</span>, <span class="keyword">int16</span>, <span class="keyword">int32</span>, <span class="keyword">int64</span>,</span><br><span class="line"><span class="keyword">uint</span>, <span class="keyword">uint8</span>, <span class="keyword">uint16</span>, <span class="keyword">uint32</span>, <span class="keyword">uint64</span>, <span class="keyword">uintptr</span>,</span><br><span class="line"><span class="keyword">float32</span>, <span class="keyword">float64</span>,</span><br><span class="line"><span class="keyword">string</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>考虑到<code>Ordered</code>这类约束可能会在很多地方用到，因此可能需要将其归到官方库中提供，该package暂定为<code>contraints</code>，如此<code>Smallest</code>即可定义为<code>func Smallest[T constraints.Ordered](s []T) T</code>。</p><p>对于类型列表中的预定义类型，基于其的type define也满足约束(如<code>type Int int</code>)，而基于自定义类型，只有精确的类型列表中的类型才满足约束(官方文档目前是这么说的)。有了类型列表约束之后，函数可以直接使用类型列表中所有类型都支持的运算，但<strong>不能直接使用类型列表都支持的方法，即使这些类型都提供了该方法</strong>:</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">type</span> MyInt <span class="keyword">int</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(i MyInt)</span> <span class="title">String</span><span class="params">()</span> <span class="title">string</span></span> &#123;</span><br><span class="line"><span class="keyword">return</span> strconv.Itoa(<span class="keyword">int</span>(i))</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> MyFloat <span class="keyword">float64</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(f MyFloat)</span> <span class="title">String</span><span class="params">()</span> <span class="title">string</span></span> &#123;</span><br><span class="line"><span class="keyword">return</span> strconv.FormatFloat(<span class="keyword">float64</span>(f), <span class="string">&#x27;g&#x27;</span>, <span class="number">-1</span>, <span class="number">64</span>)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> MyIntOrFloat <span class="keyword">interface</span> &#123;</span><br><span class="line"><span class="keyword">type</span> MyInt, MyFloat</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">ToString</span>[<span class="title">T</span> <span class="title">MyIntOrFloat</span>]<span class="params">(v T)</span> <span class="title">string</span></span> &#123;</span><br><span class="line">    <span class="keyword">return</span> v.String() <span class="comment">// Error: 泛型函数只能使用类型约束显式声明的方法</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这样是为了避免当类型和接口嵌套较深时，调用方很难搞清楚函数到底依赖了约束的哪些方法(因为没了类型约束这层契约)，因此在<code>MyIntOrFloat</code>中显式添加<code>String() string</code>接口是一个更明智的选择，这也说明，Go的类型约束支持同时指定类型列表和接口方法。</p><p>最后，如果多个类型列表约束嵌套定义，那么最终生效的是这些子类型列表的交集，如:</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">type</span> Addable <span class="keyword">interface</span> &#123;</span><br><span class="line"><span class="keyword">type</span> <span class="keyword">int</span>, <span class="keyword">int8</span>, <span class="keyword">int16</span>, <span class="keyword">int32</span>, <span class="keyword">int64</span>,</span><br><span class="line"><span class="keyword">uint</span>, <span class="keyword">uint8</span>, <span class="keyword">uint16</span>, <span class="keyword">uint32</span>, <span class="keyword">uint64</span>, <span class="keyword">uintptr</span>,</span><br><span class="line"><span class="keyword">float32</span>, <span class="keyword">float64</span>, <span class="keyword">complex64</span>, <span class="keyword">complex128</span>,</span><br><span class="line"><span class="keyword">string</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> Byteseq <span class="keyword">interface</span> &#123;</span><br><span class="line"><span class="keyword">type</span> <span class="keyword">string</span>, []<span class="keyword">byte</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 同时满足Addable和Byteseq约束的，事实上只有string</span></span><br><span class="line"><span class="keyword">type</span> AddableByteseq <span class="keyword">interface</span> &#123;</span><br><span class="line">Addable</span><br><span class="line">Byteseq</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="5-特殊运算符"><a href="#5-特殊运算符" class="headerlink" title="5. 特殊运算符(==, !=)"></a>5. 特殊运算符(==, !=)</h4><p><strong>几乎所有的运算符都仅支持内置类型</strong>，这其中有两个例外的运算符，等于(==)和不等于(!=)，比如我们知道，在Go中两个值语义struct，array是可以直接比较的，Go需要特殊处理这两个运算符，因此还是提出了<code>comparable</code>这个特殊的预定义类型约束:</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">Index</span>[<span class="title">T</span> <span class="title">comparable</span>]<span class="params">(s []T, x T)</span> <span class="title">int</span></span> &#123;</span><br><span class="line"><span class="keyword">for</span> i, v := <span class="keyword">range</span> s &#123;</span><br><span class="line"><span class="keyword">if</span> v == x &#123;</span><br><span class="line"><span class="keyword">return</span> i</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">return</span> <span class="number">-1</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>由于Go2同时支持了预定义类型约束和类型列表约束，因此开发者可能定义出一个永远无法满足的类型约束:</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">type</span> ImpossibleConstraint <span class="keyword">interface</span> &#123;</span><br><span class="line">comparable</span><br><span class="line"><span class="keyword">type</span> []<span class="keyword">int</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>由于<code>[]int</code>无法被比较，因此没有任何类型能满足<code>ImpossibleConstraint</code>约束，定义这个约束本身不会报错，但是在尝试调用使用该约束的任意泛型函数和泛型类时，会得到编译错误。</p><p>有了预定义类型约束和类型列表约束之后，类型约束的补丁算是打完了，但是为了避免理解负担，和<code>any</code>一样，Go2目前考虑只将这两者用在泛型类型约束中，而不用在常规接口定义中。</p><p>这里可以简单梳理下作为类型约束的interface和常规interface的区别:</p><ol><li>Go2预定义了两个类型约束: <code>any</code>(也可直接使用interface{}) 和 <code>comparable</code></li><li>类型约束interface支持类型列表定义</li><li>常规interface可为nil(表示没有值，但可能有类型信息)，类型约束本身描述类型的元信息(类型的类型)，不能为nil</li><li>类型约束本身也可以泛型化，这个在下一节会看到</li></ol><h4 id="6-类型约束中的复合类型"><a href="#6-类型约束中的复合类型" class="headerlink" title="6. 类型约束中的复合类型"></a>6. 类型约束中的复合类型</h4><p>如果类型列表约束中存在复合类型，再结合索引<code>[]</code>，求大小<code>len</code>，遍历<code>range</code>，字段访问<code>.</code>等针对复合类型的操作符时，有意思的问题就来了。由于不同复合类型的操作符的参数和返回值类型可能是不同的，比如如果类型列表中包含<code>[]int</code>和<code>[]int64</code>，它们的索引操作<code>[]</code>会分别返回<code>int</code>和<code>int64</code>，那么此时编译器有两种做法:</p><ol><li>支持<code>[]</code>操作，但是需要有一个类型联合(<code>type union</code>)的概念，来保存<code>[]</code>的返回值。比如在这里，编译器会生成<code>int</code>和<code>int64</code>的联合来保存<code>[]</code>的返回值</li><li>仅当<code>[]</code>对类型列表中所有的类型的参数和返回值都一致时，才允许使用<code>[]</code>，否则不允许使用<code>[]</code>并报编译错误</li></ol><p>Go2目前选择第二种，因为直观上它更容易理解，具体以例子来说:</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">type</span> structField <span class="keyword">interface</span> &#123;</span><br><span class="line"><span class="keyword">type</span> <span class="keyword">struct</span> &#123; a <span class="keyword">int</span>; x <span class="keyword">int</span> &#125;,</span><br><span class="line"><span class="keyword">struct</span> &#123; b <span class="keyword">int</span>; x <span class="keyword">float64</span> &#125;,</span><br><span class="line"><span class="keyword">struct</span> &#123; c <span class="keyword">int</span>; x <span class="keyword">uint64</span> &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">IncrementX</span>[<span class="title">T</span> <span class="title">structField</span>]<span class="params">(p *T)</span></span> &#123;</span><br><span class="line">v := p.x <span class="comment">// Error: 对structField type list来说，操作p.x的返回值类型不一样</span></span><br><span class="line">v++</span><br><span class="line">p.x = v</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> sliceOrMap <span class="keyword">interface</span> &#123;</span><br><span class="line"><span class="keyword">type</span> []<span class="keyword">int</span>, <span class="keyword">map</span>[<span class="keyword">int</span>]<span class="keyword">int</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">Entry</span>[<span class="title">T</span> <span class="title">sliceOrMap</span>]<span class="params">(c T, i <span class="keyword">int</span>)</span> <span class="title">int</span></span> &#123;</span><br><span class="line"><span class="keyword">return</span> c[i] <span class="comment">// OK. []int和map[int]int的索引操作的参数和返回值均为int</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> sliceOrFloatMap <span class="keyword">interface</span> &#123;</span><br><span class="line"><span class="keyword">type</span> []<span class="keyword">int</span>, <span class="keyword">map</span>[<span class="keyword">float64</span>]<span class="keyword">int</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">FloatEntry</span>[<span class="title">T</span> <span class="title">sliceOrFloatMap</span>]<span class="params">(c T)</span> <span class="title">int</span></span> &#123;</span><br><span class="line"><span class="keyword">return</span> c[<span class="number">1.0</span>] <span class="comment">// Error: 对[]int和map[float64]int来说，[]操作的参数类型不一致</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>目前来说，这应该能够应付绝大部分复合类型type list的应用场景。</p><h4 id="7-类型参数的相互引用"><a href="#7-类型参数的相互引用" class="headerlink" title="7. 类型参数的相互引用"></a>7. 类型参数的相互引用</h4><p>Go2支持同一个类型参数列表间的相互引用，如下面的泛型图类型:</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">package</span> graph</span><br><span class="line"></span><br><span class="line"><span class="comment">// 创建了一个泛型接口约束来表示图的节点，该约束限制类型必须提供一个返回任意类型切片的Edges()函数</span></span><br><span class="line"><span class="keyword">type</span> NodeConstraint[Edge any] <span class="keyword">interface</span> &#123;</span><br><span class="line">Edges() []Edge</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 创建了一个泛型接口约束来表示图的边，该约束限制类型必须提供一个返回两个相同的任意类型值的Nodes()函数</span></span><br><span class="line"><span class="keyword">type</span> EdgeConstraint[Node any] <span class="keyword">interface</span> &#123;</span><br><span class="line">Nodes() (from, to Node)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 重点在这里，对泛型图类的类型约束列表中，存在相互引用</span></span><br><span class="line"><span class="comment">// 即限制了NodeConstraint 的约束类型(Edges返回的切片边类型)必须满足EdgeConstraint</span></span><br><span class="line"><span class="comment">// EdgeConstraint 的约束类型(Nodes返回的节点类型)必须满足NodeConstraint</span></span><br><span class="line"><span class="comment">// 即将两个接口约束相互关联了起来</span></span><br><span class="line"><span class="keyword">type</span> Graph[Node NodeConstraint[Edge], Edge EdgeConstraint[Node]] <span class="keyword">struct</span> &#123; ... &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 创建图，由于约束本身也是泛型，所以看起来复杂一些</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">New</span>[<span class="title">Node</span> <span class="title">NodeConstraint</span>[<span class="title">Edge</span>], <span class="title">Edge</span> <span class="title">EdgeConstraint</span>[<span class="title">Node</span>]] <span class="params">(nodes []Node)</span> *<span class="title">Graph</span>[<span class="title">Node</span>, <span class="title">Edge</span>]</span> &#123;</span><br><span class="line">...</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 求图最短路径的方法，由于New的时候，编译器已经检查过了。因此方法中不再需要复填Node/EdgeConstraint，直接使用类型参数即可。</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(g *Graph[Node, Edge])</span> <span class="title">ShortestPath</span><span class="params">(from, to Node)</span> []<span class="title">Edge</span></span> &#123; ... &#125;</span><br></pre></td></tr></table></figure><p>我们可以用以下Vertex和FromTo类来实例化Graph泛型类:</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">type</span> Vertex <span class="keyword">struct</span> &#123; ... &#125;</span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(v *Vertex)</span> <span class="title">Edges</span><span class="params">()</span> []*<span class="title">FromTo</span></span> &#123; ... &#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> FromTo <span class="keyword">struct</span> &#123; ... &#125;</span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(ft *FromTo)</span> <span class="title">Nodes</span><span class="params">()</span> <span class="params">(*Vertex, *Vertex)</span></span> &#123; ... &#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> g = graph.New[*Vertex, *FromTo]([]*Vertex&#123; ... &#125;)</span><br></pre></td></tr></table></figure><p>除了相互引用以外，类型约束还可以引用自身，比如定义类型自己的<code>Equal</code>函数:</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 查找并返回e的下标，官方给出的写法</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">Index</span>[<span class="title">T</span> <span class="title">interface</span></span> &#123; Equal(T) <span class="keyword">bool</span> &#125;](s []T, e T) <span class="keyword">int</span> &#123;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 以下是个人理解的等价写法</span></span><br><span class="line"><span class="keyword">type</span> Equaler[T any] <span class="keyword">interface</span> &#123; </span><br><span class="line">Equal(T) <span class="keyword">bool</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 查找并返回e的下标</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">Index</span>[<span class="title">T</span> <span class="title">Equaler</span>[<span class="title">T</span>]]<span class="params">(s []T, e T)</span> <span class="title">int</span></span> &#123;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>Go2编译器会推导类型参数相互引用的合理性，这进一步提升了泛型的灵活性。</p><h4 id="8-类型参数推导"><a href="#8-类型参数推导" class="headerlink" title="8. 类型参数推导"></a>8. 类型参数推导</h4><p>回到前面的Print，该泛型函数的调用方式形如: <code>Print[int]([]int&#123;3,1,2&#125;)</code>，但前面的类型参数相互引用中提到，编译器需要支持一定的类型推导(<code>type inference</code>)能力，因此实际上大部分时候，我们都不需要显式指定类型参数，直接通过<code>Print([]int&#123;3,1,2&#125;)</code>调用即可，编译器会通过实参<code>[]int&#123;3,1,2&#125;</code>匹配<code>[]T</code>，推导出T为int。</p><p>Go类型推导基于底层的类型一致(type unification)机制，本质上来说是一套类型匹配机制，对了类型A和类型B:</p><ol><li>如果A，B均不包含类型参数，那么A和B一致当且仅当A和B相同</li><li>仅一方包含类型参数: 如A为<code>[]map[int]bool</code>，B为<code>[]T</code>，那么称A和B是类型一致的，并且此时T为<code>map[int]bool</code></li><li>双方都包含类型参数: 如A为<code>[]map[T1]bool</code>，B为<code>[]map[int]T2</code>，那么A B也是类型一致的，并且T1为<code>int</code>，T2为<code>bool</code></li></ol><p>Go对函数的类型参数推导是在函数调用而不是实例化的时候发生的，并且函数类型参数推导本身不包含类型约束检查和形实参赋值检查(像普通函数调用的检查一样)，这些是在类型推导完成之后才开始的。类型推导分为两个阶段:</p><ol><li>第一阶段，忽略所有的无类型(untype)实参(如字面常量<code>5</code>)，先依次推导参数列表中其它的包含类型参数的形参，如果一个类型参数在形参中出现了多次，那么它每次匹配的类型必须是相同的。注: 对于函数的类型参数推导，编译器<strong>只能对出现在函数参数列表中的类型参数进行推导</strong>，而对于那些只用于函数体或函数返回值的类型参数，编译器是无法推导的。</li><li>第二阶段，再开始处理无类型实参的匹配，因为其对应形参中的类型参数，可能在第一遍的时候被推敲出来了，如果对应形参还没被推导出来，给无类型实参赋予默认类型(如<code>5</code>对应<code>int</code>)，再开始推导对应形参。</li></ol><p>分为两个阶段是为了延迟无类型实参的类型推导，使泛型对于无类型实参更友好易用。举个例子:</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">NewPair</span>[<span class="title">F</span> <span class="title">any</span>]<span class="params">(f1, f2 F)</span> *<span class="title">Pair</span>[<span class="title">F</span>]</span> &#123; ... &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// OK, 第一阶段完成后(F类型仍然未知)，开始给无类型实参赋予默认类型int，两次匹配均得到F为int，前后一致，推导完成，F为int</span></span><br><span class="line">NewPair(<span class="number">1</span>, <span class="number">2</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment">// OK, 第一阶段完成后，根据int64(2)推导得到F为int64，第二阶段时，所有无类型实参的类型都已经确定，推导完成，F为int64</span></span><br><span class="line"><span class="comment">// 如果不是两阶段推导，那么这种情况就无法被支持</span></span><br><span class="line">NewPair(<span class="number">1</span>, <span class="keyword">int64</span>(<span class="number">2</span>))</span><br><span class="line"></span><br><span class="line"><span class="comment">// Failed，第一阶段完成后(F未知)，开始分别给无类型参数赋予默认值int和float64，F前后匹配两次的类型不一致，推导失败，编译器报错</span></span><br><span class="line">NewPair(<span class="number">1</span>, <span class="number">2.5</span>)</span><br></pre></td></tr></table></figure><h4 id="9-泛型接口约束推导"><a href="#9-泛型接口约束推导" class="headerlink" title="9. 泛型接口约束推导"></a>9. 泛型接口约束推导</h4><p>前面定义Graph泛型类时，用到了泛型接口约束:</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">type</span> NodeConstraint[Edge any] <span class="keyword">interface</span> &#123;</span><br><span class="line">Edges() []Edge</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>泛型接口约束允许定义泛型类型参数之间的关系，但是由于约束本身也是泛型的，因此对接口约束中的类型参数也需要推导，这个步骤发生在函数类型参数推导之后，具体的推导规则仍然是根据已知的具象的实参推导未知的类型参数。具体推导规则描述起来比较抽象，仍然以官方例子来说:</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 将数字切片中所有元素翻倍并返回</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">Double</span>[<span class="title">E</span> <span class="title">constraints</span>.<span class="title">Number</span>]<span class="params">(s []E)</span> []<span class="title">E</span></span> &#123;</span><br><span class="line">r := <span class="built_in">make</span>([]E, <span class="built_in">len</span>(s))</span><br><span class="line"><span class="keyword">for</span> i, v := <span class="keyword">range</span> s &#123;</span><br><span class="line">r[i] = v + v</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">return</span> r</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> MySlice []<span class="keyword">int</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 返回值是[]int，而不是MySlice</span></span><br><span class="line"><span class="keyword">var</span> V1 = Double(MySlice&#123;<span class="number">1</span>&#125;)</span><br></pre></td></tr></table></figure><p>为了<code>Double</code>函数更易用，我们需要为<code>Double</code>的参数即切片本身定义一个类型参数(这样才能定义相同类型的返回值)，但是我们同时需要约束这个切片的元素类型，因此这里需要定义泛型接口约束:</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">type</span> SC[E any] <span class="keyword">interface</span> &#123;</span><br><span class="line"><span class="keyword">type</span> []E</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 定义泛型约束 SC[E]，并且约束E的类型为数字</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">DoubleDefined</span>[<span class="title">S</span> <span class="title">SC</span>[<span class="title">E</span>], <span class="title">E</span> <span class="title">constraints</span>.<span class="title">Number</span>]<span class="params">(s S)</span> <span class="title">S</span></span> &#123;</span><br><span class="line"><span class="comment">// Note that here we pass S to make, where above we passed []E.</span></span><br><span class="line">r := <span class="built_in">make</span>(S, <span class="built_in">len</span>(s))</span><br><span class="line"><span class="keyword">for</span> i, v := <span class="keyword">range</span> s &#123;</span><br><span class="line">r[i] = v + v</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">return</span> r</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> MySlice []<span class="keyword">int</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// V3的类型是MySlice</span></span><br><span class="line"><span class="keyword">var</span> V3 = DoubleDefined(MySlice&#123;<span class="number">1</span>&#125;)</span><br></pre></td></tr></table></figure><p>上例再一次说明了泛型接口约束存在的必要性，按照我的理解，泛型接口约束允许<strong>对一个类型分层次的约束和使用</strong>。回到对约束的类型推导上来，在<code>DoubleDefined</code>中，函数的类型参数推导没有办法推导<code>E</code>的类型(因为它没有出现在函数参数列表中)，它只能推导得到<code>S -&gt; MySlice</code>，这个时候就需要约束类型参数推导来完成剩下的工作了，<code>MySlice</code>是已知的具象的类型，从它对应的<code>SC[E]</code>约束开始推导，<code>SC[E]</code>类型列表中只有一个类型<code>[]E</code>，因此<code>MySlice</code>只能<code>[]E</code>类型一致，推出<code>E -&gt; int</code>。</p><p>泛型接口约束的类型参数推导的时机在函数类型参数推导之后，但是仍然在约束检查之前。</p><h4 id="10-总结"><a href="#10-总结" class="headerlink" title="10. 总结"></a>10. 总结</h4><p>以上是Go2官方设计草案中关于泛型的主要设计细节，简单小结一下Go2泛型:</p><ol><li>和其它语言的泛型类似，本质是将类型参数化，支持函数泛型和类型泛型</li><li>通过类型约束来描述和限制类型参数</li><li>类型约束对外描述类型参数所需实现的方法或允许的类型列表</li><li>类型约束对内定义了类型参数所允许调用方法和支持的操作符</li><li>类型约束本身也可以泛型化，用以类型参数的相互引用或自引用</li><li>类型参数推导使得大部分时候调用方无需显式指定类型参数</li><li>本质上是模板泛型，即中间不存在二次封箱(boxed)操作</li></ol><p>以下是个人的一些体会，在大部分的设计取舍上，Go官方会优先考虑构建大型项目所必需的可读性，类型约束不只是type checker，更是一层设计上不可缺少的契约层，尽可能地将大部分信息都明确地内聚到这层契约上，最初设计中，很多可选项也是尽量从严要求，等后面应用相对成熟之后，再考虑放开限制。</p><p>值得一提的是Go2泛型中的类型列表约束，个人觉得它是把双刃剑，一方面它很大程度解决了Go没有运算符重载的问题，但另一方面，它也带来了如下问题:</p><ol><li>打破了接口类型约束的封装，甚至允许将泛型约束”降级”为具体类型(只有一个类型的类型列表约束)</li><li>接口约束和类型列表约束两者组合可能定义出永远不能被实例化的约束</li><li>类型列表约束没有根治没有运算符重载的问题，还加了个预定义约束<code>comparable</code>补丁</li></ol><p>Go官方认为内置类型始终是有限的，从内置类型封装的做法比较取巧，也确实要比考虑如何对操作符进行封装要简单，毕竟同一个操作符对不同类型的原型是不一样的。但反过来说，自定义类型却是无限的，类型列表允许自定义类型(也许由于type define不得不允许)，可能会给开发者一定的心智负担，或者导致不良的泛型设计代码。因此目前个人觉得，类型列表约束需要慎用，特别是对于自定义类型。</p><p>总的来说，我对Go2泛型的整体实用性还是非常有信心，可以预见的是，Go2在支持泛型之后，代码复用性和扩展性会大大增强，之前的不少代码都可以重构精简不少，标准库也会因为泛型更加丰富实用，拭目以待。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;之前我在&lt;a href=&quot;https://wudaijun.com/2019/05/programing-paradigm/&quot;&gt;编程范式游记&lt;/a&gt;中介绍了OOP中的子类化(subtype，也叫子类型多态subtype polymorphism)和泛型(generics，也叫参数多态parametric polymorphism)，关于两者的区别和比较可以参考那篇文章，在其中我吐槽了Go目前对泛型支持的匮乏，随着&lt;a href=&quot;https://github.com/golang/go/wiki/Go2&quot;&gt;Go 2&lt;/a&gt;提上日程，Go2泛型的设计细节也越来越清晰，我们从最新的&lt;a href=&quot;https://go.googlesource.com/proposal/+/refs/heads/master/design/go2draft-type-parameters.md&quot;&gt;Go泛型草案&lt;/a&gt;来了解下Go2泛型设计上有哪些考量和取舍。&lt;/p&gt;
&lt;h4 id=&quot;1-最简原型&quot;&gt;&lt;a href=&quot;#1-最简原型&quot; class=&quot;headerlink&quot; title=&quot;1. 最简原型&quot;&gt;&lt;/a&gt;1. 最简原型&lt;/h4&gt;&lt;p&gt;先从最简单的泛型定义开始:&lt;/p&gt;
&lt;figure class=&quot;highlight go&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;1&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;2&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;3&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;4&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;5&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;6&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;7&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;8&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;comment&quot;&gt;// Define&lt;/span&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;function&quot;&gt;&lt;span class=&quot;keyword&quot;&gt;func&lt;/span&gt; &lt;span class=&quot;title&quot;&gt;Print&lt;/span&gt;[&lt;span class=&quot;title&quot;&gt;T&lt;/span&gt;] &lt;span class=&quot;params&quot;&gt;(s []T)&lt;/span&gt;&lt;/span&gt; &amp;#123;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;	&lt;span class=&quot;keyword&quot;&gt;for&lt;/span&gt; _, v := &lt;span class=&quot;keyword&quot;&gt;range&lt;/span&gt; s &amp;#123;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;		fmt.Println(v)&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;	&amp;#125;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&amp;#125;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;comment&quot;&gt;// Call&lt;/span&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;Print[&lt;span class=&quot;keyword&quot;&gt;int&lt;/span&gt;]([]&lt;span class=&quot;keyword&quot;&gt;int&lt;/span&gt;&amp;#123;&lt;span class=&quot;number&quot;&gt;1&lt;/span&gt;, &lt;span class=&quot;number&quot;&gt;2&lt;/span&gt;, &lt;span class=&quot;number&quot;&gt;3&lt;/span&gt;&amp;#125;)&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;
&lt;p&gt;语法上和其它语言泛型大同小异，泛型的本质是将&lt;strong&gt;类型参数化&lt;/strong&gt;，Go2中用函数名后的&lt;code&gt;[]&lt;/code&gt;定义类型参数。以上声明对C++开发者来说非常亲切的(只是换了一种语法形式)，实际上这在Go2中是错误的泛型函数声明，因为它没有指明类型参数约束(constraints)。&lt;/p&gt;</summary>
    
    
    
    <category term="golang" scheme="http://wudaijun.com/categories/golang/"/>
    
    
    <category term="golang" scheme="http://wudaijun.com/tags/golang/"/>
    
  </entry>
  
  <entry>
    <title>GS 测试规范实践</title>
    <link href="http://wudaijun.com/2020/08/gs-testing-practice/"/>
    <id>http://wudaijun.com/2020/08/gs-testing-practice/</id>
    <published>2020-08-14T16:00:00.000Z</published>
    <updated>2020-08-21T02:19:49.722Z</updated>
    
    <content type="html"><![CDATA[<p>在之前的博客中几次简单提及过给GS做测试，关于测试的必要性不用再多说，但在实际实践过程中，却往往会因为如下原因导致想要推进测试规范困难重重:</p><p>-. Q1: 写测试代码困难: 代码耦合重，各种相互依赖，全局依赖，导致写测试代码”牵一发而动全身”，举步维艰<br>-. Q2: 测试时效性低: 需求变更快，数值变更频繁，可能导致今天写好的测试代码，明天就”过时”了<br>-. Q3: 开发进度紧: 不想浪费过多时间来写测试代码，直接开发感觉开发效率更高</p><p>要想推进测试规范，上面的三个问题是必须解决的。这里简单聊聊我们在Golang游戏后端中的测试实践和解决方案。我们在GS中尝试的测试方案主要分为四种: 单元测试，集成测试，压力测试，以及模拟测试。</p><a id="more"></a><h3 id="单元测试"><a href="#单元测试" class="headerlink" title="单元测试"></a>单元测试</h3><p>单元测的优点是与业务逻辑和外部环境关联度最小，同时go test也很容易集成到CI/CD流程中。单元测试的缺点就是上面提到的Q1(耦合依赖问题)，对此，我们的解决方案是:</p><ol><li>持续重构，解耦降低依赖。有点废话，但是写易于测试的代码确实是一种修行</li><li>通过<a href="https://github.com/smartystreets/goconvey">goconvey</a>测试框架简化单元测试的编写</li><li>通过<a href="https://github.com/golang/mock">gomock</a> Mock掉接口依赖</li><li>实在Mock不掉的，通过<a href="https://github.com/agiledragon/gomonkey">gomonkey</a> Hack掉依赖，不过要记得禁用内联</li><li>对于一些复杂的单元测试，如涉及到发消息，创建玩家，启动定时器等，可以创建通用的Mock组件和环境，便于使用</li></ol><p>goconvey+gomonkey+gomock 三件套在实践中足够灵活强大，具体使用参考文档即可，比较简单，就不展示了。</p><h3 id="集成测试"><a href="#集成测试" class="headerlink" title="集成测试"></a>集成测试</h3><p>集成测试我们又称之为用例测试，它是一种黑盒测试，以C/S交互协议为边界，站在客户端视角来测试服务器运行结果，黑盒测试本质上是消息流测试。它的优点是覆盖面广，网络层，集群管理，消息路由等细节都被会覆盖到。黑盒测试的难点在于易变性，协议变更，配置更新等都可能造成测试用例不可用，即上面提到的Q2(用例时效性问题)。对此，我们的实践是:</p><ol><li>将消息流测试离线化，即封装基本原语(Send,Wait,Expect,Select等)，化编译型为解释型，让测试用例可以通过类似配置文件的方式来描述，简化与服务器的交互细节，甚至理论做到交付给非技术人员使用。技术上除了对模拟客户端的封装外，主要是对json的处理: <a href="https://github.com/thedevsaddam/gojsonq">gojsonq</a>, <a href="https://github.com/nsf/jsondiff">jsondiff</a>, <a href="https://github.com/mkideal/pkg/tree/master/encoding/jsonx">jsonx</a></li><li>写可重入的测试用例，可重入即用例不应该依赖于当前服务器和用例机器人的初始状态，做到可重复执行</li><li>保存一份专用于用例测试策划配置快照，避免频繁的数值调整导致测试用例不可用。服务器和测试客户端都使用这份配置。即GS需要支持不同的配置源(如DB/File)</li></ol><p>以下是一个省掉很多细节的测试用例(yml格式描述):</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"># 封装一个Function，从预定义变量varRole[n]中提取字段放到自定义变量中</span><br><span class="line">InitAttackCmds:</span><br><span class="line">  - find LoginAck.city.coord.X from varRole1 to varCity1X</span><br><span class="line">  - find LoginAck.city.coord.Z from varRole1 to varCity1Z</span><br><span class="line">  - find LoginAck.city.cityID from varRole1 to varCity1ID</span><br><span class="line"></span><br><span class="line"># 单个测试用例</span><br><span class="line">AttackPersonCityWinTest:</span><br><span class="line">  # 创建两个Robot，以Rbt1 Rbt2 标识</span><br><span class="line">  - newrobot 2</span><br><span class="line">  # 此时机器人已经登录完成，初始化自定义变量</span><br><span class="line">  - call InitAttackCmds</span><br><span class="line">  # 获取Rbt1初始化城防值</span><br><span class="line">  - Rbt1 send CityDefenseReq &#123;&#125;</span><br><span class="line">  # wait 后面的消息支持json局部字段比较(包含匹配)</span><br><span class="line">  - Rbt1 wait CityDefenseAck &#123;isCombustion:false&#125;</span><br><span class="line">  - Rbt1 find cityDefense from varLastAck to varCityDefensePreVal</span><br><span class="line">  # Rbt2 向 Rbt1 城池行军</span><br><span class="line">  - Rbt2 send NewTroopReq &#123;&quot;Action&quot;:1,&quot;Soldiers&quot;:&#123;&quot;11211001&quot;:500,&quot;11211301&quot;:500&#125;,&quot;EndCoord&quot;:&#123;&quot;X&quot;:%v,&quot;Z&quot;:%v&#125;,&quot;Mission&quot;:&#123;&quot;IsCampAfterHunt&quot;:false,&quot;IsRally&quot;:false&#125;,&quot;TargetID&quot;:%v&#125; varCity1X varCity1Z varCity1ID</span><br><span class="line">  - Rbt2 wait NewTroopAck &#123;errCode:0,action:1&#125;</span><br><span class="line">  # 防守失败后被烧城</span><br><span class="line">  - Rbt1 wait CombustionStateNtf &#123;isCombustion:true&#125;</span><br><span class="line">  - Rbt1 find cityDefense from varLastAck to varCityDefensePostVal</span><br><span class="line">  # 掉城防值</span><br><span class="line">  - should varCityDefensePostVal &lt; varCityDefensePreVal</span><br></pre></td></tr></table></figure><h3 id="压力测试"><a href="#压力测试" class="headerlink" title="压力测试"></a>压力测试</h3><p>压力测试也是黑盒测试的一种，它的目标是放大服务器的性能问题以及并发状态下的正确性问题。我在<a href="https://wudaijun.com/2019/09/gs-pressure-test/">如何给GS做压测</a>中简单地阐述过压测的一些注意事项。简单来说，用例测试注重特例和自动化，而压力测试注重随机和覆盖率。</p><h3 id="模拟测试"><a href="#模拟测试" class="headerlink" title="模拟测试"></a>模拟测试</h3><p>模拟测试是指通过类似console的方式来模拟客户端，它的功能主要分为两部分:</p><ol><li>动态构造消息并返回响应数据</li><li>支持一些简单的GM，如查看/修改自身数据</li></ol><p>它最大的优点在于灵活性，主要有两个作用:</p><ol><li>服务器新功能开发完成进行快速自测验证(脱离客户端)，提升开发效率</li><li>出现某些疑似服务器的BUG时，登录已有角色进行数据验证和Debug</li></ol><p>以下是我们的模拟测试的样子:</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">&#x2F;&#x2F; 注: FC[...]# 为输入行，其余为输出行    &quot;&#x2F;&#x2F;...&quot;表示省略消息具体内容</span><br><span class="line">FakeClient connect successed</span><br><span class="line">FC[NotAuth]# auth test</span><br><span class="line">send msg: AuthReq:type:&quot;anonymous&quot; passport:&quot;user_fakeclienttest&quot; password:&quot;user_fakeclienttest&quot;</span><br><span class="line">recv msg: AuthAck &#x2F;&#x2F;... </span><br><span class="line">FC[Authed:281474976712031]#</span><br><span class="line">FC[Authed:281474976712031]# char login 11</span><br><span class="line">send msg: LoginReq: &#x2F;&#x2F; ...</span><br><span class="line">recv msg: LoginAck playerID:27113 &#x2F;&#x2F; ...</span><br><span class="line">FC[Logined:27113]#</span><br><span class="line">FC[Logined:27113]#send HeartBeatReq &#123;ClientTs:111&#125;</span><br><span class="line">send msg: HeartBeatReq:clientTs:111 </span><br><span class="line">recv msg: HeartBeatAck clientTs:111 serverTs:1597664509306</span><br><span class="line">FC[Logined:27113]#</span><br><span class="line">FC[Logined:27113]# self all</span><br><span class="line">&#123;&quot;ID&quot;:27113,&quot;name&quot;:&quot;Newbie 27113&quot;, &#x2F;&#x2F; ...</span><br></pre></td></tr></table></figure><h3 id="最后"><a href="#最后" class="headerlink" title="最后"></a>最后</h3><p>集成测试，压力测试，模拟测试，核心都需要一个模拟客户端，因此完全可以构建一套通用的fakeclient逻辑，包含基础网络通信，登录流程，数据状态同步等等。比如我们还基于fakeclient搭建了用于监控线上服务器可用性的监控机器人。</p><p>前面分别提到Q1，Q2的解决方案，至于Q3，我们的经验是，同学们之所以不愿意写测试，大部分原因都是测试框架还不够完善易用。另外，应该达成共识的是，开发效率并不只算单方面当前的开发时间，还应该包括客户端联调，QA验证反馈，后续重构负担等的时间，从这个角度来说，良好的测试规范起到的作用毋容置疑。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;在之前的博客中几次简单提及过给GS做测试，关于测试的必要性不用再多说，但在实际实践过程中，却往往会因为如下原因导致想要推进测试规范困难重重:&lt;/p&gt;
&lt;p&gt;-. Q1: 写测试代码困难: 代码耦合重，各种相互依赖，全局依赖，导致写测试代码”牵一发而动全身”，举步维艰&lt;br&gt;-. Q2: 测试时效性低: 需求变更快，数值变更频繁，可能导致今天写好的测试代码，明天就”过时”了&lt;br&gt;-. Q3: 开发进度紧: 不想浪费过多时间来写测试代码，直接开发感觉开发效率更高&lt;/p&gt;
&lt;p&gt;要想推进测试规范，上面的三个问题是必须解决的。这里简单聊聊我们在Golang游戏后端中的测试实践和解决方案。我们在GS中尝试的测试方案主要分为四种: 单元测试，集成测试，压力测试，以及模拟测试。&lt;/p&gt;</summary>
    
    
    
    <category term="gameserver" scheme="http://wudaijun.com/categories/gameserver/"/>
    
    
    <category term="gameserver" scheme="http://wudaijun.com/tags/gameserver/"/>
    
  </entry>
  
  <entry>
    <title>编程语言思维导图</title>
    <link href="http://wudaijun.com/2020/04/language-mind/"/>
    <id>http://wudaijun.com/2020/04/language-mind/</id>
    <published>2020-04-24T16:00:00.000Z</published>
    <updated>2020-04-25T08:06:57.000Z</updated>
    
    <content type="html"><![CDATA[<p>用XMind Zen 2020画了一张思维导图玩，主要是关于编程语言学习的，XMind导出的Markdown不包含链接，不知道是不是试用版的原因，由于对应链接在博客里面都找得到。就没有再做一份Markdown了。</p><p><img src="/assets/image/202004/language.png" alt=""></p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;用XMind Zen 2020画了一张思维导图玩，主要是关于编程语言学习的，XMind导出的Markdown不包含链接，不知道是不是试用版的原因，由于对应链接在博客里面都找得到。就没有再做一份Markdown了。&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;/assets/image</summary>
      
    
    
    
    <category term="mindmap" scheme="http://wudaijun.com/categories/mindmap/"/>
    
    
    <category term="mindmap" scheme="http://wudaijun.com/tags/mindmap/"/>
    
  </entry>
  
  <entry>
    <title>初识 Rust</title>
    <link href="http://wudaijun.com/2020/02/rust-basic/"/>
    <id>http://wudaijun.com/2020/02/rust-basic/</id>
    <published>2020-02-15T16:00:00.000Z</published>
    <updated>2020-03-01T03:25:21.000Z</updated>
    
    <content type="html"><![CDATA[<p>之前被同事安利了很多次Rust，周末没事去<a href="https://kaisery.github.io/trpl-zh-cn/title-page.html">Rust官方文档</a>学习了下，记录一些对Rust语言粗浅理解。</p><h3 id="一-所有权系统"><a href="#一-所有权系统" class="headerlink" title="一. 所有权系统"></a>一. 所有权系统</h3><p>要说Rust最有别于其它语言的特性，应该就是它的所有权系统了。要谈所有权系统，从GC谈起是个不错的切入点，我们众所周知的程序语言GC只要包含两种: 手动GC和自动GC，它们各有利弊，总的来说是运行时效率和开发效率之前的权衡取舍，由于现代硬件设施发展速度很快，运行时效率越来越不是问题，因此自动GC逐渐成为新语言的标配。而Rust的GC，按照我的理解，可以将其看做半自动GC，即开发者在代码中通过所有权约束来明确变量的生命周期，这样Rust在编译器就已经知道内存应该何时释放，也就不需要运行时通过复杂的<a href="[常见GC算法](https://wudaijun.com/2017/12/gc-study/">GC算法</a>去解析变量的引用关系，对运行时几乎零负担，这也是Rust敢号称系统级编程语言，运行时效率叫板C/C++的底气来源。</p><a id="more"></a><p>Rust GC的核心就是所有权系统，它基于以下事实:</p><ol><li>编译器能够解析局部变量的生命周期，正确管理栈内存的收缩扩张</li><li>堆内存最终都是通过栈变量来读取和修改</li></ol><p>那么，我们能否让堆内存管理和栈内存管理一样轻松，成为编译期就生成好的指令呢？Rust就是沿着这个思路走的，它将堆内存的生命周期和栈变量绑定在一起，当函数栈被回收，局部变量失效时，其对应的堆内存也会被回收。</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    <span class="keyword">let</span> s = <span class="built_in">String</span>::from(<span class="string">&quot;hello&quot;</span>); <span class="comment">// 从此处起，s 是有效的</span></span><br><span class="line">    <span class="comment">// 使用 s</span></span><br><span class="line">&#125;                               <span class="comment">// 此作用域已结束，</span></span><br><span class="line">                                <span class="comment">// s 不再有效</span></span><br></pre></td></tr></table></figure><p>如代码所示，局部变量s和对应的字符串堆内存绑定在了一起，称s对这块堆内存具备所有权，当s无效时，对应String堆内存也会回收。编译器知道s的作用域，也就自然知道何时执行对String执行回收。</p><p>以上是所有权系统的核心理念，但要达成整套系统的完备性，还需要一些其它规则来辅助:</p><h4 id="1-控制权转移"><a href="#1-控制权转移" class="headerlink" title="1. 控制权转移"></a>1. 控制权转移</h4><p>当发生局部变量赋值，如执行 <code>let s1 = s;</code> 时，Rust要么执行深拷贝，代价是运行时开销，要么浅拷贝，代价是s和s1只能有其中一个对String有所有权(否则对堆内存二次回收)。Rust选择了第二种方案，即s1拥有String的所有权，s在赋值给s1后不再有效，这之后对s的访问将会导致编译错误。在Rust中，这叫做<strong>控制权转移</strong>，如果不想发生控制权转移，可以使用<code>s.clone()</code>来获得s的深拷贝。</p><h4 id="2-值语义的控制权转移"><a href="#2-值语义的控制权转移" class="headerlink" title="2. 值语义的控制权转移"></a>2. 值语义的控制权转移</h4><p>针对简单值(即可直接分配在栈上的值)，按照控制权转移规则，如果有<code>let a = 5; let b = a;</code> 那么a也无效了，而实际上针对整数这种类型，大多数语言都是值语义的，在Rust中，这种值语义的类型通过<code>Copy trait</code>来实现，对于<code>Copy trait</code>类型来说，它不需要显式Clone，也不会发生控制权转移。</p><p>同样不会发生控制权转移的还有切片，如<code>let a = [1, 2, 3, 4, 5]; let slice = &amp;a[1..3];</code>，slice指向了a的部分元素，相当于引用了a的一部分，这不会发生控制权转移。</p><h4 id="3-函数调用的控制权转移"><a href="#3-函数调用的控制权转移" class="headerlink" title="3. 函数调用的控制权转移"></a>3. 函数调用的控制权转移</h4><p>按照局部变量赋值的控制权转移规则，函数返回值和函数参数的隐式赋值也会导致控制权转移:</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">fn</span> <span class="title">main</span></span>() &#123;</span><br><span class="line">        <span class="keyword">let</span> s1 = <span class="built_in">String</span>::from(<span class="string">&quot;hello&quot;</span>);</span><br><span class="line">        <span class="comment">// 调用calculate_length后，s1的控制权转移给了函数实参，在这之后s1就失效了</span></span><br><span class="line">        <span class="comment">// 为了后续能够继续访问String数据，需要通过返回值将控制权又转移回来</span></span><br><span class="line">        <span class="keyword">let</span> (s2, len) = calculate_length(s1); </span><br><span class="line">        <span class="comment">// 这里就不能继续访问s1了，只能使用s2</span></span><br><span class="line">        <span class="built_in">println!</span>(<span class="string">&quot;The length of &#x27;&#123;&#125;&#x27; is &#123;&#125;.&quot;</span>, s2, len);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">fn</span> <span class="title">calculate_length</span></span>(s: <span class="built_in">String</span>) -&gt; (<span class="built_in">String</span>, <span class="built_in">usize</span>) &#123;</span><br><span class="line">        <span class="keyword">let</span> length = s.len();</span><br><span class="line">        (s, length)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>可以看到，在控制权转移规则下，这种控制权转来转去的方式非常麻烦。这种情况下，更合适的做法是使用引用，在不转移控制权的前提下传递参数，但这里我们以另一个函数<code>first_word</code>为例，该函数求字符串内空格分隔的第一个单词:</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// first_word 通过引用借用了 s1，不发生控制权转移，函数返回后也不会回收形参s指向的值</span></span><br><span class="line"><span class="function"><span class="keyword">fn</span> <span class="title">first_word</span></span>(s: &amp;<span class="built_in">String</span>) -&gt; &amp;<span class="built_in">str</span> &#123;</span><br><span class="line">    <span class="keyword">let</span> bytes = s.as_bytes();</span><br><span class="line">    <span class="keyword">for</span> (i, &amp;item) <span class="keyword">in</span> bytes.iter().enumerate() &#123;</span><br><span class="line">        <span class="keyword">if</span> item == <span class="string">b&#x27; &#x27;</span> &#123;</span><br><span class="line">            <span class="keyword">return</span> &amp;s[<span class="number">0</span>..i];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    &amp;s[..]</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">fn</span> <span class="title">main</span></span>() &#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="keyword">mut</span> s = <span class="built_in">String</span>::from(<span class="string">&quot;hello world&quot;</span>);</span><br><span class="line">    <span class="keyword">let</span> word = first_word(&amp;s);</span><br><span class="line">    s.clear();</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;the first word is: &#123;&#125;&quot;</span>, word);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>选用<code>fist_word</code>是因为它展示了Rust引用的另一个有意思的特性。由于<code>first_word</code>返回的引用结果是基于引用参数的局部引用，因此当main调用<code>s.clear()</code>时，事实上也导致word引用失效了，导致得到非预期的结果。这在其它语言是指针/引用带来的难点之一，即要依靠开发者去解析内存引用关系，确保对内存的修改不会有非预期的副作用。而在Rust中，上面的代码不会通过编译！因为Rust对引用做了诸多限制:</p><ol><li>和变量一样，引用分为可变引用和不可变引用，可变引用需要在可变变量的基础上再显式声明: 如<code>let r = &amp;mut s;</code></li><li>在任意给定时间，要么只能有一个可变引用，要么只能有多个不可变引用</li><li>引用必须总是有效的 (例如函数返回一个局部变量的引用将会得到编译错误)</li></ol><p>结合上面的规则，<code>s.clear</code>需要清空string，因此它会尝试获取s的一个可变引用(函数原型为:<code>clear(&amp;mut self)</code>)，而由于s已经有一个不可变引用word，这破坏了规则2，因此编译器会报错。Rust通过显式的引用可变性 + 编译期检查实现了类似常量指针的功能(但其实在Rust中真正指针是Box<T>，它是另一个独立的对象)。</p><h4 id="4-引用有效性问题"><a href="#4-引用有效性问题" class="headerlink" title="4. 引用有效性问题"></a>4. 引用有效性问题</h4><p>引入了引用并不是就万事大吉了，还要考虑引用有效性问题:</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    <span class="comment">// rust不允许存在空值，确切地说是不允许使用空值，这里只是声明r，在第一次使用r前必须先初始化它，否则编译器会报错</span></span><br><span class="line">    <span class="keyword">let</span> r;</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">let</span> x = <span class="number">5</span>;</span><br><span class="line">        r = &amp;x;</span><br><span class="line">    &#125; <span class="comment">// 这之后 r 引用的 x 已经脱离作用域失效了，而 r 还在有效作用域内，继续访问 r 将会导致非预期结果</span></span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;r: &#123;&#125;&quot;</span>, r);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>那么如果避免以上代码呢，当然又是依靠万能的Rust编译器了，编译器的<strong>借用检查器</strong>会比较引用和被引用数据的生命周期，确保不会出现悬挂引用。</p><h4 id="5-生命周期注解"><a href="#5-生命周期注解" class="headerlink" title="5. 生命周期注解"></a>5. 生命周期注解</h4><p>有Rust编译器的殚精竭虑，开发者就能安全使用这套所有权系统而高枕无忧了么，当然不是，编译器所知也仅限于编译期就能获得的信息，比如以下代码:</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">fn</span> <span class="title">longest</span></span>(x: &amp;<span class="built_in">str</span>, y: &amp;<span class="built_in">str</span>) -&gt; &amp;<span class="built_in">str</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> x.len() &gt; y.len() &#123;</span><br><span class="line">        x</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        y</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">fn</span> <span class="title">main</span></span>() &#123;</span><br><span class="line">    <span class="keyword">let</span> string1 = <span class="built_in">String</span>::from(<span class="string">&quot;abcd&quot;</span>);</span><br><span class="line">    <span class="keyword">let</span> string2 = <span class="string">&quot;xyz&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">let</span> result = longest(string1.as_str(), string2);</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;The longest string is &#123;&#125;&quot;</span>, result);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>上面的代码无法通过编译，因为longest函数的参数和返回值都是引用，编译器无法获悉函数返回的引用是来自于x还是来自于y(这是运行时的东西)，那么前面说的借用检查器也就无法通过分析作用域保证引用的有效性了。</p><p>这个时候就需要建立一套额外的规则来辅助借用检查器，将本来应该在运行时决议的事情放到编译器来完成，Rust把这套规则叫做<strong>生命周期注解</strong>，生命周期注解本身不影响引用的生命周期，它用来指定函数的引用参数和引用返回值之间的生命周期对应关系，这样编译器就可以按照这种关系进行引用生命周期推敲，生命周期注释的语法和泛型类似(这也是比较有意思的一点，将引用生命周期像类型一样来抽象):</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// &#x27;a 和泛型中的T一样，这里的注解表示:  &#x27;a 的具体生命周期等同于 x 和 y 的生命周期中较小的那一个</span></span><br><span class="line"><span class="function"><span class="keyword">fn</span> <span class="title">longest</span></span>&lt;<span class="symbol">&#x27;a</span>&gt;(x: &amp;<span class="symbol">&#x27;a</span> <span class="built_in">str</span>, y: &amp;<span class="symbol">&#x27;a</span> <span class="built_in">str</span>) -&gt; &amp;<span class="symbol">&#x27;a</span> <span class="built_in">str</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> x.len() &gt; y.len() &#123;</span><br><span class="line">        x</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        y</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 下面是一些例子，说明Rust编译器是如何依靠注解来保证引用的有效性的</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 例1 </span></span><br><span class="line"><span class="comment">// 根据longest的生命周期注解，result的生命周期应该等于string1,string2中较短的那个</span></span><br><span class="line"><span class="comment">// 而这里result的生命周期明显大于string2，因此借用检查器会报错</span></span><br><span class="line"><span class="function"><span class="keyword">fn</span> <span class="title">main</span></span>() &#123;</span><br><span class="line">    <span class="keyword">let</span> string1 = <span class="built_in">String</span>::from(<span class="string">&quot;long string is long&quot;</span>);</span><br><span class="line">    <span class="keyword">let</span> result;</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">let</span> string2 = <span class="built_in">String</span>::from(<span class="string">&quot;xyz&quot;</span>);</span><br><span class="line">        result = longest(string1.as_str(), string2.as_str());</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;The longest string is &#123;&#125;&quot;</span>, result);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 例2</span></span><br><span class="line"><span class="comment">// 这里的result虽然早于string2声明，但由于Rust不能使用未赋值的变量，因此result的生命周期其实是从第一次赋值开始的</span></span><br><span class="line"><span class="comment">// 从而满足longest引用返回值生命周期&lt;=任一引用参数生命周期，能够正常运行</span></span><br><span class="line"><span class="function"><span class="keyword">fn</span> <span class="title">main</span></span>() &#123;</span><br><span class="line">    <span class="keyword">let</span> string1 = <span class="built_in">String</span>::from(<span class="string">&quot;long string is long&quot;</span>);</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">let</span> result;</span><br><span class="line">        <span class="keyword">let</span> string2 = <span class="built_in">String</span>::from(<span class="string">&quot;xyz&quot;</span>);</span><br><span class="line">        result = longest(string1.as_str(), string2.as_str());</span><br><span class="line">        <span class="built_in">println!</span>(<span class="string">&quot;The longest string is &#123;&#125;&quot;</span>, result);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">// 例3</span></span><br><span class="line"><span class="comment">// 如果我们将longest改成这样，它将不能通过编译</span></span><br><span class="line"><span class="comment">// 因为编译器看到了longest函数返回了y，然而生命周期注解中，输入引用y和返回值引用是两个独立的生命周期，互不关联。编译器觉得自己被欺骗了。</span></span><br><span class="line"><span class="function"><span class="keyword">fn</span> <span class="title">longest</span></span>&lt;<span class="symbol">&#x27;a</span>,<span class="symbol">&#x27;b</span>&gt;(x: &amp;<span class="symbol">&#x27;a</span> <span class="built_in">str</span>, y: &amp;<span class="symbol">&#x27;b</span> <span class="built_in">str</span>) -&gt; &amp;<span class="symbol">&#x27;a</span> <span class="built_in">str</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> x.len() &gt; y.len() &#123;</span><br><span class="line">        x</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        y</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="6-其它补丁"><a href="#6-其它补丁" class="headerlink" title="6. 其它补丁"></a>6. 其它补丁</h4><p>除了前面讨论的这些特性之外，Rust还针对所有权系统提供了其它工具，用于各类上述规则无法满足的情形，这里只是列举，不再详述。</p><ol><li><code>Box&lt;T&gt;</code>: 相当于C的<code>malloc</code>，用于允许将一个值放在堆上而不是栈上，在栈上只保留固定大小的指针字段</li><li><code>Rc&lt;T&gt;</code>: 非线程安全的引用计数指针，用于实现多所有权，通过<code>Rc::clone</code>即可获得一个新的指针，与被克隆指针指向同一个对象</li><li><code>RefCell&lt;T&gt;</code>: 能够基于不可变值修改其内部值(即RefCell字段)，它的本质是对RefCell字段的可变借用检查将<strong>发生运行时而非编译期</strong></li></ol><p>总之，Rust编译器是天生保守的，它会尽全力拒绝那些可能不正确的程序，Rust确实能在编译期检查到很多大部分语言只能在运行期暴露的错误，这是Rust最迷人的地方之一。但是，与此同时，Rust编译器也可能会拒绝一些正确的程序，此时就需要如生命周期注解，<code>Rc&lt;T&gt;</code>等工具来辅助编译器，甚至通过<code>RefCell&lt;T&gt;</code>，unsafe等方案来绕过编译器检查。把<strong>编译器做厚</strong>，把<strong>运行时做薄</strong>，是Rust易用但高效，能够立足于系统级编程语言的根本。</p><p>最后总结下Rust所有权系统的规则:</p><ol><li>Rust 中的每一个值都有一个被称为其 所有者（owner）的变量。</li><li>值有且只有一个所有者。</li><li>当所有者（变量）离开作用域，这个值将被丢弃。</li></ol><p>Rust的所有权系统重度依赖编译器的各种检查，在使用简单和运行快速安全之下，是Rust编译器在负重前行，Rust的编译速度目前来看还不是很理想，一直在优化。但作为一名开发者，个人是很赞同这种<strong>编译器能做的检查，就决不让开发者操心</strong>的准则的。</p><h3 id="二-函数式特性"><a href="#二-函数式特性" class="headerlink" title="二. 函数式特性"></a>二. 函数式特性</h3><p>我在<a href="https://wudaijun.com/2018/05/understand-functional-programing/">理解函数式编程</a>中提到，现在的语言不再受限于各种编程范式的约束，而是更偏实用主义，Rust也是这样的语言，它受函数式语言的影响颇深。</p><h4 id="1-函数是第一类对象"><a href="#1-函数是第一类对象" class="headerlink" title="1. 函数是第一类对象"></a>1. 函数是第一类对象</h4><p>函数可作为参数，返回值，动态创建，并且动态创建的函数具备捕获当前作用域上下文的能力，也就是闭包，提供标准库容器迭代器模式并支持开发者扩展等，这些都是如今大部分语言的标配，无需过多解释。</p><p>有一点需要提一下，Rust的闭包如果要捕获上下文的话，也要考虑到所有权转移的问题(转移，引用，可变引用)，并且Rust编译器会尝试自动推测你的闭包希望以那种方式来捕获环境。</p><h4 id="2-变量可变性"><a href="#2-变量可变性" class="headerlink" title="2. 变量可变性"></a>2. 变量可变性</h4><p>Rust中的变量默认是不可变的，但也支持通过<code>let mut x = 5;</code>声明可变变量。合理使用不可变变量能够利用编译器检查使代码易于推导，可重入，无副作用。</p><h4 id="3-模式匹配"><a href="#3-模式匹配" class="headerlink" title="3. 模式匹配"></a>3. 模式匹配</h4><p>模式匹配我最早在Erlang中接触，这个起初不是很适应的功能在用习惯之后，会发现它可以为程序提供更多对程序控制流的支配权，写出强大而简洁的代码。Rust也支持模式匹配:</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Point</span></span> &#123;</span><br><span class="line">    x: <span class="built_in">i32</span>,</span><br><span class="line">    y: <span class="built_in">i32</span>,</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">fn</span> <span class="title">main</span></span>() &#123;</span><br><span class="line">    <span class="keyword">let</span> p = Point &#123; x: <span class="number">7</span>, y: <span class="number">2</span> &#125;; <span class="comment">// 构造一个Point值匹配给命名变量p</span></span><br><span class="line">    <span class="keyword">match</span> p &#123;</span><br><span class="line">        Point &#123; x :<span class="number">0</span>, y&#125; =&gt; <span class="built_in">println!</span>(<span class="string">&quot;case 1: &#123;&#125;&quot;</span>, y), <span class="comment">// 匹配p.x==0</span></span><br><span class="line">        Point &#123; x, y: <span class="number">0</span>..=<span class="number">2</span> &#125; =&gt; <span class="built_in">println!</span>(<span class="string">&quot;case 2: &#123;&#125;&quot;</span>, x), <span class="comment">// 匹配0&lt;=p.y&lt;=2</span></span><br><span class="line">        Point &#123; x: a, y: _ &#125; =&gt; <span class="built_in">println!</span>(<span class="string">&quot;case 3: &#123;&#125;&quot;</span>, a), <span class="comment">// 匹配其它情况，并将字段x的值赋给变量a</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">let</span> Point &#123;x: <span class="number">7</span>, y: b&#125; = p &#123; <span class="comment">// 匹配 x==7，并取出y值</span></span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;case 4: &#123;&#125;&quot;</span>, b)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="三-OOP特性"><a href="#三-OOP特性" class="headerlink" title="三. OOP特性"></a>三. OOP特性</h3><h4 id="1-Object"><a href="#1-Object" class="headerlink" title="1. Object"></a>1. Object</h4><p>Rust提供基本的结构体字段封装和字段访问控制(可见性)，并且允许在此之上扩展结构体方法及方法的可见性:</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">pub</span> <span class="class"><span class="keyword">struct</span> <span class="title">MyStruct</span></span> &#123;</span><br><span class="line">    x : <span class="built_in">i32</span>     <span class="comment">// 默认为私有字段</span></span><br><span class="line">    <span class="keyword">pub</span> name : <span class="built_in">String</span> <span class="comment">// 指定为公有字段</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">impl</span> MyStruct &#123;</span><br><span class="line">    <span class="keyword">pub</span> <span class="function"><span class="keyword">fn</span> <span class="title">getx</span></span>(&amp;<span class="keyword">self</span>) -&gt; <span class="built_in">i32</span> &#123;</span><br><span class="line">        <span class="keyword">self</span>.x</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="2-继承"><a href="#2-继承" class="headerlink" title="2. 继承"></a>2. 继承</h4><p>OOP的继承(subclass)主要有两个作用，<strong>代码复用</strong> 和 <strong>子类化(subtype)</strong> ，如C++的继承就同时实现了这两点，继承是一把双刃剑，因为传统继承不只是有代码复用和子类化的功能，它还做到了字段复用，即对象父子内存模型的一致性，当引入对象内存模型之后，各种多重继承，菱形继承所带来的问题不堪其扰。<strong>虚基类</strong>，<strong>显式指定父类作用域</strong>或者干脆<strong>不允许多重继承</strong>等方案也是头痛医头，脚痛医脚。</p><p>近10年兴起的新语言，如Golang就没有继承，它通过内嵌匿名结构体来实现代码复用，但丢失了dynamic dispatch，通过interface{}(声明式接口，隐式implement)来实现子类化，但也带来了运行时开销。</p><p>关于subclass, subtype, dynamic dispatch等概念，可以参考我之前的<a href="https://wudaijun.com/2019/05/programing-paradigm/">编程范式游记</a>)。</p><p>在Rust中，是通过trait来实现这两者的，trait本质上是<strong>实现式接口</strong>，用于对不同类型的相同方法进行抽象。Rust的trait有如下特性:</p><ol><li>trait是需要显式指明继承实现的</li><li>trait可以提供默认实现，但不能包含字段(部分subclass)</li><li>trait的默认实现可以调用trait中的其它方法，哪怕这些方法没有提供默认实现(dynamic dispatch)</li><li>trait可以用做参数或返回值用于表达满足该接口的类型实例抽象(subtype)</li><li>trait可通过+号实现拼接，表示同时满足多个接口</li></ol><p>以下代码简单展示了Rust trait的基本特性:</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">pub</span> <span class="class"><span class="keyword">trait</span> <span class="title">Summary</span></span> &#123;</span><br><span class="line">    <span class="function"><span class="keyword">fn</span> <span class="title">summarize_author</span></span>(&amp;<span class="keyword">self</span>) -&gt; <span class="built_in">String</span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">fn</span> <span class="title">summarize</span></span>(&amp;<span class="keyword">self</span>) -&gt; <span class="built_in">String</span> &#123;</span><br><span class="line">        <span class="built_in">format!</span>(<span class="string">&quot;(Read more from &#123;&#125;...)&quot;</span>, <span class="keyword">self</span>.summarize_author())</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">pub</span> <span class="class"><span class="keyword">trait</span> <span class="title">Title</span></span> &#123;</span><br><span class="line">    <span class="function"><span class="keyword">fn</span> <span class="title">title</span></span>(&amp;<span class="keyword">self</span>) -&gt; <span class="built_in">String</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">pub</span> <span class="class"><span class="keyword">struct</span> <span class="title">Tweet</span></span> &#123;</span><br><span class="line">    <span class="keyword">pub</span> username: <span class="built_in">String</span>,</span><br><span class="line">    <span class="keyword">pub</span> title: <span class="built_in">String</span>,</span><br><span class="line">    <span class="keyword">pub</span> content: <span class="built_in">String</span>,</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">impl</span> Summary <span class="keyword">for</span> Tweet &#123;</span><br><span class="line">    <span class="function"><span class="keyword">fn</span> <span class="title">summarize_author</span></span>(&amp;<span class="keyword">self</span>) -&gt; <span class="built_in">String</span> &#123;</span><br><span class="line">        <span class="built_in">format!</span>(<span class="string">&quot;@&#123;&#125;&quot;</span>, <span class="keyword">self</span>.username)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">impl</span> Title <span class="keyword">for</span> Tweet &#123;</span><br><span class="line">    <span class="function"><span class="keyword">fn</span> <span class="title">title</span></span>(&amp;<span class="keyword">self</span>) -&gt; <span class="built_in">String</span> &#123;</span><br><span class="line">        <span class="built_in">format!</span>(<span class="string">&quot;&#123;&#125;&quot;</span>, <span class="keyword">self</span>.title)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 函数声明等价于 pub fn notify&lt;T: Summary + Title&gt;(item: T) &#123;</span></span><br><span class="line"><span class="comment">// 即本质是bounded generic types的语法糖</span></span><br><span class="line"><span class="keyword">pub</span> <span class="function"><span class="keyword">fn</span> <span class="title">notify</span></span>(item: <span class="keyword">impl</span> Summary + Title) &#123;</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;1 new tweet: &#123;&#125;, &#123;&#125;&quot;</span>, item.title(), item.summarize());</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">fn</span> <span class="title">main</span></span>() &#123;</span><br><span class="line">        <span class="keyword">let</span> t = Tweet &#123;</span><br><span class="line">        username: <span class="built_in">String</span>::from(<span class="string">&quot;wudaijun&quot;</span>),</span><br><span class="line">        title: <span class="built_in">String</span>::from(<span class="string">&quot;study rust&quot;</span>),</span><br><span class="line">        content: <span class="built_in">String</span>::from(<span class="string">&quot;rust is the best language&quot;</span>)</span><br><span class="line">        &#125;;</span><br><span class="line">        notify(t)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>总的来说，Rust对OOP的支持是比较完善的，舍弃了继承和字段复用，通过trait来完成代码复用和子类化，避免了OOP继承的各种坑。</p><h3 id="四-泛型和元编程"><a href="#四-泛型和元编程" class="headerlink" title="四. 泛型和元编程"></a>四. 泛型和元编程</h3><p>Rust的泛型和元编程赋予语言更强大的灵活性。这里只列举个人目前学习到的一些要点。</p><p>Rust泛型的一些特性:</p><ol><li>模板泛型: 在编译期填充具体类型，实现单态化</li><li>支持枚举泛型: 如: <code>enum Option&lt;T&gt; &#123; Some(T), None, &#125;</code></li><li>Trait Bound: <code>fn notify(item: impl Summary) &#123;...</code> 等价于 <code>fn notify&lt;T: Summary&gt;(item: T) &#123;...</code> 等价于 <code>fn notify&lt;T&gt;(item: T) where T: Summary &#123;...</code></li><li>blanket implementations: 对实现了特定 trait 的类型有条件地实现方法，如标准库为任何实现了<code>Display</code> trait的类型实现了<code>ToString</code> trait: <code>impl&lt;T: Display&gt; ToString for T &#123;</code>。这意味着你实现了A trait，标准库/第三方库就可以为你实现B trait。这是trait和泛型的一种特殊结合，也是Rust trait和传统OOP不同的地方之一</li></ol><p>元编程能够生成代码的代码，如C++的模板由于其在预编译期处理，并且图灵完备，完全可以作为另一种语言来看待，它的执行结果就是另一种语言的代码。Rust的元编程通过宏来实现，宏的语法类似于这样:</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#[macro_export]</span></span><br><span class="line"><span class="built_in">macro_rules!</span> vec &#123;</span><br><span class="line">    ( $( $x:expr ),* ) =&gt; &#123;</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">let</span> <span class="keyword">mut</span> temp_vec = <span class="built_in">Vec</span>::new();</span><br><span class="line">            $(</span><br><span class="line">                temp_vec.push($x);</span><br><span class="line">            )*</span><br><span class="line">            temp_vec</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这段代码能将<code>let v: Vec&lt;u32&gt; = vec![1, 2, 3];</code>转换成:</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> <span class="keyword">mut</span> temp_vec = <span class="built_in">Vec</span>::new();</span><br><span class="line">temp_vec.push(<span class="number">1</span>);</span><br><span class="line">temp_vec.push(<span class="number">2</span>);</span><br><span class="line">temp_vec.push(<span class="number">3</span>);</span><br><span class="line">temp_vec</span><br></pre></td></tr></table></figure><p>由于宏编程日常开发中使用较少，这里不再展开讨论。</p><h3 id="五-并发编程"><a href="#五-并发编程" class="headerlink" title="五. 并发编程"></a>五. 并发编程</h3><p>基于Rust本身系统级编程语言的定位，Rust标准库本身只提供对OS Thread的基础抽象，即运行时本身不实现<strong>轻量级线程</strong>及其调度器，以保持其运行时的精简高效。</p><p>Rust的所有权系统设计之初是为了简化运行时的内存管理，解决内存安全问题，而Rust作为系统级编程语言，并发自然也是绕不过去的传统难题，起初Rust觉得这是两个独立的问题，然而随着所有权系统的完善，Rust发现<strong>所有权系统也能解决一系列的并发安全问题</strong>。相较于并发领域佼佼者Erlang前辈的口号”任其崩溃(let it crash)”，Rust的并发口号也是不输分毫: “无畏并发(fearless concurrency)”。下面我们来看看Rust为何如此自信，Rust支持消息交互和共享内存两种并发编程范式。</p><h4 id="1-消息交互"><a href="#1-消息交互" class="headerlink" title="1. 消息交互"></a>1. 消息交互</h4><p>Rust消息交互CSP模型，但也与Go这类CSP语言有一些区别。</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">fn</span> <span class="title">main</span></span>() &#123;</span><br><span class="line">    <span class="keyword">let</span> (tx, rx) = mpsc::channel();</span><br><span class="line">    <span class="keyword">let</span> val = <span class="built_in">String</span>::from(<span class="string">&quot;hi&quot;</span>);</span><br><span class="line">    thread::spawn(<span class="keyword">move</span> || &#123;</span><br><span class="line">        tx.send(val).unwrap();</span><br><span class="line">        </span><br><span class="line">    &#125;);</span><br><span class="line">    <span class="keyword">let</span> received = rx.recv().unwrap();</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;Got: &#123;&#125;&quot;</span>, received);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这个小小的例子有如下需要关注的细节:</p><ol><li>Rust 在创建 channel 时无需指定其大小，因为Rust Channel的大小是没有限制的，并且明确区分发送端和接收端，对channel的写入是永远不会阻塞的。</li><li>Rust 在创建 channel 时也无需指定其类型，这是因为 tx 和 rx 是泛型对象，编译器会根据其实际发送的数据类型来实例化泛型(如这里的<code>std::sync::mpsc::Sender&lt;std::string::String&gt;</code>)，如果尝试对同一个 channel 发送不同类型，如果代码中没有调用<code>tx.send</code>函数都将会导致编译错误。</li><li>Rust编译器本身会尝试推测闭包以何种方式捕获外部变量，但通常是保守的借用。这里 move 关键字强制闭包获取其使用的环境值的所有权，因此main函数在创建线程后对val和tx的任何访问都会导致编译错误</li><li><code>tx.send</code>本身是转移语义，即会转移val变量的控制权，因此在新创建线程在<code>tx.send(val)</code>之后对val的任何访问也会导致编译错误</li></ol><p>上面的3,4其实就是我们在并发编程常犯的错误：对相同变量的非并发安全访问，由于闭包的存在，使得这类”犯罪”的成本异常低廉。而Rust的所有权系统则巧妙地在编译器就发现了这类错误，因为变量所有权只会同时在一个线程中，也就避免了有意无意的变量共享(哪怕只读)。</p><h4 id="2-共享内存"><a href="#2-共享内存" class="headerlink" title="2. 共享内存"></a>2. 共享内存</h4><p>受Rust所有权系统的影响，Rust中的内存共享初看起来有点繁杂:</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">use</span> std::sync::&#123;Mutex, Arc&#125;;</span><br><span class="line"><span class="keyword">use</span> std::thread;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">fn</span> <span class="title">main</span></span>() &#123;</span><br><span class="line">    <span class="keyword">let</span> counter = Arc::new(Mutex::new(<span class="number">0</span>));</span><br><span class="line">    <span class="keyword">let</span> <span class="keyword">mut</span> handles = <span class="built_in">vec!</span>[];</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> _ <span class="keyword">in</span> <span class="number">0</span>..<span class="number">10</span> &#123;</span><br><span class="line">        <span class="keyword">let</span> counter = Arc::clone(&amp;counter);</span><br><span class="line">        <span class="keyword">let</span> handle = thread::spawn(<span class="keyword">move</span> || &#123;</span><br><span class="line">            <span class="keyword">let</span> <span class="keyword">mut</span> num = counter.lock().unwrap();</span><br><span class="line"></span><br><span class="line">            *num += <span class="number">1</span>;</span><br><span class="line">        &#125;);</span><br><span class="line">        handles.push(handle);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> handle <span class="keyword">in</span> handles &#123;</span><br><span class="line">        handle.join().unwrap();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;Result: &#123;&#125;&quot;</span>, *counter.lock().unwrap());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>同样，这里面也有一些细节:</p><ol><li>和channel一样，<code>Mutex&lt;T&gt;</code>也是泛型的，并且只能通过<code>lock</code>才能得到其中的<code>T</code>值，确保不会忘记加锁</li><li>Mutex会在脱离作用域时，会自动释放锁，确保不会忘记释放锁</li><li>这里有多个线程需要共享Mutex的所有权，因此需要用到并发安全的引用计数智能指针<code>Arc&lt;T&gt;</code>(<code>RC&lt;T&gt;</code>不是线程安全的)</li></ol><h3 id="六-体会"><a href="#六-体会" class="headerlink" title="六 体会"></a>六 体会</h3><p>本文比较散乱，主要从编程范式的角度理解Rust，总的来说，这门语言给我的感觉还是挺好的，既有强于其它静态语言的安全性，又想尽办法让编写Rust像动态语言一样方便简洁，这中间Rust编译器功不可没。所有权的概念让Rust在编程语言中独树一帜，可能要多适应下，这种特性让我想起了Erlang中的变量不可变，为代码分析和并发安全提供了很多便利，所有权系统本质上也是一种不变性约束，即<strong>一个值的所有权只能属于一个变量</strong>，这让编译器可以检查一些隐藏的代码错误，并且保持高效的运行时(对比golang的<code>-race</code>竞态检查要让运行时多占几倍CPU内存，并且运行时还要帮开发者检查并发的map访问)。准备有机会找个合适的场景用Rust实践下，增强理解。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;之前被同事安利了很多次Rust，周末没事去&lt;a href=&quot;https://kaisery.github.io/trpl-zh-cn/title-page.html&quot;&gt;Rust官方文档&lt;/a&gt;学习了下，记录一些对Rust语言粗浅理解。&lt;/p&gt;
&lt;h3 id=&quot;一-所有权系统&quot;&gt;&lt;a href=&quot;#一-所有权系统&quot; class=&quot;headerlink&quot; title=&quot;一. 所有权系统&quot;&gt;&lt;/a&gt;一. 所有权系统&lt;/h3&gt;&lt;p&gt;要说Rust最有别于其它语言的特性，应该就是它的所有权系统了。要谈所有权系统，从GC谈起是个不错的切入点，我们众所周知的程序语言GC只要包含两种: 手动GC和自动GC，它们各有利弊，总的来说是运行时效率和开发效率之前的权衡取舍，由于现代硬件设施发展速度很快，运行时效率越来越不是问题，因此自动GC逐渐成为新语言的标配。而Rust的GC，按照我的理解，可以将其看做半自动GC，即开发者在代码中通过所有权约束来明确变量的生命周期，这样Rust在编译器就已经知道内存应该何时释放，也就不需要运行时通过复杂的&lt;a href=&quot;[常见GC算法](https://wudaijun.com/2017/12/gc-study/&quot;&gt;GC算法&lt;/a&gt;去解析变量的引用关系，对运行时几乎零负担，这也是Rust敢号称系统级编程语言，运行时效率叫板C/C++的底气来源。&lt;/p&gt;</summary>
    
    
    
    <category term="rust" scheme="http://wudaijun.com/categories/rust/"/>
    
    
    <category term="rust" scheme="http://wudaijun.com/tags/rust/"/>
    
  </entry>
  
  <entry>
    <title>Golang GC核心要点和度量方法</title>
    <link href="http://wudaijun.com/2020/01/go-gc-keypoint-and-monitor/"/>
    <id>http://wudaijun.com/2020/01/go-gc-keypoint-and-monitor/</id>
    <published>2019-12-31T16:00:00.000Z</published>
    <updated>2020-01-10T14:14:46.000Z</updated>
    
    <content type="html"><![CDATA[<h3 id="一-Go-GC-要点"><a href="#一-Go-GC-要点" class="headerlink" title="一. Go GC 要点"></a>一. Go GC 要点</h3><p>先来回顾一下GC的几个重要的阶段:</p><h4 id="Mark-Prepare-STW"><a href="#Mark-Prepare-STW" class="headerlink" title="Mark Prepare - STW"></a>Mark Prepare - STW</h4><p>做标记阶段的准备工作，需要停止所有正在运行的goroutine(即STW)，标记根对象，启用内存屏障，内存屏障有点像内存读写钩子，它用于在后续并发标记的过程中，维护三色标记的完备性(三色不变性)，这个过程通常很快，大概在10-30微秒。</p><h4 id="Marking-Concurrent"><a href="#Marking-Concurrent" class="headerlink" title="Marking - Concurrent"></a>Marking - Concurrent</h4><p>标记阶段会将大概25%(gcBackgroundUtilization)的P用于标记对象，逐个扫描所有G的堆栈，执行三色标记，在这个过程中，所有新分配的对象都是黑色，被扫描的G会被暂停，扫描完成后恢复，这部分工作叫后台标记(<a href="https://github.com/golang/go/blob/dev.boringcrypto.go1.13/src/runtime/mgc.go#L1817">gcBgMarkWorker</a>)。这会降低系统大概25%的吞吐量，比如<code>MAXPROCS=6</code>，那么GC P期望使用率为<code>6*0.25=1.5</code>，这150%P会通过专职(Dedicated)/兼职(Fractional)/懒散(Idle)三种工作模式的Worker共同来完成。</p><a id="more"></a><p>这还没完，为了保证在Marking过程中，其它G分配堆内存太快，导致Mark跟不上Allocate的速度，还需要其它G配合做一部分标记的工作，这部分工作叫辅助标记(mutator assists)。在Marking期间，每次G分配内存都会更新它的”负债指数”(gcAssistBytes)，分配得越快，gcAssistBytes越大，这个指数乘以全局的”负载汇率”(assistWorkPerByte)，就得到这个G需要帮忙Marking的内存大小(这个计算过程叫<a href="https://github.com/golang/go/blob/dev.boringcrypto.go1.13/src/runtime/mgc.go#L484">revise</a>)，也就是它在本次分配的mutator assists工作量(<a href="https://github.com/golang/go/blob/dev.boringcrypto.go1.13/src/runtime/mgcmark.go#L363">gcAssistAlloc</a>)。</p><h4 id="Mark-Termination-STW"><a href="#Mark-Termination-STW" class="headerlink" title="Mark Termination - STW"></a>Mark Termination - STW</h4><p>标记阶段的最后工作是Mark Termination，关闭内存屏障，停止后台标记以及辅助标记，做一些清理工作，整个过程也需要STW，大概需要60-90微秒。在此之后，所有的P都能继续为应用程序G服务了。</p><h4 id="Sweeping-Concurrent"><a href="#Sweeping-Concurrent" class="headerlink" title="Sweeping - Concurrent"></a>Sweeping - Concurrent</h4><p>在标记工作完成之后，剩下的就是清理过程了，清理过程的本质是将没有被使用的内存块整理回收给上一个内存管理层级(mcache -&gt; mcentral -&gt; mheap -&gt; OS)，清理回收的开销被平摊到应用程序的每次内存分配操作中，直到所有内存都Sweeping完成。当然每个层级不会全部将待清理内存都归还给上一级，避免下次分配再申请的开销，比如Go1.12对mheap归还OS内存做了<a href="https://ms2008.github.io/2019/06/30/golang-madvfree/">优化</a>，使用<a href="https://go-review.googlesource.com/c/go/+/135395/">NADV_FREE</a>延迟归还内存。</p><h4 id="STW"><a href="#STW" class="headerlink" title="STW"></a>STW</h4><p>在<a href="https://wudaijun.com/2018/01/go-scheduler/">Go调度模型</a>中我们已经提到，Go没有真正的实时抢占机制，而是一套协作式抢占(cooperative preemption)，即给G(groutine)打个标记，等待G在调用函数时检查这个标记，以此作为一个安全的抢占点(GC safe-point)。但如果其它P上的G都停了，某个G还在执行如下代码:</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">add</span><span class="params">(numbers []<span class="keyword">int</span>)</span> <span class="title">int</span></span> &#123;</span><br><span class="line">     <span class="keyword">var</span> v <span class="keyword">int</span></span><br><span class="line">     <span class="keyword">for</span> _, n := <span class="keyword">range</span> numbers &#123;</span><br><span class="line">         v += n</span><br><span class="line">     &#125;</span><br><span class="line">     <span class="keyword">return</span> v</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>add函数的运行时间取决于切片的长度，并且在函数内部是没有调用其它函数的，也就是没有抢占点。就会导致整个运行时都在等待这个G调用函数(以实现抢占，开始处理GC)，其它P也被挂起。这就是Go GC最大的诟病: GC STW时间会受到G调用函数的时机的影响并被延长，甚至如果某个G在执行无法抢占的死循环(即循环内部没有发生函数调用的死循环)，那么整个Go的runtime都会挂起，CPU 100%，节点无法响应任何消息，连正常停服都做不到。pprof这类调试工具也用不了，只能通过gdb，delve等外部调试工具来找到死循环的goroutine正在执行的堆栈。如此后果比没有被defer的panic更严重，因为那个时候的节点内部状态是无法预期的。</p><p>因此有Gopher开始倡议Go使用非协作式抢占(non-cooperative preemption)，通过堆栈和寄存器来保存抢占上下文，避免对抢占不友好的函数导致GC STW延长(毕竟第三方库代码的质量也是参差不齐的)。相关的Issue在<a href="https://github.com/golang/go/issues/24543">这里</a>。好消息是，<strong><a href="https://tip.golang.org/doc/go1.14">Go1.14</a>(目前还是Beta1版本，还未正式发布)已经支持异步抢占</strong>，也就是说:</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 简单起见，没用channel协同</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">  <span class="keyword">go</span> <span class="function"><span class="keyword">func</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="keyword">for</span> &#123;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;()</span><br><span class="line"></span><br><span class="line">  time.Sleep(time.Millisecond)</span><br><span class="line">  runtime.GC()</span><br><span class="line">  <span class="built_in">println</span>(<span class="string">&quot;OK&quot;</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这段代码在Go1.14中终于能输出<code>OK</code>了。这个提了近五年的Issue: <a href="https://github.com/golang/go/issues/10958">runtime: tight loops should be preemptible #10958</a>前几天终于关闭了。不得不说，这是Go Runtime的一大进步，它不止避免了单个goroutine死循环导致整个runtime卡死的问题，更重要的是，它为STW提供了最坏预期，避免了GC STW造成了性能抖动隐患。</p><h3 id="二-Go-GC-度量"><a href="#二-Go-GC-度量" class="headerlink" title="二. Go GC 度量"></a>二. Go GC 度量</h3><h4 id="1-go-tool-prof"><a href="#1-go-tool-prof" class="headerlink" title="1. go tool prof"></a>1. go tool prof</h4><p>Go 基础性能分析工具，pprof的用法和启动方式参考<a href="https://wudaijun.com/2018/04/go-pprof/">go pprof性能分析</a>，其中的heap即为内存分配分析，go tool默认是查看正在使用的内存(<code>inuse_heap</code>)，如果要看其它数据，使用<code>go tool pprof --alloc_space|inuse_objects|alloc_objects</code>。</p><p>需要注意的是，go pprof本质是数据采样分析，其中的值并不是精确值，适用于性能热点优化，而非真实数据统计。</p><h4 id="2-go-tool-trace"><a href="#2-go-tool-trace" class="headerlink" title="2. go tool trace"></a>2. go tool trace</h4><p>go tool trace可以将GC统计信息以可视化的方式展现出来。要使用go tool trace，可以通过以下方式生成采样数据:</p><ol><li>API: <code>trace.Start</code></li><li>go test: <code>go test -trace=trace.out pkg</code></li><li>net/http/pprof: <code>curl http://127.0.0.1:6060/debug/pprof/trace?seconds=20</code></li></ol><p>得到采样数据后，之后即可以通过 <code>go tool trace trace.out</code> 启动一个HTTP Server，在浏览器中查看可视化trace数据:</p><p><img src="/assets/image/202001/trace-index.jpg" alt=""></p><p>里面提供了各种trace和prof的可视化入口，点击第一个View trace可以看到追踪总览:</p><p><img src="/assets/image/202001/trace-view.jpg" alt=""></p><p>包含的信息量比较广，横轴为时间线，各行为各种维度的度量，通过A/D左右移动，W/S放大放小。以下是各行的意义:</p><ul><li>Goroutines: 包含GCWaiting，Runnable，Running三种状态的Goroutine数量统计</li><li>Heap: 包含当前堆使用量(Allocated)和下次GC阈值(NextGC)统计</li><li>Threads: 包含正在运行和正在执行系统调用的Threads数量</li><li>GC: 哪个时间段在执行GC</li><li>ProcN: 各个P上面的goroutine调度情况</li></ul><p>除了<strong>View trace</strong>之外，trace目录的第二个<strong>Goroutine analysis</strong>也比较有用，它能够直观统计Goroutine的数量和执行状态:</p><p><img src="/assets/image/202001/trace-goroutines.jpg" alt=""></p><p><img src="/assets/image/202001/trace-goroutines2.jpg" alt=""></p><p>通过它可以对各个goroutine进行健康诊断，各种network,syscall的采样数据下载下来之后可以直接通过<code>go tool pprof</code>分析，因此，实际上pprof和trace两套工具是相辅相成的。</p><h4 id="3-GC-Trace"><a href="#3-GC-Trace" class="headerlink" title="3. GC Trace"></a>3. GC Trace</h4><p>GC Trace是Golang提供的非侵入式查看GC信息的方案，用法很简单，设置<code>GCDEBUG=gctrace=1</code>环境变量即可:</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">GODEBUG&#x3D;gctrace&#x3D;1 bin&#x2F;game</span><br><span class="line">gc 1 @0.039s 3%: 0.027+4.5+0.015 ms clock, 0.11+2.3&#x2F;4.0&#x2F;5.5+0.063 ms cpu, 4-&gt;4-&gt;2 MB, 5 MB goal, 4 P</span><br><span class="line">gc 2 @0.147s 1%: 0.007+1.2+0.008 ms clock, 0.029+0.15&#x2F;1.1&#x2F;2.0+0.035 ms cpu, 5-&gt;5-&gt;3 MB, 6 MB goal, 4 P</span><br><span class="line">gc 3 @0.295s 0%: 0.010+2.3+0.013 ms clock, 0.040+0.14&#x2F;2.1&#x2F;4.3+0.053 ms cpu, 7-&gt;7-&gt;4 MB, 8 MB goal, 4 P</span><br></pre></td></tr></table></figure><p>下面是各项指标的解释:</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line">gc 1 @0.039s 3%: 0.027+4.5+0.015 ms clock, 0.11+2.3&#x2F;4.0&#x2F;5.5+0.063 ms cpu, 4-&gt;4-&gt;2 MB, 5 MB goal, 4 P</span><br><span class="line"></span><br><span class="line">&#x2F;&#x2F; 通用参数</span><br><span class="line">gc 2: 程序运行后的第2次GC</span><br><span class="line">@0.147s: 到目前为止程序运行的时间</span><br><span class="line">3%: 到目前为止程序花在GC上的CPU%</span><br><span class="line"></span><br><span class="line">&#x2F;&#x2F; Wall-Clock 流逝的系统时钟</span><br><span class="line">0.027ms+4.5ms+0.015 ms   : 分别是 STW Mark Prepare，Concurrent Marking，STW Mark Termination 的时钟时间</span><br><span class="line"></span><br><span class="line">&#x2F;&#x2F; CPU Time 消耗的CPU时间</span><br><span class="line">0.11+2.3&#x2F;4.0&#x2F;5.5+0.063 ms : 以+分隔的阶段同上，不过将Concurrent Marking细分为Mutator Assists Time, Background GC Time(包括Dedicated和Fractional Worker), Idle GC Time三种。其中0.11&#x3D;0.027*4，0.063&#x3D;0.015*4。</span><br><span class="line"></span><br><span class="line">&#x2F;&#x2F; 内存相关统计</span><br><span class="line">4-&gt;4-&gt;2 MB: 分别是开始标记时，标记结束后的堆占用大小，以及标记结束后真正存活的(有效的)堆内存大小</span><br><span class="line">5 MB goal: 下次GC Mark Termination后的目标堆占用大小，该值受GC Percentage影响，并且会影响mutator assist工作量(每次堆大小变更时都动态评估，如果快超出goal了，就需要其它goroutine帮忙干活了, https:&#x2F;&#x2F;github.com&#x2F;golang&#x2F;go&#x2F;blob&#x2F;dev.boringcrypto.go1.13&#x2F;src&#x2F;runtime&#x2F;mgc.go#L484)</span><br><span class="line"></span><br><span class="line">&#x2F;&#x2F; Processors</span><br><span class="line">4 P : P的数量，也就是GOMAXPROCS大小，可通过runtime.GoMaxProcs设置</span><br><span class="line"></span><br><span class="line">&#x2F;&#x2F; 其它</span><br><span class="line">GC forced: 如果两分钟内没有执行GC，则会强制执行一次GC，此时会换行打印 GC forced</span><br></pre></td></tr></table></figure><h4 id="4-MemStats"><a href="#4-MemStats" class="headerlink" title="4. MemStats"></a>4. MemStats</h4><p><a href="https://github.com/golang/go/blob/dev.boringcrypto.go1.13/src/runtime/mstats.go#L147">runtime.MemStats</a>记录了内存分配的一些统计信息，通过<code>runtime.ReadMemStats(&amp;ms)</code>获取，它是<a href="https://github.com/golang/go/blob/dev.boringcrypto.go1.13/src/runtime/mstats.go#L24">runtime.mstats</a>的对外版(再次可见Go单一访问控制的弊端)，MemStats字段比较多，其中比较重要的有:</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// HeapSys </span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 以下内存大小字段如无特殊说明单位均为bytes</span></span><br><span class="line"><span class="keyword">type</span> MemStats <span class="keyword">struct</span> &#123;</span><br><span class="line">    <span class="comment">// 从开始运行到现在累计分配的堆内存数</span></span><br><span class="line">    TotalAlloc <span class="keyword">uint64</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 从OS申请的总内存数(包含堆、栈、内部数据结构等)</span></span><br><span class="line">    Sys <span class="keyword">uint64</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 累计分配的堆对象数量 (当前存活的堆对象数量=Mallocs-Frees)</span></span><br><span class="line">    Mallocs <span class="keyword">uint64</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 累计释放的堆对象数量</span></span><br><span class="line">    Frees   <span class="keyword">uint64</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 正在使用的堆内存数，包含可访问对象和暂未被GC回收的不可访问对象</span></span><br><span class="line">    HeapAlloc <span class="keyword">uint64</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 虚拟内存空间为堆保留的大小，包含还没被使用的(还没有映射物理内存，但这部分通常很小)</span></span><br><span class="line">    <span class="comment">// 以及已经将物理内存归还给OS的部分(即HeapReleased)</span></span><br><span class="line">    <span class="comment">// HeapSys = HeapInuse + HeapIdle</span></span><br><span class="line">    HeapSys <span class="keyword">uint64</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 至少包含一个对象的span字节数</span></span><br><span class="line">    <span class="comment">// Go GC是不会整理内存的</span></span><br><span class="line">    <span class="comment">// HeapInuse - HeapAlloc 是为特殊大小保留的内存，但是它们还没有被使用</span></span><br><span class="line">    HeapInuse <span class="keyword">uint64</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 未被使用的span中的字节数</span></span><br><span class="line">    <span class="comment">// 未被使用的span指没有包含任何对象的span，它们可以归还OS，也可以被重用，或者被用于栈内存</span></span><br><span class="line">    <span class="comment">// HeapIdle - HeadReleased 即为可以归还OS但还被保留的内存，这主要用于避免频繁向OS申请内存</span></span><br><span class="line">    HeapIdle <span class="keyword">uint64</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">// HeapIdle中已经归还给OS的内存量</span></span><br><span class="line">    HeapReleased <span class="keyword">uint64</span></span><br><span class="line"> </span><br><span class="line">    <span class="comment">// ....</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>程序可以通过定期调用<code>runtime.ReadMemStats</code>API来获取内存分配信息发往时序数据库进行监控。另外，该API是会STW的，但是很短，Google内部也在用，用他们的话说:”STW不可怕，长时间STW才可怕”，该API通常一分钟调用一次即可。</p><h4 id="5-ReadGCStats"><a href="#5-ReadGCStats" class="headerlink" title="5. ReadGCStats"></a>5. ReadGCStats</h4><p><code>debug.ReadGCStats</code>用于获取最近的GC统计信息，主要是GC造成的延迟信息:</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// GCStats collect information about recent garbage collections.</span></span><br><span class="line"><span class="keyword">type</span> GCStats <span class="keyword">struct</span> &#123;</span><br><span class="line">LastGC         time.Time       <span class="comment">// 最近一次GC耗费时间</span></span><br><span class="line">NumGC          <span class="keyword">int64</span>           <span class="comment">// 执行GC的次数</span></span><br><span class="line">PauseTotal     time.Duration   <span class="comment">// 所有GC暂停时间总和</span></span><br><span class="line">Pause          []time.Duration <span class="comment">// 每次GC的暂停时间，最近的排在前面</span></span><br><span class="line">...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>和ReadMemStats一样，ReadGCStats也可以定时收集，发送给时序数据库做监控统计。</p><h3 id="三-Go-GC-调优"><a href="#三-Go-GC-调优" class="headerlink" title="三. Go GC 调优"></a>三. Go GC 调优</h3><p>Go GC相关的参数少得可怜，一如既往地精简:</p><h4 id="1-debug-SetGCPercent"><a href="#1-debug-SetGCPercent" class="headerlink" title="1. debug.SetGCPercent"></a>1. debug.SetGCPercent</h4><p>一个百分比数值，决定即本次GC后，下次触发GC的阈值，比如本次GC Sweeping完成后的内存占用为200M，GC Percentage为100(默认值)，那么下次触发GC的内存阈值就是400M。这个值通常不建议修改，因为优化GC开销的方法通常是避免不必要的分配或者内存复用，而非通过调整GC Percent延迟GC触发时机(Go GC本身也会根据当前分配速率来决定是否需要提前开启新一轮GC)。另外，debug.SetGCPercent传入&lt;0的值将关闭GC。</p><h4 id="2-runtime-GC"><a href="#2-runtime-GC" class="headerlink" title="2. runtime.GC"></a>2. runtime.GC</h4><p>强制执行一次GC，如果当前正在执行GC，则帮助当前GC执行完成后，再执行一轮完整的GC。该函数阻塞直到GC完成。</p><h4 id="3-debug-FreeOSMemory"><a href="#3-debug-FreeOSMemory" class="headerlink" title="3. debug.FreeOSMemory"></a>3. debug.FreeOSMemory</h4><p>强制执行一次GC，并且尽可能多地将不再使用的内存归还给OS。</p><p>严格意义上说，以上几个API预期说调优，不如说是补救，它们都只是把Go GC本身就会做的事情提前或者延后了，通常是治标不治本的方法。真正的GC调优主要还是在应用层面。我在<a href="https://wudaijun.com/2019/09/go-performance-optimization/">这篇文章</a>聊了一些Go应用层面的内存优化。</p><p>以上主要从偏应用的角度介绍了Golang GC的几个重要阶段，STW，GC度量/调试，以及相关API等。这些理论和方法能在在必要的时候派上用场，帮助更深入地了解应用程序并定位问题。</p><p>推荐文献:</p><ol><li><a href="https://www.ardanlabs.com/blog/2018/12/garbage-collection-in-go-part1-semantics.html">Garbage Collection In Go</a></li><li><a href="https://github.com/qcrao/Go-Questions/blob/master/GC/GC.md">GC 20 问</a></li><li><a href="https://blog.learngoprogramming.com/a-visual-guide-to-golang-memory-allocator-from-ground-up-e132258453ed">A visual guide to Go Memory Allocator from scratch</a></li></ol>]]></content>
    
    
    <summary type="html">&lt;h3 id=&quot;一-Go-GC-要点&quot;&gt;&lt;a href=&quot;#一-Go-GC-要点&quot; class=&quot;headerlink&quot; title=&quot;一. Go GC 要点&quot;&gt;&lt;/a&gt;一. Go GC 要点&lt;/h3&gt;&lt;p&gt;先来回顾一下GC的几个重要的阶段:&lt;/p&gt;
&lt;h4 id=&quot;Mark-Prepare-STW&quot;&gt;&lt;a href=&quot;#Mark-Prepare-STW&quot; class=&quot;headerlink&quot; title=&quot;Mark Prepare - STW&quot;&gt;&lt;/a&gt;Mark Prepare - STW&lt;/h4&gt;&lt;p&gt;做标记阶段的准备工作，需要停止所有正在运行的goroutine(即STW)，标记根对象，启用内存屏障，内存屏障有点像内存读写钩子，它用于在后续并发标记的过程中，维护三色标记的完备性(三色不变性)，这个过程通常很快，大概在10-30微秒。&lt;/p&gt;
&lt;h4 id=&quot;Marking-Concurrent&quot;&gt;&lt;a href=&quot;#Marking-Concurrent&quot; class=&quot;headerlink&quot; title=&quot;Marking - Concurrent&quot;&gt;&lt;/a&gt;Marking - Concurrent&lt;/h4&gt;&lt;p&gt;标记阶段会将大概25%(gcBackgroundUtilization)的P用于标记对象，逐个扫描所有G的堆栈，执行三色标记，在这个过程中，所有新分配的对象都是黑色，被扫描的G会被暂停，扫描完成后恢复，这部分工作叫后台标记(&lt;a href=&quot;https://github.com/golang/go/blob/dev.boringcrypto.go1.13/src/runtime/mgc.go#L1817&quot;&gt;gcBgMarkWorker&lt;/a&gt;)。这会降低系统大概25%的吞吐量，比如&lt;code&gt;MAXPROCS=6&lt;/code&gt;，那么GC P期望使用率为&lt;code&gt;6*0.25=1.5&lt;/code&gt;，这150%P会通过专职(Dedicated)/兼职(Fractional)/懒散(Idle)三种工作模式的Worker共同来完成。&lt;/p&gt;</summary>
    
    
    
    <category term="golang" scheme="http://wudaijun.com/categories/golang/"/>
    
    
    <category term="gc" scheme="http://wudaijun.com/tags/gc/"/>
    
    <category term="golang" scheme="http://wudaijun.com/tags/golang/"/>
    
  </entry>
  
  <entry>
    <title>聊聊Go内存优化和相关底层机制</title>
    <link href="http://wudaijun.com/2019/09/go-performance-optimization/"/>
    <id>http://wudaijun.com/2019/09/go-performance-optimization/</id>
    <published>2019-09-13T16:00:00.000Z</published>
    <updated>2019-09-25T09:36:20.000Z</updated>
    
    <content type="html"><![CDATA[<p>最近做的优化比较多，整理下和Go内存相关的一些东西。</p><h2 id="一-不要过早优化"><a href="#一-不要过早优化" class="headerlink" title="一. 不要过早优化"></a>一. 不要过早优化</h2><p>虽是老生常谈，但确实需要在做性能优化的时候铭记在心，说说我的理解:</p><ol><li>first make it work, then measure, then optimize</li><li>二八原则</li><li>需求变更快</li><li>对性能的主观直觉不靠谱</li></ol><a id="more"></a><h2 id="二-内存优化"><a href="#二-内存优化" class="headerlink" title="二. 内存优化"></a>二. 内存优化</h2><p>Golang运行时的内存分配算法主要源自 Google 为 C 语言开发的TCMalloc算法，全称Thread-Caching Malloc。核心思路是层级管理，以降低锁竞争开销。Golang内存管理大概分为三层，每个线程(GPM中的P)都会自行维护一个独立的内存池(mcache)，进行内存分配时优先从mcache中分配(无锁)，当mcache内存不足时才会向全局内存池(mcentral)申请(有锁)，当mcentral内存不足时再向全局堆空间管理(mheap)中申请(有锁+按照固定大小切割)，最后mheap如果不足，则向OS申请(SysCall)。mcache -&gt; mcentral -&gt; mheap -&gt; OS 代价逐层递增，Golang运行时的很多地方都有这种层级管理的思路，比如GPM调度模型中对G的分配，这种层级在并发运行时下，通常有比较好的性能表现。</p><p>以下讨论下内存优化(主要是优化分配和GC开销，而非内存占用大小)常用的手段。</p><h3 id="1-内存复用"><a href="#1-内存复用" class="headerlink" title="1. 内存复用"></a>1. 内存复用</h3><p>关于内存复用最常见的手段就是内存池了，它缓存已分配但不再使用的对象，在下次分配时进行复用，以避免频繁的对象分配。</p><h4 id="1-1-sync-Pool"><a href="#1-1-sync-Pool" class="headerlink" title="1.1 sync.Pool"></a>1.1 sync.Pool</h4><p>Go的<code>sync.Pool</code>包是Go官方提供的内存池，在使用sync.Pool时，需要注意:</p><ol><li>sync.Pool是goroutine safe的，也就是会用mutex</li><li>sync.Pool无法设置大小，所以理论上只受限于系统内存大小</li><li>sync.Pool中的对象不支持自定义过期时间及策略</li><li>sync.Pool中的对象会在GC开始前全部清除，这样可以让Pool大小随应用峰值而自动收缩扩张，更有效地利用内存。在go1.13中有优化，会留一部分Object，相当于延缓了收缩扩张的速度</li></ol><p>sync.Pool适用于跨goroutine且需要动态伸缩的场景，典型的如网络层或日志层，每个连接(goroutine)都需要Pool，并且连接数是不稳定的。</p><h4 id="1-2-leakybuf"><a href="#1-2-leakybuf" class="headerlink" title="1.2 leakybuf"></a>1.2 leakybuf</h4><p>有时为了达成更轻量，更可控的复用，我们可能会根据应用场景自己造轮子，比如实现一个固定大小，不会被GC的内存池。比如shadowsocks-go的<a href="https://github.com/shadowsocks/shadowsocks-go/blob/master/shadowsocks/leakybuf.go">LeakyBuf</a>就用channel巧妙实现了个[]byte Pool。</p><p>leakybuf这类Pool相较于sync.Pool的主要优势是不会被GC，但缺点是少了收缩性，设置大了浪费内存，设置小了复用作用不明显。因此它适用于能够提前预估池子大小的场景，在实践中，我们将其用在DB序列化层，其Worker数量固定，单个[]byte较大，也相对稳定。</p><h4 id="1-3-逻辑对象复用"><a href="#1-3-逻辑对象复用" class="headerlink" title="1.3 逻辑对象复用"></a>1.3 逻辑对象复用</h4><p>复用的粒度不仅限于简单struct或slice，也可以是逻辑实体。比如我们游戏中每次生成地图NPC时，会根据配置初始化大量的属性和BUFF，涉及到很多小对象的分配，这里我们选择将整个NPC作为复用粒度，在NPC倒计时结束或被击败消失时，将NPC整理缓存在池子中，并重置其战斗状态和刷新时间。这种逻辑实体的复用不通用但往往有用，必要的时候可以派上用场。</p><h4 id="1-4-原地复用"><a href="#1-4-原地复用" class="headerlink" title="1.4. 原地复用"></a>1.4. 原地复用</h4><p>除了内存池这种”有借有还”的复用外，另一种常用的内存复用思路是就地复用，它主要用于切片这类容易重置的数据结构，比如下面是一个过滤切片中所有奇数的操作:</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">s := []<span class="keyword">int</span>&#123;<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span>&#125;</span><br><span class="line">ret := s[:<span class="number">0</span>]</span><br><span class="line"><span class="keyword">for</span> i:=<span class="number">0</span>; i&lt;<span class="built_in">len</span>(s); i++ &#123;</span><br><span class="line">    <span class="keyword">if</span> s[i] &amp; <span class="number">1</span> == <span class="number">1</span>&#123;</span><br><span class="line">        ret = <span class="built_in">append</span>(ret, s[i])</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>另一个”黑科技”操作是[]byte to string:</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">bs := <span class="built_in">make</span>([]<span class="keyword">byte</span>, <span class="number">1000</span>)</span><br><span class="line"><span class="comment">// 175 ns/op 底层会拷贝bs，保证string的字符常量语义</span></span><br><span class="line">ss2 = <span class="keyword">string</span>(bs)</span><br><span class="line"><span class="comment">// 2.48 ns/op 直接地址转换，慎用，破坏了string的字符常量语义，更改bs将影响到ss1 ！</span></span><br><span class="line">ss1 = *(*<span class="keyword">string</span>)(unsafe.Pointer(&amp;bs))</span><br></pre></td></tr></table></figure><p>切片的这类技巧经常用在网络层和算法层，有时候也能起到不错的效果。</p><h3 id="2-预分配"><a href="#2-预分配" class="headerlink" title="2. 预分配"></a>2. 预分配</h3><p>预分配主要针对map, slice这类数据结构，当你知道它要分配多大内存时，就提前分配，一是为了避免多次内存分配，二是为了减少space grow带来的数据迁移(map evacuate or slice copy)开销。</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line">n := <span class="number">10</span></span><br><span class="line">src := <span class="built_in">make</span>([]<span class="keyword">int</span>, n)</span><br><span class="line"></span><br><span class="line"><span class="comment">// 无预分配: 162 ns/op</span></span><br><span class="line"><span class="keyword">var</span> dst []<span class="keyword">int</span></span><br><span class="line"><span class="keyword">for</span> i:=<span class="number">0</span>; i&lt;n; i++ &#123;</span><br><span class="line">dst = <span class="built_in">append</span>(dst, src[i])</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 预分配，32.3 ns/op，提升了5倍</span></span><br><span class="line">dst2 := <span class="built_in">make</span>([]<span class="keyword">int</span>, <span class="number">0</span>, n)</span><br><span class="line"><span class="keyword">for</span> i:=<span class="number">0</span>; i&lt;n; i++ &#123;</span><br><span class="line">dst2 = <span class="built_in">append</span>(dst2, src[i])</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 预分配+append...: 30.1 ns/op</span></span><br><span class="line">dst2 = <span class="built_in">append</span>(dst2, src...)</span><br><span class="line"></span><br><span class="line"><span class="comment">// 预分配+copy: 26.0 ns/op</span></span><br><span class="line"><span class="built_in">copy</span>(dst2, src)</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>可以看到，slice预分配对性能的提升是非常大的，这里为了简单起见，以纯粹的拷贝切片为例，而对于切片拷贝，应该使用go专门为优化slice拷贝提供的内置函数copy，如果抛开分配dst2的开销，<code>copy(dst2, src)</code>的运算速度达到0.311ns，是<code>append(dst, src...)</code>的20倍左右！</p><p>平时编码中养成预分配的习惯是有利而无害的，Go的一些代码分析工具如<a href="https://github.com/alexkohler/prealloc">prealloc</a>可以帮助你检查可做的预分配优化。</p><p>有时候预分配的大小不一定是精确的，也可能模糊的，比如要将一个数组中所有的偶数选出来，那么可以预分配1/2的容量，在不是特别好估算大小的情况下，尽可能保守分配。</p><p>预分配的另一个思路就是对于一些频繁生成的大对象，比如我们逻辑中打包地图实体，这是个很大的pb协议，pb默认生成的内嵌消息字段全是指针，给指针赋值的过程中为了保证深拷贝语义需要频繁地分配这些各级字段的内存，为了优化分配内存次数，我们使用<a href="https://github.com/gogo/protobuf">gogoproto</a>的nullable生成器选项来对这类消息生成嵌套的值字段而非指针字段，这样可以减少内存分配次数(但分配的数量是一样的)。</p><h3 id="3-避免不必要的分配"><a href="#3-避免不必要的分配" class="headerlink" title="3. 避免不必要的分配"></a>3. 避免不必要的分配</h3><p>Go的逃逸分析+GC会让Gopher对指针很青睐，receiver，struct field, arguments, return value等，却容易忽略背后的开销(当然，大部分时候开发者确实不需要操心)。</p><h4 id="3-1-减少返回值逃逸"><a href="#3-1-减少返回值逃逸" class="headerlink" title="3.1 减少返回值逃逸"></a>3.1 减少返回值逃逸</h4><p>为了避免引用必要的时候也可以化切片为数组:</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 创建并返回了一个切片，切片是引用语义，导致ret逃逸</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">GetNineGrid1</span><span class="params">()</span> []<span class="title">int</span></span> &#123;</span><br><span class="line">ret := <span class="built_in">make</span>([]<span class="keyword">int</span>, <span class="number">9</span>)</span><br><span class="line"><span class="keyword">return</span> ret</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 创建了一个数组但返回了它的切片(相当于它的引用)，导致数组逃逸</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">GetNineGrid2</span><span class="params">()</span> []<span class="title">int</span></span> &#123;</span><br><span class="line"><span class="keyword">var</span> ret [<span class="number">9</span>]<span class="keyword">int</span></span><br><span class="line"><span class="keyword">return</span> ret[:]</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 创建并返回数组，数组是值语义，因此不会逃逸</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">GetNineGrid3</span><span class="params">()</span> [9]<span class="title">int</span></span> &#123;</span><br><span class="line"><span class="keyword">return</span> [<span class="number">9</span>]<span class="keyword">int</span>&#123;&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这还不够，有时候还需要对计算流程进行优化:</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">type</span> Coord <span class="keyword">struct</span>&#123;</span><br><span class="line">X   <span class="keyword">int32</span></span><br><span class="line">Z   <span class="keyword">int32</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">f1</span><span class="params">(c *Coord)</span> *<span class="title">Coord</span></span> &#123;</span><br><span class="line">ret := Coord&#123;</span><br><span class="line">X: c.X/<span class="number">2</span>,</span><br><span class="line">Z: c.Z/<span class="number">2</span>,</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">return</span> &amp;ret</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">f2</span><span class="params">()</span> <span class="title">int32</span></span> &#123;</span><br><span class="line">c := &amp;Coord&#123;</span><br><span class="line">X: <span class="number">2</span>,</span><br><span class="line">Z: <span class="number">4</span>,</span><br><span class="line">&#125;</span><br><span class="line">c2 := f1(c)</span><br><span class="line"><span class="keyword">return</span> c2.X</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 优化后</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">new_f1</span><span class="params">(c *Coord, ret *Coord)</span></span> &#123;</span><br><span class="line">ret.X = c.X/<span class="number">2</span>,</span><br><span class="line">ret.Z = c.Z/<span class="number">2</span>,</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">new_f2</span><span class="params">()</span> <span class="title">int32</span></span> &#123;</span><br><span class="line">    c := &amp;Coord&#123;</span><br><span class="line">        X:  <span class="number">2</span>,</span><br><span class="line">        Z:  <span class="number">4</span>,</span><br><span class="line">    &#125;</span><br><span class="line">    ret := &amp;Coord&#123;&#125;</span><br><span class="line">    new_f1(c, ret)</span><br><span class="line">    <span class="keyword">return</span> ret.X</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在上面的代码中，Go编译器会分析到f1的变量ret地址会返回，因此它不能分配在栈在(调用完成后，栈就回收了)，必须分配在堆上，这就是逃逸分析(escape analyze)。而对于f2中的变量c来说，虽然函数中用了c的地址，但都是用于其调用的子函数(此时f1的栈还有效)，并未返回或传到函数栈有效域外，因此f2中的c会分配到栈上。</p><p>在默认编译参数下，f1的ret并不会逃逸，这是因为f1会被内联的，f1的调用并不会有新的函数栈的扩展和收缩，都会在f2的函数栈上进行，由于f2中的变量都没有逃逸到f2之外，因此对f2的调用也不会有任何内存分配，可以通过<code>-gcflags -N -l</code>编译选项来禁用内联，并通过<code>-gcflags -m</code>打印逃逸分析信息。但内联也是有条件的(函数足够简单，不涉及Interface)，我们将在后面再聊到内联。</p><h4 id="3-2-化指针为值"><a href="#3-2-化指针为值" class="headerlink" title="3.2 化指针为值"></a>3.2 化指针为值</h4><p>可以将使用频繁并且简单的结构体比如前面的地图坐标Coord，使用值而不是指针，这样可以减少不必要的变量逃逸带来的GC开销。</p><h4 id="3-3-字符串操作优化"><a href="#3-3-字符串操作优化" class="headerlink" title="3.3 字符串操作优化"></a>3.3 字符串操作优化</h4><p>以下是一个简单的测试，并注有其benchmark性能数据:</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 1834 ns/op     880 B/op      29 allocs/op</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">stringSprintf</span><span class="params">()</span> <span class="title">string</span></span> &#123;</span><br><span class="line"><span class="keyword">var</span> s <span class="keyword">string</span></span><br><span class="line">v := <span class="string">&quot;benmark&quot;</span></span><br><span class="line"><span class="keyword">for</span> i := <span class="number">0</span>; i &lt; <span class="number">10</span>; i++ &#123;</span><br><span class="line">s = fmt.Sprintf(<span class="string">&quot;%s[%s]&quot;</span>, s, v)</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">return</span> s</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 616 ns/op     576 B/op      10 allocs/op</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">stringPlus</span><span class="params">()</span> <span class="title">string</span></span> &#123;</span><br><span class="line"><span class="keyword">var</span> s <span class="keyword">string</span></span><br><span class="line">v := <span class="string">&quot;benmark&quot;</span></span><br><span class="line"><span class="keyword">for</span> i := <span class="number">0</span>; i &lt; <span class="number">10</span>; i++ &#123;</span><br><span class="line">s = s + <span class="string">&quot;[&quot;</span> + v + <span class="string">&quot;]&quot;</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">return</span> s</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 395 ns/op     304 B/op       3 allocs/op</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">stringBuffer</span><span class="params">()</span> <span class="title">string</span></span> &#123;</span><br><span class="line"><span class="keyword">var</span> buffer bytes.Buffer</span><br><span class="line">v := <span class="string">&quot;benmark&quot;</span></span><br><span class="line"><span class="keyword">for</span> i := <span class="number">0</span>; i &lt; <span class="number">10</span>; i++ &#123;</span><br><span class="line">buffer.WriteString(<span class="string">&quot;[&quot;</span>)</span><br><span class="line">buffer.WriteString(v)</span><br><span class="line">buffer.WriteString(<span class="string">&quot;]&quot;</span>)</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">return</span> buffer.String()</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 218 ns/op     248 B/op       5 allocs/op</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">stringBuilder</span><span class="params">()</span> <span class="title">string</span></span> &#123;</span><br><span class="line"><span class="keyword">var</span> builder strings.Builder</span><br><span class="line">v := <span class="string">&quot;benmark&quot;</span></span><br><span class="line"><span class="keyword">for</span> i := <span class="number">0</span>; i &lt; <span class="number">10</span>; i++ &#123;</span><br><span class="line">builder.WriteString(<span class="string">&quot;[&quot;</span>)</span><br><span class="line">builder.WriteString(v)</span><br><span class="line">builder.WriteString(<span class="string">&quot;]&quot;</span>)</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">return</span> builder.String()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>导致内存分配差异如此大的主要原因是string是常量语义，每次在构造新的string时，都会将之前string的底层[]byte拷贝一次，在执行多次string拼接时，<code>strings.Builder</code>和<code>bytes.Buffer</code>会缓存中间生成的[]byte，直到真正需要string结果时再调用它们的<code>String()</code>返回，避免了生成不必要的string中间结果，因此在多次拼接字符串的场景，<code>strings.Builder</code>和<code>bytes.Buffer</code>是更好的选择。</p><p>简单总结下:</p><ul><li><code>fmt.Sprintf</code>: 适用于将其它类型格式化为字符串，灵活性高</li><li><code>+</code>: 适用于少量的的常量字符串拼接，易读性高</li><li><code>bytes.Buffer</code>: 有比slice默认更激进的cap分配，它的<code>String()</code>方法需要拷贝。适用于大量的字符串的二进制拼接，如网络层。</li><li><code>strings.Builder</code>: 底层使用slice默认cap分配，主要的优点是调用<code>String()</code>不需要拷贝(直接地址转换)，适用于要求输出结果是string的地方</li></ul><h2 id="三-逃逸和内联"><a href="#三-逃逸和内联" class="headerlink" title="三 逃逸和内联"></a>三 逃逸和内联</h2><h3 id="1-逃逸分析"><a href="#1-逃逸分析" class="headerlink" title="1. 逃逸分析"></a>1. 逃逸分析</h3><p>前面简单提了下逃逸分析，这里我们再深入讨论下，逃逸分析虽然好用，却并不免费，只有理解其内部机制，才能将收益最大化(开发效率vs运行效率)。逃逸分析的本质是当编译器发现函数变量将脱离函数栈有效域或被函数栈有效域外的变量所引用时时，将变量分配在堆上而不是栈在，典型的情况有:</p><ol><li>函数返回变量地址，或返回包含变量地址的struct，刚才已经讨论过</li><li>将变量地址写入channel或sync.Pool，编译器无法获悉其它goroutine如何使用这个变量，也就无法在编译时决议变量的生命周期</li><li>闭包也可能导致闭包上下文逃逸</li><li>slice变量超过cap重新分配时，将在堆上进行，栈的大小毕竟是固定和有限的</li><li>将变量地址赋给可扩容容器(如map,slice)时</li><li>将变量赋给可扩容Interface容器(k或v为Interface的map，或[]Interface)时</li><li>涉及到Interface的地方都有可能导致对象逃逸，<code>MyInterface(x).Foo(a)</code>将会导致x逃逸，如果a是引用语义(指针,slice,map等)，a也会分配到堆上，涉及到Interface的很多逃逸优化都很保守，比如<code>reflect.ValueOf(x)</code>会显式调用<code>escapes(x)</code>导致x逃逸。<br>第4点和第5点单独说下，以slice和空接口为例:</li></ol><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">example</span><span class="params">()</span></span> &#123;</span><br><span class="line">    s1 := <span class="built_in">make</span>([]<span class="keyword">int</span>, <span class="number">10</span>)</span><br><span class="line">    s2 := <span class="built_in">make</span>([]*<span class="keyword">int</span>, <span class="number">10</span>)</span><br><span class="line">    s3 := <span class="built_in">make</span>([]<span class="keyword">interface</span>&#123;&#125;, <span class="number">10</span>)</span><br><span class="line">    a := <span class="number">123</span></span><br><span class="line">    s1[<span class="number">1</span>] = a <span class="comment">// case1: 导致a分配在栈在</span></span><br><span class="line">    s2[<span class="number">1</span>] = &amp;a <span class="comment">// case2: 导致a分配到堆上</span></span><br><span class="line">    s3[<span class="number">1</span>] = a   <span class="comment">// case3: 导致a分配在堆上</span></span><br><span class="line">    s3[<span class="number">1</span>] = &amp;a<span class="comment">// case4: 导致a分配在堆上</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>首先我们知道slice重分配是在堆上，slice重分配时，会发生数据迁移，此时要将原本slice len内的元素<em>浅拷贝</em>到新的空间，而这个浅拷贝会导致新的slice(堆内存)引用了p(栈内存)的内容，而栈内存和堆内存的生命周期是不一样的，可能出现函数返回后，堆内存引用无效的栈内存的情况，这会影响到运行时的稳定性。因此即使slice变量本身没有显式逃逸，但由于隐式的数据迁移，编译器会保守地将slice或map的指针elem逃逸到堆上。这就是第4点的原因，也解释了上面代码中的case1 case2 case4，现在来看看case3。</p><p>简单来说，interface{}让值语义变为引用语义，interface{}本质上为typ + pointer，这个pointer指向实际的data，参考我之前写的<a href="https://wudaijun.com/2018/01/go-interface-implement/">go interface实现</a>。<code>s3[i] = a</code>实际上让s3 slice持有了a的引用，因此a会逃逸到堆上分配。</p><p>我们逻辑中调用的<code>fmt.Sprintf</code>或<code>logrus.Debugf</code>都会导致所有传入参数逃逸，因为不定参数实际上是slice的语法糖，编译器无法确定<code>logrus.Debugf</code>不会对参数slice进行append操作导致重分配，只能保守地将传入的参数分配到堆上以保证浅拷贝是正确的。我认为用保守来形容Go的逃逸分析策略是比较合适的，比如前面代码的s1,s2,s3，既slice变量本身没有逃逸，也没有发生扩容，那么让slice以及其元素都在栈上应该是安全的，目前不理解Go编译器出于何种考虑没有做这种优化。当然，好的逃逸分析需要在编译期更深入地理解程序，这本身就是非常困难的，特别是当涉及到interface{}，指针，可扩展容器的时候。</p><h3 id="2-内联"><a href="#2-内联" class="headerlink" title="2. 内联"></a>2. 内联</h3><p>前面说过，逃逸分析+GC好用但不免费，但如果没有内联这个最佳辅助的话，前两者的代价怕是昂贵得用不起，所有函数返回的地方，都会树立起一道”墙”，任何想要从墙内逃逸到墙外的变量都会被分配在堆上，如:</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">NewCoord</span><span class="params">()</span> *<span class="title">Coord</span></span> &#123;</span><br><span class="line"><span class="keyword">return</span> &amp;Coord&#123;</span><br><span class="line">X: <span class="number">1</span>,</span><br><span class="line">Z: <span class="number">2</span>,</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">foo</span><span class="params">()</span></span> &#123;</span><br><span class="line">c := NewCoord()</span><br><span class="line"><span class="keyword">return</span> c.x</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>NewCoord</code>这类简单的构造函数都会导致返回值分配在堆上，抽离函数的代价也更大。因此Go的内联，逃逸分析，GC是名副其实的三剑客，它们共同将其它语言避之不及的指针变得”物美价廉”。</p><p>Go1.9开始对内联做了比较大的运行时优化，开始支持<a href="https://go.googlesource.com/proposal/+/master/design/19348-midstack-inlining.md">mid-stack inline</a>，talk链接在<a href="https://docs.google.com/presentation/d/1Wcblp3jpfeKwA0Y4FOmj63PW52M_qmNqlQkNaLj0P5o/edit#slide=id.g1d00ad65a7_2_17">这里</a>。并且支持通过<code>-l</code>编译参数指定内联等级(参数定义参考<a href="https://github.com/golang/go/blob/71a6a44428feb844b9dd3c4c8e16be8dee2fd8fa/src/cmd/compile/internal/gc/inl.go#L10-L17">cmd/compile/internal/gc/inl.go</a>)。并且只在<code>-l=4</code>中提供了mid-stack inline，据Go官方统计，这大概可以提升9%的性能，也增加了11%左右的二进制大小。</p><p>Go1.10做一些Interface相关的优化，如<a href="https://github.com/golang/go/issues/19361">devirtualization</a>，编译器在能够知晓Interface具体对象的情况下(如<code>var i Iface = &amp;myStruct&#123;&#125;</code>)，可以直接生成对象相关代码调用(还不是内联)，而无需走Interface方法查找。目前devirtualization优化还不完善，还不能应用于逃逸分析优化(<a href="https://github.com/golang/go/issues/33160#issuecomment-512653356">这里</a>有讨论)。</p><p>Go1.12开始默认支持了mid-stack inline。</p><p>PS: 关注和更新Go最新版本可能是最”廉价”的优化手段。如GC，编译器优化，defer等特性都还在不断改进，毕竟Go还很年轻</p><p>我们目前还没有调整过内联参数，因为这是有利有弊的，过于激进的内联会导致生成的二进制文件更大，CPU instruction cache miss也可能会增加。默认等级的内联大部分时候都工作得很好并且稳定，到目前(Go1.13)为止，对Interface方法的调用还不能被内联(即使编译器知晓具体类型):</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">type</span> I <span class="keyword">interface</span> &#123;</span><br><span class="line">F() <span class="keyword">int</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">type</span> A <span class="keyword">struct</span>&#123;</span><br><span class="line">x <span class="keyword">int</span></span><br><span class="line">y <span class="keyword">int</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(a *A)</span> <span class="title">F</span><span class="params">()</span> <span class="title">int</span></span> &#123;</span><br><span class="line">z := a.x + a.y</span><br><span class="line"><span class="keyword">return</span> z</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">BenchmarkX</span><span class="params">(b *testing.B)</span></span> &#123;</span><br><span class="line">b.ReportAllocs()</span><br><span class="line"><span class="keyword">for</span> i:=<span class="number">0</span>; i&lt;b.N; i++ &#123;</span><br><span class="line"><span class="comment">// F() 会被内联 0.36 ns/op</span></span><br><span class="line"><span class="comment">// var a = &amp;A&#123;&#125;</span></span><br><span class="line"><span class="comment">// a.F()</span></span><br><span class="line"><span class="comment">// 对Interface的方法调用不能被内联 18.4 ns/op</span></span><br><span class="line"><span class="keyword">var</span> i I = &amp;A&#123;&#125;</span><br><span class="line">i.F()</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>对于一些底层基础的结构体，比如我们地图上的实体基础信息Entity，包含ID，坐标，碰撞半径等最基础的信息，我们为其抽象了一个接口IEntity，它只提供简单的对字段的访问和设置，最近再考虑将IEntity去掉，直接用Struct，借助于内联，字段访问会快一个数量级。</p><p>另外，针对目前Go Interface 内联做得不够好的情况，一个比较好的实践是，让你的公用API返回具体类型而非Interface，如<code>etcdclient.New</code>，<code>grpc.NewServer</code>等都是如此实践的，它们通过私有字段加公开方法让外部用起来像Interface一样，但数据逻辑层可能实践起来会有一些难度，因为Go的访问控制太弱了…</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;最近做的优化比较多，整理下和Go内存相关的一些东西。&lt;/p&gt;
&lt;h2 id=&quot;一-不要过早优化&quot;&gt;&lt;a href=&quot;#一-不要过早优化&quot; class=&quot;headerlink&quot; title=&quot;一. 不要过早优化&quot;&gt;&lt;/a&gt;一. 不要过早优化&lt;/h2&gt;&lt;p&gt;虽是老生常谈，但确实需要在做性能优化的时候铭记在心，说说我的理解:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;first make it work, then measure, then optimize&lt;/li&gt;
&lt;li&gt;二八原则&lt;/li&gt;
&lt;li&gt;需求变更快&lt;/li&gt;
&lt;li&gt;对性能的主观直觉不靠谱&lt;/li&gt;
&lt;/ol&gt;</summary>
    
    
    
    <category term="go" scheme="http://wudaijun.com/categories/go/"/>
    
    
    <category term="go" scheme="http://wudaijun.com/tags/go/"/>
    
  </entry>
  
  <entry>
    <title>如何给GS做压测</title>
    <link href="http://wudaijun.com/2019/09/gs-pressure-test/"/>
    <id>http://wudaijun.com/2019/09/gs-pressure-test/</id>
    <published>2019-09-12T16:00:00.000Z</published>
    <updated>2020-08-14T08:52:22.953Z</updated>
    
    <content type="html"><![CDATA[<p>简单谈谈我们最近是如何给GS做压测的。</p><h3 id="1-压测机器人"><a href="#1-压测机器人" class="headerlink" title="1. 压测机器人"></a>1. 压测机器人</h3><p>压测机器人需要满足如下几个条件:</p><ol><li>异步请求: 异步才能模拟真实的客户端请求和压力</li><li>数据同步: 像客户端一样缓存和处理服务器响应数据，这样才能做好有效请求和可重入</li><li>可重入: 机器人应该可以在任何时候关闭/重启，而不应该假设初始状态(比如只有注册的时候能跑)</li><li>随机性: 机器人行为尽可能随机分布，并且每次重启重新初始化随机种子</li></ol><a id="more"></a><h3 id="2-压测用例"><a href="#2-压测用例" class="headerlink" title="2. 压测用例"></a>2. 压测用例</h3><p>压测用例可以从这几个方面来考虑:</p><ol><li>服务器比较耗时的API: 如寻路，战斗等</li><li>玩家越多越耗时的逻辑: 如视野同步，消息广播等</li><li>玩家日常操作频繁的行为: 如城建升级，联盟加入退出，以尽可能覆盖如任务，BUFF，排行榜等支撑系统</li></ol><h3 id="3-压测统计"><a href="#3-压测统计" class="headerlink" title="3. 压测统计"></a>3. 压测统计</h3><p>在做压测中，我们会从如下几个方面来获取性能指标:</p><ol><li>函数级分析: go prof简单易用，参考<a href="https://wudaijun.com/2018/04/go-pprof/">go pprof性能分析</a></li><li>消息级统计: 统计每个逻辑Actor(如地图，玩家)对单次请求的处理时间(最大/平均/次数)，消息是Actor之间交互的最小单位，每个消息处理过程是一条函数调用链</li><li>服务器请求统计: 统计每条客户端请求从网关层收到请求到网关层发出响应的时间差(最大/平均/次数)，相比消息级统计，服务器请求统计包含了多个Actor处理请求相关若干消息的时间，以及Actor之间的路由和通信开销</li><li>客户端请求统计: 统计从请求发出到收到响应的处理时间(最大/平均/次数)，相比服务器消息统计，多了网络层的时延和机器人本身的处理时间，这是最接近客户端实际体验的指标</li></ol>]]></content>
    
    
    <summary type="html">&lt;p&gt;简单谈谈我们最近是如何给GS做压测的。&lt;/p&gt;
&lt;h3 id=&quot;1-压测机器人&quot;&gt;&lt;a href=&quot;#1-压测机器人&quot; class=&quot;headerlink&quot; title=&quot;1. 压测机器人&quot;&gt;&lt;/a&gt;1. 压测机器人&lt;/h3&gt;&lt;p&gt;压测机器人需要满足如下几个条件:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;异步请求: 异步才能模拟真实的客户端请求和压力&lt;/li&gt;
&lt;li&gt;数据同步: 像客户端一样缓存和处理服务器响应数据，这样才能做好有效请求和可重入&lt;/li&gt;
&lt;li&gt;可重入: 机器人应该可以在任何时候关闭/重启，而不应该假设初始状态(比如只有注册的时候能跑)&lt;/li&gt;
&lt;li&gt;随机性: 机器人行为尽可能随机分布，并且每次重启重新初始化随机种子&lt;/li&gt;
&lt;/ol&gt;</summary>
    
    
    
    <category term="gameserver" scheme="http://wudaijun.com/categories/gameserver/"/>
    
    
    <category term="gameserver" scheme="http://wudaijun.com/tags/gameserver/"/>
    
  </entry>
  
  <entry>
    <title>编程范式游记</title>
    <link href="http://wudaijun.com/2019/05/programing-paradigm/"/>
    <id>http://wudaijun.com/2019/05/programing-paradigm/</id>
    <published>2019-05-25T16:00:00.000Z</published>
    <updated>2020-03-09T16:01:49.000Z</updated>
    
    <content type="html"><![CDATA[<p>这段时间学习OOP对语言和编程范式有一些新的理解，之前系统整理过<a href="https://wudaijun.com/2018/05/understand-functional-programing/">函数式编程</a>，因此先从OOP谈起。我们先回顾下面向对象(OOP)的核心思想:</p><ol><li>将数据及其相关操作(方法)封装起来，以对象的方式暴露出来，对象与对象之间通过方法调用(或者说是发消息)进行通信。</li><li>对象可以有自己的私有字段，只有对象的方法可以访问这些字段。</li><li>每个对象都是一个类(Class)的实例，类定义了对象的行为(内部数据和方法实现)。</li></ol><p>与函数式的”一切皆函数”一样，OOP也有一个宏大的目标”一切皆对象”。</p><a id="more"></a><h3 id="oop-with-dynamic-type"><a href="#oop-with-dynamic-type" class="headerlink" title="oop with dynamic type"></a>oop with dynamic type</h3><p>动态OOP语言，以Ruby为例，它是一种”纯度比较高”的OOP语言，它有一些比较有意思的特性:</p><ol><li>null,3,true等都是对象，对象的类也是对象，当然也有例外，如Blocks</li><li>由于对象的类也是对象，因此你可以动态更改类的定义，如添加新方法，如何更改对象一样</li><li>对象与对象之间只能通过方法通信，即对象不能直接访问其它对象的字段</li></ol><h4 id="subclass"><a href="#subclass" class="headerlink" title="subclass"></a>subclass</h4><p>除了基于对象的封装之外，另一个OOP需要考虑的问题就是复用，以Ruby为例，比如我们有个Point类:</p><figure class="highlight ruby"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point</span></span></span><br><span class="line">    <span class="keyword">attr_accessor</span> <span class="symbol">:x</span>, <span class="symbol">:y</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">initialize</span><span class="params">(x,y)</span></span></span><br><span class="line">        @x = x</span><br><span class="line">        @y = y</span><br><span class="line">    <span class="keyword">end</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">distFromOrigin</span></span></span><br><span class="line">        Math.sqrt(@x * @x + @y * @y)</span><br><span class="line">    <span class="keyword">end</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">distFromOrigin2</span></span></span><br><span class="line">        Math.sqrt(x * x + y * y)</span><br><span class="line">    <span class="keyword">end</span></span><br><span class="line"><span class="keyword">end</span></span><br></pre></td></tr></table></figure><p>现在我们要创建一个ColorPoint类，它除了多个Color字段外，其它属性和行为与Point一模一样，这个时候我们有如下三种做法:</p><ol><li>在ColorPoint类定义中，将Point相关的代码拷贝过来或重写，ColorPoint成为了与Point不相关的两个类</li><li>在ColorPoint类中定义一个Point类的成员，然后将distFromOrigin和distFromOrigin2等方法都转调(forwarding)到该成员上</li><li>通过将ColorPoint声明为Point的子类(subclass)，这样ColorPoint就继承Point的所有属性和方法，并且仍然可以自己扩展属性，覆盖或新增方法</li></ol><p>以上三种实现方式导致的ColorPoint和Point耦合度依次递增，在大多数场景下，该问题的最佳方案应该是方案3，因为它能够最大程度达成代码复用。ColorPoint可以在Point上添加Color字段并实现自己的构造函数。</p><p>在OOP中，subclass通常是很容易被滥用的，比如我们现在要实现一个Point3D类，它多了个z属性，那么它的<code>distFromOrigin</code>和<code>distFromOrigin2</code>都需要override，它真正能够复用的只有<code>x</code>，<code>y</code>两个存取器，这个时候就会有一些争议(复用程度太低，是否应该使用subclass)，特别是如果Point还有个方法<code>distance(p)</code>，用于求出两点距离时，此时ColorPoint需要override该方法，并且参数为ColorPoint，此时将Point对象传给ColorPoint的distance将得到运行时错误。因此在用subclass时，需要谨慎评估类之间的关系，以及类扩展和重写带来的影响。</p><h4 id="duck-typing"><a href="#duck-typing" class="headerlink" title="duck typing"></a>duck typing</h4><p>由于Ruby是动态语言，前面我们讨论的ColorPoint的三种实现方式都不影响ColorPoint和Point的使用，如我们有个函数:</p><figure class="highlight ruby"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">mirror_update</span> <span class="title">pt</span></span></span><br><span class="line">    pt.x = pt.x * -<span class="number">1</span></span><br><span class="line"><span class="keyword">end</span></span><br></pre></td></tr></table></figure><p>三种方式实现的ColorPoint都可以作为<code>mirror_update</code>的参数，因为<code>mirror_update</code>对参数pt的要求是:</p><ol><li>pt实现了<code>x()</code>方法</li><li><code>pt.x()</code>方法的返回值实现了<code>*</code>方法，可接受<code>-1</code>为参数</li><li>pt实现了<code>x=</code>方法，可接受<code>pt.x*-1</code>的结果作为参数</li></ol><p>三种方式实现的Point和ColorPoint都满足以上要求，因此它们都可以使用<code>mirror_update</code>函数。这就是所谓的<a href="https://zh.wikipedia.org/wiki/%E9%B8%AD%E5%AD%90%E7%B1%BB%E5%9E%8B">Duck Typing</a>。在Ruby这类动态类型OOP语言中，代码复用非常灵活。当然这也不是免费的，由于少了静态类型检查，如果调用<code>mirror_update(Point.new(1,&quot;haha&quot;))</code>会”正确”得到<code>-1</code>，而调用<code>mirror_update(Point.new(&quot;haha&quot;,-1))</code>则会得到<code>negative argument (ArgumentError)</code>运行时错误(string没有实现<code>*</code>方法)，两种调用报错的时机和报错的形式取决与业务代码。尽管它们从逻辑上来说都是错误的。</p><h4 id="dynamic-dispatch"><a href="#dynamic-dispatch" class="headerlink" title="dynamic dispatch"></a>dynamic dispatch</h4><p>再举一个例子，如果我们要以subclass创建一个极坐标点类PolarPoint(包含一个半径属性和一个角度属性)，为了保证继承的语义，PolarPoint不得不重写来自于Point的<code>x</code>，<code>y</code>属性，我们不再讨论这里使用继承的合理性，而是想引出一个有意思的地方: PlarPoint的<code>distFromOrigin2</code>无需重写，已经可以正常工作！这得益于ruby的<strong>dynamic dispatch</strong>: 用subclass的对象调用superclass的方法时，将优先动态dispatch到subclass的方法上。dynamic dispatch在其它语言中也叫做虚函数(virtual function)或延迟绑定(late bingding)，核心思路是基于对象的方法调用，总是优先从对象实际所属类(动态)上动态查找，而不是方法所属类(静态)。</p><p>dynamic dispatch让代码复用更上了一个层次，比如你可能有一些GUI相关的基类，它已经实现绘图，缩放等操作，你在子类中只需要实现必要的形状信息，就可以基类的方法绘制定制图形，而无需自己再重写绘图相关操作。</p><h4 id="mutiple-inheritance"><a href="#mutiple-inheritance" class="headerlink" title="mutiple inheritance"></a>mutiple inheritance</h4><p>回到我们的ColorPoint，随着功能迭代，ColorPoint的颜色相关API越来越多，如加深/调色等，你可能希望将这部分代码单独抽象为一个Color类，以达成更好的代码复用。然后ColorPoint再从Color类和Point类继承，这就是多重继承。在OOP中，多重继承的名声不是太好，因为它有一些”哲学上的问题”无法达成统一: 当两个superclass有相同的字段和方法时，subclass应该如何继承？</p><p><img src="/assets/image/201905/mutiple-inheritance.png" alt=""></p><p>假如B和C有相同的属性和方法，那么有如下可能:</p><ol><li>D希望同时继承B和C的同名方法: 即D同时有B和C的能力，这是继承的本意，名字碰撞应该通过其它作用域来限制</li><li>D希望只继承B或C的方法: 比如B为ColorPoint，C为Point3D，D为ColorPoint3D，那么显然，D应该继承C的distFromOrigin方法</li><li>B,C相同的方法在A中也有，D想直接从A中继承…</li></ol><p>这就像在现实中，子女的长相可能随父亲，身高可能随母亲，脾气可能谁也不像。多重继承的这些问题很容易造成歧义和理解负担。Ruby选择不支持多重继承，但提供一个叫<strong>mixins</strong>的机制: 尝试通过include module的方式来消除对多重继承的需要。比如ColorPoint:</p><figure class="highlight ruby"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">module</span> <span class="title">Color</span></span></span><br><span class="line">    <span class="keyword">attr_accessor</span> <span class="symbol">:color</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">darken</span></span></span><br><span class="line">        <span class="keyword">self</span>.color = <span class="string">&quot;dark &quot;</span> + <span class="keyword">self</span>.color</span><br><span class="line">    <span class="keyword">end</span></span><br><span class="line"><span class="keyword">end</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ColorPoint</span> &lt; Point</span></span><br><span class="line">    <span class="keyword">include</span> Color</span><br><span class="line"><span class="keyword">end</span></span><br></pre></td></tr></table></figure><p>module和class的主要区别在于它不能实例化对象且不能派生子类，它可以有属性，但用得更多地场景是它只提供方法，然后引用一些它本身未定义的方法交给子类去实现(当然，这里又要用到dynamic dispatch)，比如Ruby的<strong>Enumerable</strong>模块:</p><figure class="highlight ruby"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MyRange</span></span></span><br><span class="line">    <span class="keyword">include</span> Enumerable</span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">initialize</span><span class="params">(low,high)</span></span></span><br><span class="line">        @low = low</span><br><span class="line">        @high = high</span><br><span class="line">    <span class="keyword">end</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">each</span></span></span><br><span class="line">        i=@low</span><br><span class="line">        <span class="keyword">while</span> i &lt;= @high</span><br><span class="line">            <span class="keyword">yield</span> i</span><br><span class="line">            i=i+<span class="number">1</span></span><br><span class="line">        <span class="keyword">end</span></span><br><span class="line">    <span class="keyword">end</span></span><br><span class="line"><span class="keyword">end</span></span><br></pre></td></tr></table></figure><p>包含Enumerable的class只需要实现each方法，即可调用<code>MyRange.new(4,8).inject &#123;|x,y| x+y&#125;</code>，<code>MyRange.new(5,12).count &#123;|i| i.odd?&#125;</code>等方法。mixins通过更像组合(i can)而不是继承(i am)的方式来处理需要多重继承的情形。在方法查找(lookup)规则中，mixins优于subclass，后包含的module优于先包含的。</p><p>很多文章说mixins有多重继承的优点，同时也规避了多重继承的问题，我认为mixins的好处非常有限:</p><ol><li>module可以有属性，方法，除了不能实例对象，和普通superclass没有本质区别，也满足<code>is_a?</code>语义</li><li>module之间，module与superclass仍然有命名冲突的问题，只不过Ruby将lookup规则定死了</li><li>module可以include一个或多个module,这是换了种形式的多重继承</li></ol><p>总的来说，由于动态语言的duck typing特性，subclass主要是用在代码复用上，动态语言通常不关心某个对象是否是某个类(或其子类)的实例(<code>is_a?</code>)，它只关心某个对象有没有实现某个方法，对方法的查找是基于dynamic dispath的。</p><h3 id="oop-with-static-type"><a href="#oop-with-static-type" class="headerlink" title="oop with static type"></a>oop with static type</h3><p>聊完动态OOP语言，再来看看静态OOP语言，我们先抛开OOP，静态语言中所有的数据结构，函数参数/返回值都有静态类型，为了阐述方便，我们用伪代码表示某种虚拟的静态语言，它的描述格式为:</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">&#x2F;&#x2F; distFromOrigin的参数为两个字段x(类型为double)和y(类型为double)的record(或者叫struct)</span><br><span class="line">fun distFromOrigin (p:&#123;x:double,y:double&#125;) double -&gt;</span><br><span class="line">    Math.sqrt(p.x*p.x + p.y*p.y)</span><br><span class="line"></span><br><span class="line">&#x2F;&#x2F; 通过val声明变量pythag类型并赋值</span><br><span class="line">val pythag : &#123;x:double,y:double&#125; &#x3D; &#123;x&#x3D;3.0, y&#x3D;4.0&#125;</span><br><span class="line">&#x2F;&#x2F; 调用函数并接收返回值，由于pythag与distFromOrigin的参数类型一致，因此静态类型检查通过，函数调用成功</span><br><span class="line">val five : double &#x3D; distFromOrigin(pythag)</span><br><span class="line">&#x2F;&#x2F; 调用函数失败，静态类型检查(实参类型int，形参类型&#123;x:doule, y:double&#125;)，编译不通过</span><br><span class="line">val _ &#x3D; distFromOrigin(2)</span><br></pre></td></tr></table></figure><p>注意，在我们用于举例的语言中，record不只是数据结构的概念，它也可以包含function类型的字段，它可以推广到OOP中的Class。</p><h4 id="subtype"><a href="#subtype" class="headerlink" title="subtype"></a>subtype</h4><p>现在假设我们有个描述带颜色的点的record: <code>&#123;x:double, y:double, color:string&#125;</code>:</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">val cp : &#123;x:double,y:double,color:string&#125; &#x3D; &#123;x&#x3D;3.0, y&#x3D;4.0, color&#x3D;&quot;red&quot;&#125;</span><br><span class="line">&#x2F;&#x2F; 静态类型检查失败, &#123;x:double,y:double,color:string&#125; 与 &#123;x:double,y:double&#125; 类型不匹配</span><br><span class="line">var five : double &#x3D; distFromOrigin(cp)</span><br></pre></td></tr></table></figure><p>而事实上，我们是希望cp能够调用distFromOrigin函数的，因为多color字段既不影响函数计算过程(函数需要的字段都有)，也不影响逻辑上的正确性。因此在这里，为了达成更好地代码复用，我们需要静态类型检查做一些扩展: 如果recordA去掉或交换某些字段后变为recordB，那么能用recordB的地方都应该能用recordA。在这种情况下，我们称recordA是recordB的子类型(subtype，注意和子类subclass区分)，记 B &lt;: A。有了这个规则后，由于<code>&#123;x:double,y:double,color:string&#125;</code>是<code>&#123;x:double,y:double&#125;</code>的子类型，子类型实参可以匹配父类型形参，因此cp可使用distFromOrigin函数。</p><h4 id="depth-subtype"><a href="#depth-subtype" class="headerlink" title="depth subtype"></a>depth subtype</h4><p>现在考虑如下情况，即recordA的某个字段是recordB的某个字段的子类型，那么有recordA&lt;:recordB 吗？听起来是可以的，因为如果一个函数以recordB为形参，那么传入recordA，该函数需要的所有字段都能正确访问到。然而还要考虑到字段可变性:</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">fun setToOrigin (c:&#123;center:&#123;x:double,y:double&#125;, r:double&#125;) -&gt; </span><br><span class="line">    c.center &#x3D; &#123;x&#x3D;0.0, y&#x3D;0.0&#125;</span><br><span class="line">    </span><br><span class="line">val sphere:&#123;center:&#123;x:double,y:double,z:double&#125;, r:double&#125;) &#x3D; &#123;center&#x3D;&#123;x&#x3D;3.0,y&#x3D;4.0,z&#x3D;0.0&#125;, r&#x3D;1.0&#125;</span><br><span class="line"></span><br><span class="line">val _ &#x3D; setToOrigin(sphere)</span><br><span class="line">val _ &#x3D; sphere.center.z</span><br></pre></td></tr></table></figure><p>由于setToOrigin不会知道外部传入的supertype还有哪些额外字段，它对center的重置导致center.z字段丢失了，也就破坏了函数本来的语义。因此通常来说: type checker，field setter，depth subtype只能三选二。</p><p>那么实际的编程语言对depth subtype的取舍如何？以Array为例，假如我们有ColorPoint &lt;: Point，那么是否有ColorPoint[] &lt;: Point[]呢？在C++/Go中，是不支持depth subtype的，比如在Go中你不能将[]int实参用于[]interface{}形参(在Go中，interface{}相当于没有任何字段的record，它是任意类型的supertype)，因为函数可能将[]interface{}某个元素改为string类型。而在Java/C#中，却是支持的，比如在Java中，以下代码却能静态检查通过正常编译:</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point</span> </span>&#123; ... &#125; <span class="comment">// has fields double x, y</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ColorPoint</span> <span class="keyword">extends</span> <span class="title">Point</span> </span>&#123; ... &#125; <span class="comment">// adds field String color</span></span><br><span class="line">...</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">m1</span><span class="params">(Point[] pt_arr)</span> </span>&#123;</span><br><span class="line">    pt_arr[<span class="number">0</span>] = <span class="keyword">new</span> Point(<span class="number">3</span>,<span class="number">4</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="function">String <span class="title">m2</span><span class="params">(<span class="keyword">int</span> x)</span> </span>&#123;</span><br><span class="line">    ColorPoint[] cpt_arr = <span class="keyword">new</span> ColorPoint[x];</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>; i &lt; x; i++)</span><br><span class="line">        cpt_arr[i] = <span class="keyword">new</span> ColorPoint(<span class="number">0</span>,<span class="number">0</span>,<span class="string">&quot;green&quot;</span>);</span><br><span class="line">    m1(cpt_arr);</span><br><span class="line">    <span class="keyword">return</span> cpt_arr[<span class="number">0</span>].color;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>但是运行时执行m2函数，却会得到ArrayStoreException异常，因为Java/C#选择了可变性和depth subtype，放弃了type checker，放宽了类型检查的限制，它在运行时记录m1中<code>pt_arr</code>的真实类型(ColorPoint[])，并在执行类型不匹配的写入时，抛出异常。</p><h4 id="function-subtype"><a href="#function-subtype" class="headerlink" title="function subtype"></a>function subtype</h4><p>这里讨论当函数参数/返回值也是函数，那么这些作为参数的函数的subtype规则。假如我们有如下函数:</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">fun callWithOrigin(f: &#123;x:double,y:double&#125;-&gt;&#123;x:double,y:double&#125;) -&gt;</span><br><span class="line">    f(&#123;x:0.0,y:0.0&#125;)</span><br><span class="line">end</span><br></pre></td></tr></table></figure><p>callWithOrigin的参数类型为<code>&#123;x:double,y:double&#125;-&gt;&#123;x:double,y:double&#125;</code>，现在我们来考虑哪些函数实参允许调用callWithOrigin:</p><ul><li><code>&#123;x:double,y:double&#125; -&gt; &#123;x:double,y:double&#125;</code>: ok, 类型完全匹配</li><li><code>&#123;x:double,y:double&#125; -&gt; &#123;x:double,y:double,color:string&#125;</code>: ok, callWithOrigin传入f的函数参数匹配，只不过返回的参数多了个color，callWithOrigin可以正常调用和使用f，只不过它不关心返回的color字段</li><li><code>&#123;x:double,y:double&#125; -&gt; &#123;x:double&#125;</code>: error, callWithOrigin可能用到f返回值中的y字段</li><li><code>&#123;x:double,y:double,z:double&#125; -&gt; &#123;x:double,y:double&#125;</code>: error, callWithOrigin传给f的参数不包含z字段，那么f执行过程就会出错</li><li><code>&#123;x:double&#125; -&gt; &#123;x:double,y:double&#125;</code>: ok, callWithOrigin传给f函数的参数包括x,y字段，只是f函数只用到了x，f函数可以正常执行</li><li><code>&#123;x:double&#125; -&gt; &#123;x:double,y:double,z:double&#125;</code>: ok，由前面的分析可知，实参函数(即subtype function)参数字段不能多，返回值字段不能少。</li></ul><p>因此，有如下结论，对于 t3 &lt;: t1 并且 t2 &lt;: t4, 有 t1-&gt;t2 &lt;: t3-&gt;t4。</p><h4 id="subtype-vs-subclass"><a href="#subtype-vs-subclass" class="headerlink" title="subtype vs subclass"></a>subtype vs subclass</h4><p>我们在讨论Ruby时用的subclass(子类)，而在讨论静态OOP语言时用的是subtype(子类型)，因为它们本质上不是一个东西:</p><ul><li>subclass是通过继承来解决class之间关系和代码复用(class内部实现的复用)的问题。关注的是class实现</li><li>subtype关注的是type checker。它本质是为了放宽静态语言type checker的标准，让静态语言更像动态语言，达成更好地复用性(class之外的复用)和灵活性。Ruby是动态语言，它有duck typing，因此不需要subtype(它没有type，type是一种对象)。</li></ul><p>subtype不一定要通过subclass来实现，理论上你可以有两个完全不相关的类A和B，但他们提供一致的方法，然后你可以声明A是B的subtype。然后任何用B的地方都可以用A。type checker不care这些方法是通过继承得来的还是完全不同的。</p><p>但在大多数静态OOP语言，如Java/C#/C++中，type和class的边界很模糊，绝大多数时候，你可以认为它们是一个东西，这是因为这些语言主要依赖subclass来实现subtype，因此当你创建一个class时，相当于创建了一个type，它的名字和class名字一样，当你声明subclass关系时，也声明了subtype关系。</p><p>另一点是我们在虚拟语言中以record作为对象的type，这个record可以包含字段，方法等，然后这个record的字段还可以修改，而实际上大多数静态OOP语言中，方法字段是不能修改的，比如你不能拿到一个对象，然后修改它的某个方法，因为方法实现是属于类而不是对象的。</p><p>理解了以上两点，现在我们可以将前面讨论的模型放到实际OOP语言中:</p><ol><li>type和class大部分时候是一个东西，声明一个class也就声明了一个type</li><li>subclass可以基于superclass之上添加字段/方法但是不能移除已有字段/方法</li><li>subclass可以override superclass的方法</li></ol><p>为了阐述方便，以下不再严格区分静态OOP语言中的subclass/subtype。</p><h4 id="dynamic-dispath"><a href="#dynamic-dispath" class="headerlink" title="dynamic dispath"></a>dynamic dispath</h4><p>这里我们进一步讨论静态OOP语言中的dynamic dispatch和this指针，以C++为例:</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line">class Point &#123;&#125;; &#x2F;&#x2F; include double x, y field</span><br><span class="line">class ColorPoint: public Point &#123;...&#125;; &#x2F;&#x2F; adds string color field</span><br><span class="line"></span><br><span class="line">class B &#123;</span><br><span class="line">public:</span><br><span class="line">    virtual void showX(Point* p) &#123; cout &lt;&lt; &quot;B showX: &quot; &lt;&lt; p-&gt;x &lt;&lt; endl; &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">class D: public B &#123;</span><br><span class="line">public:</span><br><span class="line">    virtual void showX(Point* p) &#123; cout &lt;&lt; &quot;D showX: &quot; &lt;&lt; p-&gt;x &lt;&lt; endl; &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">int main(void)&#123;</span><br><span class="line">     Point* p &#x3D; new Point(1.0,2.0);</span><br><span class="line">     ColorPoint* cp &#x3D;  new ColorPoint(3.0,4.0,&quot;red&quot;);</span><br><span class="line">     D* d &#x3D; new D();</span><br><span class="line">     d-&gt;showX(p);   &#x2F;&#x2F; D showX: 1  原生调用，不涉及subtype转换</span><br><span class="line">     d-&gt;showX(cp);  &#x2F;&#x2F; D showX: 3  ColorPoint作为Point的subtype，可以替换Point参数</span><br><span class="line">     B* b &#x3D; d;      &#x2F;&#x2F; 将subtype D对象转换为supertype B</span><br><span class="line">     b-&gt;showX(cp);  &#x2F;&#x2F; D showX: 3, dynamic dispath，以对象实际类型(D*)而不是当前类型(B*)来查找方法实现</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>上面定义了四个类，ColorPoint是Point的subtype，D是B的subtype。以下是一些你需要注意的点:</p><ol><li>如果D是B的subtype,那么D*也是B*的subtype</li><li>C++中的dynamic dispatch不是默认开启的，而是通过为指定方法<code>virtual</code>关键字手动开启的</li><li>对象d上调用showX(声明为virtual)方法，总能找到其对应类D的showX实现，而不管d是否被转换为supertype B*</li></ol><p>现在来考虑一个问题，如果B的showX函数声明为<code>virtual void showX(ColorPoint* p)</code>，实现不变，那么上面的<code>b-&gt;showX(cp)</code>会输出什么？答案是<code>B showX: 3</code>。熟悉C++的同学会知道这是因为C++支持重载，编译认识的函数符号是如<code>_showX_Point_</code>这种编码了参数类型的，修改函数参数类型后，将被编译器认为是另一个函数，而非override。但有一门不支持重载的静态OOP语言L，这个修改能够正常的dynamic dispatch吗？我的理解是可以的，传递给<code>B::showX</code>的ColorPoint总能被<code>D::showX</code>正确使用(<code>D:::showX</code>&lt;:<code>B::showX</code>)，这是因为静态OOP语言中的对象不能修改方法(方法属于类)，只能修改字段，也就是在前面说的depth subtype中，舍弃了mehotd field setter，得到method field depth subtyping。</p><p>然后来看看this指针，对C++有一定理解的同学通常将this指针看做类方法的一个隐藏参数，它由编译器自动传入。这种看法确实能更好地理解OOP，将类方法与普通函数统一起来。但有了subtype这个概念，再来看showX方法，B和D的showX方法类型分别为: <code>void showX(B* this, Point* p)</code> 以及 <code>void showX(D* this, Point* p)</code>，这里就出现一个很奇怪的现象，D的showX不再是B的showX的subtype,我只传给了showX B的对象，但可能调用到D的showX(需要D对象，而D对象包含比B对象更多的字段，可能引发未定义错误)。这是因为this参数是特殊处理的，虽然传给showX的实参只是B对象的地址，但它同时也是D对象的地址，编译器会透明地完成这层转换，保证D的showX拿到的是正确的D对象。这也是为什么多态要在指针下才能生效的原因(值拷贝只会拷贝值的静态类型对应内容，后面多余的派生类数据以及虚函数表信息会丢失)。</p><p>结合C++对象内存布局来回顾一下:</p><p><img src="/assets/image/201905/cpp-object-model.png" alt=""></p><ol><li>ColorPoint对象只会基于Point对象增加字段，本身是满足subtype语义的</li><li>ColorPoint向后追加新增字段，而不会变更基类对象的内存布局，这样可以让对象地址转换更轻量(不必做任何额外操作)</li><li>vtable指针会在对象创建时即初始化好，不管该对象地址被转换为何种类型，vtable总是指向对象实际类型的虚函数实现(如果没实现，则指向父类该函数)</li></ol><p>如果我们省掉vtable这些细节，将这个对象模型扩展一下，结合function depth subtype，它应该是这样:</p><p><img src="/assets/image/201905/static-object-model.png" alt=""></p><h4 id="mutiple-inheritance-1"><a href="#mutiple-inheritance-1" class="headerlink" title="mutiple inheritance"></a>mutiple inheritance</h4><p>C++支持多重继承，为了解决多重继承的命名冲突和冗余数据的问题，它可以在subclass构造函数中指定要哪些字段用哪个superclass的。另外它提供一个叫虚继承(virtual interitance)的机制来解决菱形继承的数据冗余问题，即D继承自B,C，B,C有个共同父类A，那么C只会有一份继承自A的数据。</p><p>Java/C#不支持多重继承，它们的类只能有一个直接父类，但是可以实现多个接口(Interface)，Interface是一堆方法的集合，它没有任何实现，当然也不能实例化。相比Ruby的mixins这种”实现继承”言，这种”声明继承”更安全，因为Interface没有任何的字段，即使方法声明有冲突，子类也只需要提供一份实现，并以此为准，没有歧义。</p><p>C++还提供纯虚函数的概念，即方法本身只包含声明，没有实现，在Java中的抽象方法也提供类似的机制，包含抽象方法或纯虚函数的类就和Ruby中的mixins很像，它既可包含声明，也可包含实现，并且不能实例化对象。当类中的所有方法都为抽象方法并且不包含任何字段时，这个类也就变成了Interface。</p><h4 id="generics"><a href="#generics" class="headerlink" title="generics"></a>generics</h4><p>前面我们所说的<strong>subtyping(子类化)</strong>，也叫做 <strong>subtype polymorphism(子类型多态)</strong> ，而另一种静态语言中常见的用于放宽type checker，提高灵活性和复用性的方案叫 <strong>parametric polymorphism(参数多态)</strong> ，也叫做<strong>generics(泛型)</strong> ，generics是很多静态语言都要考虑的一个特性，不只是OOP。比如ML就有强大的类型推导，可以实现很灵活的泛型编程。</p><p>generics用在那些需要表述<strong>任何类型</strong>的地方，即不关心对象的实际类型，通常出现在容器结构和通用算法中，比如大名鼎鼎的C++ STL，它其实就是对泛型的极致运用，封装了各类常用的容器以及各种常用算法，比如如下是一个 STL 中的 reduce 实现:</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="class"><span class="keyword">class</span> <span class="title">InputIt</span>, <span class="title">class</span> <span class="title">T</span>, <span class="title">class</span> <span class="title">BinaryOperation</span>&gt;</span></span><br><span class="line"><span class="class"><span class="title">constexpr</span> // <span class="title">since</span> <span class="title">C</span>++20</span></span><br><span class="line"><span class="class"><span class="title">T</span> <span class="title">accumulate</span>(<span class="title">InputIt</span> <span class="title">first</span>, <span class="title">InputIt</span> <span class="title">last</span>, <span class="title">T</span> <span class="title">init</span>, <span class="title">BinaryOperation</span> <span class="title">op</span>)</span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">for</span> (; first != last; ++first) &#123;</span><br><span class="line">        init = op(<span class="built_in">std</span>::move(init), *first); <span class="comment">// std::move since C++20</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> init;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这几行代码充分展示了STL的一些基础特性:</p><ol><li>通过迭代器抽象对容器元素的基本操作: 借鉴于函数式编程，上例的<code>InputIt</code>提供元素类型<code>T</code>的容器的操作(<code>++</code>,<code>!=</code>,<code>*</code>等)接口，容器本身并不出现在泛型中，它可以是任意Stack, List, Pair等提供了迭代器的标准甚至自定义容器</li><li>函数本身亦可泛型: 已经有函数是第一类对象的雏形，上例中的BinaryOperation既可是一个函数，也可是一个函数对象(重载了<code>()</code>的对象)，STL提供了很多函数对象，如加减乘除</li><li>大量的运算符重载: 双刃剑(方便vs隐晦)，主要是为了兼容C，比如迭代器<code>++</code>操作本身相当于<code>Next()</code></li><li>泛型声明本身不包含对class的任何约束说明: 依赖于编译时对泛型代码的生成来检查，并且运算符重载，隐式构造函数等特性让算法在理解和使用在有一些负担。这是个人认为还不够好的地方，对应的解决方案有: <strong>bounded generic types</strong>，下一节会提到</li></ol><p>C++/Java/C#都提供了泛型机制，但它们的实现方式有些区别，Java的实现方式是”类型擦除(Type Erasure)”，即在编译时将<code>List&lt;T&gt;</code>变为<code>List&lt;Object&gt;</code>，然后加上一些类型检查和类型提取转换，Java运行时没有关于泛型的任何信息，它只会看到Object(动态类型语言的思路)，这样最大的好处在于兼容性，即老的Java运行时也可以运行泛型代码，缺点是由于运行时不知道T的具体类型，因此无法对T进行诸如instanceof,new等操作。C#/C++的泛型则被称为”模板泛型”，即有运行时的支持，对使用者来说像是为每个类型T都生成了对应的ListT类，因此克服了Java这方面的缺点，是语义完整的。C++的template则更强大，它可以实现所谓的元编程，即在模板语法中可以使用分支(偏特化)，递归等特性达到图灵完全性，如你可以通过模板语法求斐波那契数列(写法和函数式语言类似)，并将运算结果或错误在编译器就吐出来，因此C++被戏称”两层语言”，一层是生成C++目标代码的函数式语言(使用模板语法)，另一层才是命令式语言(C++本身)。当然这并不是C++的初衷，这里不再展开。</p><p>Go目前没有对开发者提供泛型(据说Go2.0会加入泛型)，它的代码复用主要靠interface+reflect(额外运行时type check开销)或code generator(额外的复杂度和开发成本)来实现，它们只能解决很少一部分对泛型的需求，因此Go在这方面被广为诟病，比如知乎上<a href="https://www.zhihu.com/question/62991191">Go有什么泛型的实现方法？</a>的高票答案，相信大部分Gopher都深有体会:</p><p><img src="/assets/image/201905/go-generics.gif" alt=""></p><h4 id="generics-vs-subtyping"><a href="#generics-vs-subtyping" class="headerlink" title="generics vs subtyping"></a>generics vs subtyping</h4><p>那么有了generics后，我们还需要subtyping么？比如前面的Pair类，如果使用subtype来完成:</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">LamePair</span> </span>&#123;</span><br><span class="line">    Object x;</span><br><span class="line">    Object y;</span><br><span class="line">    LamePair(Object _x, Object _y)&#123; x=_x; y=_y; &#125;</span><br><span class="line">    <span class="function">LamePair <span class="title">swap</span><span class="params">()</span> </span>&#123; <span class="keyword">return</span> <span class="keyword">new</span> LamePair(y,x); &#125;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br><span class="line">String s = (String)(<span class="keyword">new</span> LamePair(<span class="string">&quot;hi&quot;</span>,<span class="number">4</span>).y); <span class="comment">// error caught only at run-time</span></span><br></pre></td></tr></table></figure><p>由于在构建LamePair时，进行了向上转换(将传入的参数转换为共同的supertype Object)，因此这里实际会有类型信息丢失，当外部想要再次获取LamePair中的元素时，就不得不进行一次向下转换(downcast)，如<code>(String)e</code>，这类转换属于run-time check，即将一部分本应在静态类型检查时暴露的错误放到了运行时，这是有悖静态类型语言的初衷的。所有的对象都属于Object，将所有的方法参数返回值都声明为Object，这是动态类型语言的思路。</p><p>因此subtyping在某些场景下不能替换generics，那反过来呢，如果我们用C++的template来实现distFromOrigin2:</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point</span> &#123;</span></span><br><span class="line"><span class="keyword">float</span> x;</span><br><span class="line"><span class="keyword">float</span> y;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line"><span class="function"><span class="keyword">float</span> <span class="title">X</span><span class="params">(<span class="keyword">void</span>)</span> </span>&#123; <span class="keyword">return</span> x; &#125;</span><br><span class="line"><span class="function"><span class="keyword">float</span> <span class="title">Y</span><span class="params">(<span class="keyword">void</span>)</span> </span>&#123; <span class="keyword">return</span> y; &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ColorPoint</span> &#123;</span></span><br><span class="line"><span class="keyword">float</span> x;</span><br><span class="line"><span class="keyword">float</span> y;</span><br><span class="line"><span class="built_in">string</span> color;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line"><span class="function"><span class="keyword">float</span> <span class="title">X</span><span class="params">(<span class="keyword">void</span>)</span> </span>&#123; <span class="keyword">return</span> x; &#125;</span><br><span class="line"><span class="function"><span class="keyword">float</span> <span class="title">Y</span><span class="params">(<span class="keyword">void</span>)</span> </span>&#123; <span class="keyword">return</span> y; &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="class"><span class="keyword">class</span> <span class="title">T</span>&gt;</span></span><br><span class="line"><span class="class"><span class="title">float</span> <span class="title">distFromOrigin2</span>(<span class="title">T</span> <span class="title">b</span>) &#123;</span></span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">sqrt</span>(b.X()*b.X() + b.Y()*b.Y());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>可以看到，仍然是一种很蹩脚的写法，由于泛型函数本身对类型缺乏认识，通常需要一堆辅助函数(<code>X()</code>,<code>Y()</code>)来帮忙完成算法的运作，前面的STL accumulate中的迭代器类型参数也是这个作用，即便如此虽然<code>distFromOrigin2</code>复用了，但并没解决Point/ColorPoint的其它字段和方法的复用问题。当然，就本例而言，即便这里的ColorPoint是Point的subtype，C++ template额外生成的函数以及丢失的dynamic dispatch也提示着将distFromOrigin2实现为Point方法是更好的选择。</p><p>综上，subtype和generics各自有自己的适用情形，它们作用于不同维度。subtype是基于supertype上的操作复用，强调类与类的复用关系。而generics基于任意类型T，对T之上的容器封装(如Stack,List,Pair,Swap等)和算法复用(如Sort,Find,Reduce等)，强调类通用的扩展。从另一个角度来讲，subtype可以实现运行时多态(dynamic dispatch)，而generics则是编译期多态(编译期生成新的类/函数)。</p><p>事实上，Java/C#同时支持subtyping和generics，因此它们支持一种将两种结合的polymorphism: <strong>bounded generic types</strong>，核心思想是通过subtype来限制generics可接受的类型，想要鱼和熊掌兼得。比如:</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Bound</span>&lt;<span class="title">T</span> <span class="keyword">extends</span> <span class="title">Point</span>&gt; </span></span><br><span class="line"><span class="class"></span>&#123; </span><br><span class="line">    <span class="keyword">private</span> T objRef; </span><br><span class="line">       </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Bound</span><span class="params">(T obj)</span></span>&#123; </span><br><span class="line">        <span class="keyword">this</span>.objRef = obj; </span><br><span class="line">    &#125; </span><br><span class="line">       </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">double</span> <span class="title">doRunTest</span><span class="params">()</span></span>&#123; </span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">this</span>.objRef.distFromOrigin(); </span><br><span class="line">    &#125; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这样，通过subtype让Bound对传入的T有一定的基本认知，可以调用Point的方法(无需外部传入)，通过generics让这些基于Point类之上的算法可复用。</p><h4 id="oop-in-golang"><a href="#oop-in-golang" class="headerlink" title="oop in golang"></a>oop in golang</h4><p>我将Go单独放到一节，因为它与我们熟知的C++/Java/C#/Ruby等OOP语言很不一样，它有一些创新的地方，用来解决那些困扰了OOP几十年的难题。</p><p>如果按照我们前面给出来的OOP定义，Go是OOP语言，或者说它可以实现OOP编程范式。但是Go没有继承(<code>is-a</code>)的概念，即没有subclass的概念，如果一门OOP语言没有subclass，那么我们会考虑两个问题: 1. Go如何实现class代码复用？2. Go如何实现subtype? 下面分别讨论这两个问题。</p><p>Go没有<code>is-a</code>的概念，它推崇<a href="https://en.wikipedia.org/wiki/Composition_over_inheritance">composition over inheritance principle</a>原则，即组合胜于继承，用<code>has-a</code>替代<code>is-a</code>:</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">type</span> A <span class="keyword">struct</span> &#123;</span><br><span class="line">Name <span class="keyword">string</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(a *A)</span> <span class="title">Print</span><span class="params">()</span></span> &#123;</span><br><span class="line">fmt.Println(<span class="string">&quot;Print A Name: &quot;</span>, a.Name)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> B <span class="keyword">struct</span> &#123;</span><br><span class="line">A</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(b *B)</span> <span class="title">Print</span><span class="params">()</span></span> &#123;</span><br><span class="line">fmt.Println(<span class="string">&quot;Print B Name: &quot;</span>, b.Name)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">a1 := A&#123;Name: <span class="string">&quot;Name A1&quot;</span>&#125;</span><br><span class="line">b := &amp;B&#123;A: a1&#125;</span><br><span class="line">a2 := A&#123;Name: <span class="string">&quot;Name A2&quot;</span>&#125;</span><br><span class="line">b.A = a2</span><br><span class="line">b.Print()   <span class="comment">// Print B Name:  Name A2</span></span><br><span class="line">b.A.Print() <span class="comment">// Print A Name:  Name A2</span></span><br><span class="line"><span class="comment">// a := (*A)(b) // cannot convert expression of type *B to type *A</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>以上代码展示了Go如何通过组合而非继承来实现代码复用，当B需要复用A的代码时，它将A声明为类的一个匿名字段，之后就可以通过B来访问A中的方法和字段，当然这里也需要一套名字查找规则: 1. 先查找B中有没有对应的方法和字段 2. 再从后往前查找B中的匿名字段有无该方法和字段。因此可以通过B调用A的方法实际上是编译器的语法糖，并不是dynamic dispatch，因为Go只能通过子类对象调用子类方法，而不能通过父类对象调用子类方法。事实上，Go的子类对象无法转换为父类对象，从实现上来说，它们就是组合关系，你可以动态将B中的A字段赋为其它A对象。当然，这里的”父类””子类”叫法是不严谨的，因为Go没有subclass。</p><p>解决了面向对象的代码复用问题，我们再来看Go如何实现subtype，Go的subtype不是通过subclass来实现的，而是通过Interface来实现的:</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">type Printer interface &#123;</span><br><span class="line">Print()</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">func test(p Printer) &#123;</span><br><span class="line">p.Print()</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">func test2(a *A) &#123;</span><br><span class="line">    a.Print()</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">test(&amp;a1)</span><br><span class="line">test(b)</span><br><span class="line">test2(b) &#x2F;&#x2F; error: cannot use b (type *B) as type *A in argument to test2</span><br></pre></td></tr></table></figure><p>在Java/C#/Go中，都有Interface的概念，但Java/C#的接口实现是需要显式声明的(即类在定义时就知道自己实现了哪些接口)，但是Go的接口不需要显式声明implement，可以在运行时动态判断(实现细节参考<a href="https://wudaijun.com/2018/01/go-interface-implement/">这里</a>)，这个特性为程序提供了极大的灵活性。A,B并不知道自己实现了Printer接口，它在定义的时候甚至还没有出现Printer接口，或者只有个类似Ouputter之类的接口包含相同的方法，Interface将类如何定义和类如何被使用分离开，比如你只要实现了<code>Read(p []byte) (n int, err error)</code>方法，就实现了<code>io.Reader</code>接口，就可以使用<code>ioutil.Read/ReadAll</code>等lib API。</p><p>总结一下，Go通过组合加编译器的一些静态查找规则来实现代码复用，通过Interface来实现subtype，而如C++/Java/C#等语言用subclass来同时提供两种功能，因此导致类关系错综复杂，甚至一度被戏称COP(Class-oriented programing)而非OOP。虽然Interface实现的subtype不如subclass实现的subtype一样强大(Interface只是方法声明集合，而superclass还包含字段)，但Interface的灵活性远胜于需要显式指定的superclass，并且避免了OOP继承长久以来的痛点。</p><h3 id="dynamic-type-vs-static-type"><a href="#dynamic-type-vs-static-type" class="headerlink" title="dynamic type vs static type"></a>dynamic type vs static type</h3><p>简单来说，静态语言的设计宗旨是尽可能在静态检查中多做事情，通过静态检查来过滤大部分的类型错误，优点是程序运行更稳定，Debug也更容易，缺点是会一定程度限制代码设计的灵活性，因此通常需要subtype来在通过规则放宽type check的限制。</p><p>而动态语言的设计宗旨是优先支持更灵活的代码设计(如duck typing)，将type check放到了run-time，优点是程序更灵活，开发效率通常更高，但程序运行的稳定性会差一些，遇到问题的调试也要更复杂。毕竟没有静态类型检查，允许了更灵活的设计，也放行了很多类型错误。</p><h3 id="oop-vs-fp"><a href="#oop-vs-fp" class="headerlink" title="oop vs fp"></a>oop vs fp</h3><p>计算机业界有句古老的名言: “程序=数据结构+算法”。</p><p>OOP(Object-oriented programming)偏向数据结构，函数只是数据结构的行为(对象的方法)，通过class来封装对象，通过subclass来复用对象。OOP的终极奥义是: 一切皆对象，甚至对象的类也是对象。</p><p>FP(Functional programming)偏向算法(函数)，即函数为第一类值，数据只是函数的参数或者执行环境(闭包)。用闭包，柯里化，高阶函数等去完成函数的封装和复用。FP的终极奥义是: 一切皆函数，连数据也可以是函数。</p><p>举个例子，我们可以将数据结构和算法分为两个维度，做成一个表格:</p><p><img src="/assets/image/201905/data-func-grid.png" alt=""></p><p>表的行表示各种数据类型，列为对应的操作。每个编程语言必须要做的就是定义每种数据类型执行每种操作时的行为。</p><p>OOP的做法是按照各个行划分，定义各个数据结构的类，然后在类上面实现该类型所支持的各个方法。而FP的做法是按照各个列划分，定义各个函数，如toString，然后在函数中去区分各个数据类型并实现。</p><p>从这个角度来说，FP和OOP只是以不同的方式来组织你的代码，OOP按照数据来聚合，FP按照函数来聚合，如果使用OOP，那么添加数据类型很方便，你只需要在新定义的类中去实现它支持的操作，无需影响其它已有类。同理如果使用FP，则新添加一个函数很方便。</p><p>另一个比较有意思的点是，FP的二元操作要比OOP更直观，比如我们的add操作:</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">fun add (v1,v2) &#x3D;</span><br><span class="line">    case (v1,v2) of</span><br><span class="line">      (Int i, Int j) &#x3D;&gt; Int (i+j)</span><br><span class="line">    | (Int i, Point(x,y)) &#x3D;&gt; Point(x+i, y+i)</span><br><span class="line">    | (Point(x,y), Int i) &#x3D;&gt; add(v2, v1) &#x2F;&#x2F; 代码复用</span><br><span class="line">    | (Point(x1,y1), Point(x2,y2)) &#x3D;&gt; Point(x1+x2, y1+y2)</span><br></pre></td></tr></table></figure><p>在FP中，二元操作很直观，<code>add(Point, Int)</code>和<code>add(Int, Point)</code>可以复用，而在OOP中:</p><figure class="highlight ruby"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Int</span></span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">add</span> <span class="title">v</span></span></span><br><span class="line">        <span class="keyword">if</span> v.is_a? Int</span><br><span class="line">            addInt(v)</span><br><span class="line">        <span class="keyword">elsif</span> v.is_a? Point</span><br><span class="line">            addPoint(v)</span><br><span class="line">    <span class="keyword">end</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">addInt</span><span class="params">(v)</span></span></span><br><span class="line">        ...</span><br><span class="line">    <span class="keyword">end</span></span><br><span class="line"><span class="keyword">end</span></span><br></pre></td></tr></table></figure><p>上面的代码虽然也算直观，但有两个问题，一是代码复用不好，<code>Int.addPoint</code>和<code>Point.addInt</code>的实现其实是一样的，即使想要复用代码，只用一份实现，那这份实现应该放在Int类还是Point类呢？第二个问题是这里其实是将FP和OOP混着用，在OOP中，应该尽量避免通过运行时判断对象属于哪个类，纯正的OOP应该通过函数调用+dynamic dispatch来避免类型判断:</p><figure class="highlight ruby"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Int</span></span></span><br><span class="line">    <span class="keyword">attr_reader</span> <span class="symbol">:i</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">add</span><span class="params">(v)</span></span> <span class="comment"># first dispatch</span></span><br><span class="line">        v.addInt(<span class="keyword">self</span>)</span><br><span class="line">    <span class="keyword">end</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">addInt</span><span class="params">(v)</span></span> <span class="comment"># second dispatch: v is Int</span></span><br><span class="line">        Int.new(v.i + i)</span><br><span class="line">    <span class="keyword">end</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">addPoint</span><span class="params">(v)</span></span> <span class="comment"># second dispatch: v is Point</span></span><br><span class="line">        Point.new(v.x+i, v.y+i)</span><br><span class="line">    <span class="keyword">end</span></span><br><span class="line"><span class="keyword">end</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Point</span></span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">add</span><span class="params">(v)</span></span> <span class="comment"># first dispatch</span></span><br><span class="line">        v.addPoint(<span class="keyword">self</span>)</span><br><span class="line">    <span class="keyword">end</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">addInt</span><span class="params">(v)</span></span> ... <span class="keyword">end</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">addPoint</span><span class="params">(v)</span></span> ... <span class="keyword">end</span></span><br><span class="line"><span class="keyword">end</span></span><br></pre></td></tr></table></figure><p>现在虽然是纯正的OOP了，但实际上维护这些代码却很麻烦，假设我们增加一个String类型，它也可以参与add运算，那么除了定义String类型本身以外，我们还需要去已有所有类型中添加addString方法，在这种情况下，OOP增加一个数据类型也不那么方便了。</p><ol><li>静态OOP语言如C++/Java/C#可能会提供一种重载的机制，允许同一个方法名不同的类型参数，编译器会自动选择匹配的函数调用。这能够避免运行时的类型检查，也不用二次分发，但复用性和扩展性仍然不好。</li><li>在Java/C#中，可以通过Interface来声明所有的addInt/Point方法，让Int,Point实现这个接口，这样在添加String类型时，在Interface中添加addString方法，静态类型检查能够保证所有的子类都实现了addString</li></ol><p>PS: 这里只是从程序结构的角度对比FP和OOP，事实上FP的一些理念还来自于lamda演算和数学领域，因此大部分的FP语言还有不可变语义，纯函数等特性。</p><p>FP或OOP或其它的编程范式，本质上是以不同的方式对现实问题进行建模，不管是”一切皆函数”还是”一切皆对象”，都是理想化的解决方案。现实中可能没有编程语言完美实现了某一编程范式，更多地是借鉴和吸收，同时支持多种编程范式，毕竟语言是用来解决问题的。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;这段时间学习OOP对语言和编程范式有一些新的理解，之前系统整理过&lt;a href=&quot;https://wudaijun.com/2018/05/understand-functional-programing/&quot;&gt;函数式编程&lt;/a&gt;，因此先从OOP谈起。我们先回顾下面向对象(OOP)的核心思想:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;将数据及其相关操作(方法)封装起来，以对象的方式暴露出来，对象与对象之间通过方法调用(或者说是发消息)进行通信。&lt;/li&gt;
&lt;li&gt;对象可以有自己的私有字段，只有对象的方法可以访问这些字段。&lt;/li&gt;
&lt;li&gt;每个对象都是一个类(Class)的实例，类定义了对象的行为(内部数据和方法实现)。&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;与函数式的”一切皆函数”一样，OOP也有一个宏大的目标”一切皆对象”。&lt;/p&gt;</summary>
    
    
    
    <category term="programing" scheme="http://wudaijun.com/categories/programing/"/>
    
    
    <category term="programing" scheme="http://wudaijun.com/tags/programing/"/>
    
  </entry>
  
  <entry>
    <title>Cache一致性和内存模型</title>
    <link href="http://wudaijun.com/2019/04/cpu-cache-and-memory-model/"/>
    <id>http://wudaijun.com/2019/04/cpu-cache-and-memory-model/</id>
    <published>2019-04-29T16:00:00.000Z</published>
    <updated>2020-09-21T08:54:32.687Z</updated>
    
    <content type="html"><![CDATA[<p>本文主要谈谈CPU Cache的设计，内存屏障的原理和用法，最后简单聊聊内存一致性。</p><p>我们都知道存储器是分层级的，从CPU寄存器到硬盘，越靠近CPU的存储器越小越快，离CPU越远的存储器越大但越慢，即所谓存储器层级(Memory Hierarchy)。以下是计算机内各种存储器的容量和访问速度的典型值。</p><div class="table-container"><table><thead><tr><th>存储器类型</th><th>容量</th><th>特性</th><th>速度</th></tr></thead><tbody><tr><td>CPU寄存器</td><td>几十到几百Bytes</td><td>数据电路触发器，断电丢失数据</td><td>一纳秒甚至更低</td></tr><tr><td>Cache</td><td>分不同层级，几十KB到几MB</td><td>SRAM，断电丢失数据</td><td>几纳秒到几十纳秒</td></tr><tr><td>内存</td><td>几百M到几十G</td><td>DRAM，断电丢失数据</td><td>几百纳秒</td></tr><tr><td>固态硬盘(SDD)</td><td>几十到几百G</td><td>SSD，断电不丢失数据</td><td>几十微秒</td></tr><tr><td>机械硬盘(HDD)</td><td>上百G</td><td>磁性介质和磁头，断电不丢失数据</td><td>几毫秒</td></tr></tbody></table></div><p>从广义的概念上来说，所有的存储器都是其下一级存储器的Cache，CPU Cache缓存的是内存数据，内存缓存的是硬盘数据，而硬盘缓存的则是网络中的数据。本文只谈CPU Cache，一个简单的CPU Cache示意图如下:</p><p><img src="/assets/image/201904/cache-simple.png" alt=""></p><p>图中忽略了一些细节，现代的CPU Cache通常分为三层，分别叫L1,L2,L3 Cache, 其中L1,L2 Cache为每个CPU核特有，L3为所有CPU核共有，L1还分为缓存指令的i-cache(只读)和缓存程序数据的d-cache，L2 L3 Cache则不区分指令和程序数据，称为统一缓存(unified cache)。本文主要讨论缓存命中和缓存一致性的问题，因此我们只关注L1 Cache，不区分指令缓存和程序数据缓存。</p><a id="more"></a><h3 id="Cache-Geometry"><a href="#Cache-Geometry" class="headerlink" title="Cache Geometry"></a>Cache Geometry</h3><p>当CPU加载某个地址上的数据时，会从Cache中查找，Cache由多个Cache Line构成(通常L1 Cache的Cache Line大小为64字节)，因此目标地址必须通过某种转换来映射对应的Cache Line，我们可以很容易想到两种方案:</p><ol><li>指定地址映射到指定Cache Line，读Cache时对地址哈希(通常是按照Cache Line数量取模，即在二进制地址中取中间位)来定位Cache Line，写Cache时如果有冲突则丢掉老的数据。这种策略叫<strong>直接映射</strong></li><li>任何地址都可以映射到任何Cache Line，读Cache时遍历所有Cache Line查找地址，写Cache时，可以按照LFU(最不常使用)或LRU(最近最少使用)等策略来替换。这种策略叫<strong>全相联</strong></li></ol><p>直接映射的缺点在于在特定的代码容易发生冲突不命中，假设某CPU Cache的Cache Line大小为16字节，一共2个Cache Line，有以下求向量点乘的代码:</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 代码片段1</span></span><br><span class="line"><span class="function"><span class="keyword">float</span> <span class="title">dotprod</span><span class="params">(<span class="keyword">float</span> x[<span class="number">8</span>], <span class="keyword">float</span> y[<span class="number">8</span>])</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">float</span> sum = <span class="number">0.0</span>;</span><br><span class="line">    <span class="keyword">int</span> i;</span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; <span class="number">8</span>; i ++)</span><br><span class="line">        sum += x[i] * y[i];</span><br><span class="line">    <span class="keyword">return</span> sum;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>由于x和y在函数栈中是连续存放的，<code>x[0..3]</code>和<code>y[0..3]</code>将映射到同一个Cache Line, <code>x[4..7]</code>和<code>y[4..7]</code>被映射到同一个Cache Line，那么在for循环一次读取<code>x[i]</code>,<code>y[i]</code>的过程中，Cache Line将不断被冲突替换，导致Cache “抖动”(thrashing)。也就是说，在直接映射中，即使程序看起来局部性良好，也不一定能充分利用Cache。</p><p>那么同样的例子，换成全相联，则不会有这个问题，因为LRU算法会使得<code>y[0..3]</code>不会替换<code>x[0..3]</code>所在的Cache Line，也就不会造成Cache抖动。全相连的缺点是由于每一次读Cache都需要遍历所有的Cache Line进行地址匹配，出于效率考虑，它不适用于太大的Cache。</p><p>So，现代OS的操作系统是取两者折中，即组相连结构: 将若干Cache Line分为S个组，组间直接映射，组内全相连，如下图:</p><p><img src="/assets/image/201904/cache-geometry.png" alt=""></p><p>通用的Cache映射策略，将目标地址分为t(标记位)，s(组索引)，b(块偏移)三个部分。我在<a href="http://wudaijun.com/2019/04/linux-perf/">Linux Perf 简单试用</a>中也有例子说明程序局部性对效率的影响。</p><h3 id="Cache-Coherency"><a href="#Cache-Coherency" class="headerlink" title="Cache Coherency"></a>Cache Coherency</h3><p>前面我们谈的主要是Cache的映射策略，Cache设计的最大难点其实在于Cache一致性: 即所有CPU看到的指定地址的值是一致的。比如在CPU尝试修改某个地址值时，其它CPU可能已有该地址的缓存，甚至可能也在执行修改操作。因此该CPU需要先征求其它CPU的”同意”，才能执行操作。这需要给各个CPU的Cache Line加一些标记(状态)，辅以CPU之间的通信机制(事件)来完成， 这可以通过MESI协议来完成。MESI是以下四个状态的简称:</p><p>M(modified): 该行刚被 CPU 改过，并且保证不会出现在其它CPU的Cache Line中。即CPU是该行的所有者。CPU持有该行的唯一正确参照。<br>E(exclusive): 和M类似，但是未被修改，即和内存是一致的，CPU可直接对该行执行修改(修改之后为modified状态)。<br>S(shared): 该行内容至少被一个其它CPU共享，因此该CPU不能直接修改该行。而需要先与其它CPU协商。<br>I(invalid): 该行为无效行，即为空行，前面提到Cache策略会优先填充Invalid行。</p><p>除了状态之外，CPU还需要一些消息机制:</p><p>Read: CPU发起读取数据请求，请求中包含需要读取的数据地址。<br>Read Response: 作为Read消息的响应，该消息可能是内存响应的，也可能是某CPU响应的(比如该地址在某CPU Cache Line中为Modified状态，该CPU必须返回该地址的最新数据)。<br>Invalidate: 该消息包含需要失效的地址，所有的其它CPU需要将对应Cache置为Invalid状态<br>Invalidate Ack: 收到Invalidate消息的CPU在将对应Cache置为Invalid后，返回Invalid Ack<br>Read Invalidate: 相当于Read消息+Invalidate消息，即取得数据并且独占它，将收到一个Read Response和所有其它CPU的Invalid Ack<br>Writeback: 写回消息，即将状态为Modified的行写回到内存，通常在该行将被替换时使用。现代CPU Cache基本都采用”写回(Write Back)”而非”直写(Write Through)”的方式。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">思考: 为什么要有专门的Read Invalidate消息，而不直接用Read + Invalidate消息的方式呢？</span><br></pre></td></tr></table></figure><p>具体MESI状态机的转换不再赘述，本质上来说，CPU体系结构依赖消息传递的方式来共享内存。</p><p>下面举个例子，假设我们有个四核CPU系统，每个CPU只有一个Cache Line，每个Cache Line包含一个字节，内存地址空间为0x0-0xF，一共两个字节的数据，有如下操作序列:</p><p><img src="/assets/image/201904/mesi-example.png" alt=""></p><p>上图第一行代表操作发生的时序，第二行是执行操作的CPU，第三行是CPU执行的操作，后面四行是各个CPU的Cache Line状态，最后两行是地址0和地址8在内存中的数据是是最新的(V)还是过期的(I)。初始状态下，每个CPU Cache Line都是未填充(Invalid)的。</p><ol><li>CPU0 加载地址0x0的数据，发送Read消息，对应Cache Line被标记为Shared</li><li>CPU3 加载地址0x0的数据，同样，Cache Line标记为Shared</li><li>CPU0 加载地址0x8的数据，导致Cache Line被替换，由于Cache Line之前为Shared状态，即与内存中数据一致，可直接覆盖Cache Line，而无需写回</li><li>CPU2 加载地址0x0的数据，并且之后将要修改它，因此CPU2发出Read Invalidate消息以获取该地址的独占权，导致CPU3的Cache Line被置为Invalid，CPU2 Cache Line为Exclusive</li><li>CPU2 修改地址0x0的数据，由于此时Cache Line为Exclusive，因此它可以直接修改Cache Line，此时Cache Line状态为Modified。此时内存中的0x0内存为过期数据(I)</li><li>CPU1 对地址0x0的数据执行原子(atomic)递增操作，将发出Read Invalidate消息，CPU2将返回Read Response(而不是内存)，然后CPU1将持有地址0x0的Cache Line，状态为Modified，数据为递增后的数据，CPU2的Cache Line为Invalid，内存中的数据仍然是过期(I)状态</li><li>CPU1 加载地址0x0的数据，此时CPU1 Cache Line将被替换，由于其状态为Modified，因此需要先执行写回操作将Cache Line写回内存，此时内存中的数据才是最新(V)的</li></ol><h3 id="Store-Buffers"><a href="#Store-Buffers" class="headerlink" title="Store Buffers"></a>Store Buffers</h3><p>MESI协议足够简单，并且能够满足我们对Cache一致性的需求，它在单个CPU对指定地址的反复读写方面有很好的性能表现，但在某个CPU尝试修改在其它CPU Cache Line中存在的数据时，性能表现非常糟糕，因为它需要发出Invalidate消息并等待Ack，这个延迟(Stall)对CPU来说对难以忍受的并且有时是无必要的，比如执行写入的CPU可能只是简单的给这个地址赋值(而不关心它的当前值是什么)。解决这类不必要的延迟的一个方案就是在CPU和Cache之间加一个Store Buffer: CPU可以先将要写入的数据写到Store Buffer，然后继续做其它事情。等到收到其它CPU发过来的Cache Line(Read Response)，再将数据从Store Buffer移到Cache Line。结构如下所示:</p><p><img src="/assets/image/201904/cache-with-store-buffer.png" alt=""></p><p>然后加了Store Buffer之后，会引入另一个问题，比如有如下代码:</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 代码片段2</span></span><br><span class="line">a = <span class="number">1</span>;</span><br><span class="line">b = a + <span class="number">1</span>;</span><br><span class="line">assert(b == <span class="number">2</span>);</span><br></pre></td></tr></table></figure><p>初始状态下，假设a,b值都为0，并且a存在CPU1的Cache Line中(Shared状态)，可能出现如下操作序列:</p><ol><li>CPU0 要写入A，发出Read Invalidate消息，并将a=1写入Store Buffer</li><li>CPU1 收到Read Invalidate，返回Read Response(包含a=0的Cache Line)和Invalidate Ack</li><li>CPU0 收到Read Response，更新Cache Line(a=0)</li><li>CPU0 开始执行 <code>b = a + 1</code>，从Cache Line中加载a，得到a=0</li><li>CPU0 将Store Buffer中的a=1应用到Cache Line</li><li>CPU0 得到 b=0+1，断言失败</li></ol><p>造成这个问题的根源在于对同一个CPU存在对a的两份拷贝，一份在Cache，一份在Store Buffer，前者用于读，后者用于写，因而出现CPU执行顺序与程序顺序(Program Order)不一致(先执行了<code>b=a+1</code>，再执行<code>a=1</code>)。</p><h3 id="Store-Forwarding"><a href="#Store-Forwarding" class="headerlink" title="Store Forwarding"></a>Store Forwarding</h3><p>Store Buffer可能导致破坏程序顺序(Program Order)的问题，硬件工程师在Store Buffer的基础上，又实现了”Store Forwarding”技术: CPU可以直接从Store Buffer中加载数据，即支持将CPU存入Store Buffer的数据传递(forwarding)给后续的加载操作，而不经由Cache。结构如图:</p><p><img src="/assets/image/201904/cache-with-store-forwarding.png" alt=""></p><p>现在解决了同一个CPU读写数据的问题，再来看看并发程序:</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 代码片段3</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">foo</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    a = <span class="number">1</span>;</span><br><span class="line">    b = <span class="number">1</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">bar</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">while</span> (b == <span class="number">0</span>) <span class="keyword">continue</span>;</span><br><span class="line">    assert(a == <span class="number">1</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>假设初始状态下，<code>a=0; b=0;</code>，a存在于CPU1的Cache中，b存在于CPU0的Cache中，均为Exclusive状态，CPU0执行foo函数，CPU1执行bar函数，上面代码的预期显然为断言为真。那么来看下执行序列:</p><ol><li>CPU1执行<code>while(b == 0)</code>，由于CPU1的Cache中没有b，发出<code>Read b</code>消息</li><li>CPU0执行<code>a = 1</code>，由于CPU0的Cache中没有a，因此它将<code>a(当前值1)</code>写入到Store Buffer并发出<code>Read Invalidate a</code>消息</li><li>CPU0执行<code>b = 1</code>，由于b已经存在在Cache中(Exclusive)，因此可直接执行写入</li><li>CPU0收到<code>Read b</code>消息，将Cache中的b(当前值1)返回给CPU1，将b写回到内存，并将Cache Line状态改为Shared</li><li>CPU1收到包含b的Cache Line，结束<code>while (b == 0)</code>循环</li><li>CPU1执行<code>assert(a == 1)</code>，由于此时CPU1 Cache Line中的a仍然为0并且有效(Exclusive)，断言失败</li><li>CPU1收到<code>Read Invalidate a</code>消息，返回包含a的Cache Line，并将本地包含a的Cache Line置为Invalid(已经晚了)</li><li>CPU0收到CPU1传过来的Cache Line，然后将Store Buffer中的a(当前值1)刷新到Cache Line</li></ol><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">思考: 为什么CPU0执行&#96;a&#x3D;1&#96;时要发送Read Invalidate而不直接发送Invalidate?</span><br></pre></td></tr></table></figure><p>出现这个问题的原因在于CPU不知道a, b之间的数据依赖，CPU0对a的写入走的是Store Buffer(有延迟)，而对b的写入走的是Cache，因此b比a先在Cache中生效，导致CPU1读到<code>b=1</code>时，a还存在于Store Buffer中。</p><h3 id="Memory-Barrier"><a href="#Memory-Barrier" class="headerlink" title="Memory Barrier"></a>Memory Barrier</h3><p>对于上面的内存不一致，很难从硬件层面优化，因为CPU不可能知道哪些值是相关联的，因此硬件工程师提供了一个叫内存屏障的东西，开发者可以用它来告诉CPU该如何处理值关联性。我们可以在<code>a=1</code>和<code>b=1</code>之间插入一个内存屏障:</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 代码片段4</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">foo</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    a = <span class="number">1</span>;</span><br><span class="line">    smp_mb()</span><br><span class="line">    b = <span class="number">1</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">bar</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">while</span> (b == <span class="number">0</span>) <span class="keyword">continue</span>;</span><br><span class="line">    assert(a == <span class="number">1</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>当CPU看到内存屏障<code>smp_mb()</code>时，会先刷新当前(屏障前)的Store Buffer，然后再执行后续(屏障后)的Cache写入。这里的”刷新Store Buffer”有两种实现方式: 一是简单地刷新Store Buffer(需要挂起等待相关的Cache Line到达)，二是将后续的写入也写到Store Buffer中，直到屏障前的条目全部应用到Cache Line(可以通过给屏障前的Store Buffer中的条目打个标记来实现)。这样保证了屏障前的写入一定先于屏障后的写入生效，第二种方案明显更优，以第二种方案为例:</p><ol><li>CPU1执行<code>while(b == 0)</code>，由于CPU1的Cache中没有b，发出<code>Read b</code>消息</li><li>CPU0执行<code>a = 1</code>，由于CPU0的Cache中没有a，因此它将<code>a(当前值1)</code>写入到Store Buffer并发出<code>Read Invalidate a</code>消息</li><li>CPU0看到<code>smp_mb()</code>内存屏障，它会标记当前Store Buffer中的所有条目(即<code>a = 1</code>被标记)</li><li>CPU0执行<code>b = 1</code>，尽管b已经存在在Cache中(Exclusive)，但是由于Store Buffer中还存在被标记的条目，因此b不能直接写入，只能先写入Store Buffer中</li><li>CPU0收到<code>Read b</code>消息，将Cache中的b(当前值0)返回给CPU1，将b写回到内存，并将Cache Line状态改为Shared</li><li>CPU1收到包含b的Cache Line，继续<code>while (b == 0)</code>循环</li><li>CPU1收到<code>Read Invalidate a</code>消息，返回包含a的Cache Line，并将本地的Cache Line置为Invalid</li><li>CPU0收到CPU1传过来的包含a的Cache Line，然后将Store Buffer中的a(当前值1)刷新到Cache Line，并且将Cache Line状态置为Modified</li><li>由于CPU0的Store Buffer中被标记的条目已经全部刷新到Cache，此时CPU0可以尝试将Store Buffer中的<code>b=1</code>刷新到Cache，但是由于包含B的Cache Line已经不是Exclusive而是Shared，因此需要先发<code>Invalid b</code>消息</li><li>CPU1收到<code>Invalid b</code>消息，将包含b的Cache Line置为Invalid，返回<code>Invalid Ack</code></li><li>CPU1继续执行<code>while(b == 0)</code>，此时b已经不在Cache中，因此发出Read消息</li><li>CPU0收到<code>Invalid Ack</code>，将Store Buffer中的<code>b=1</code>写入Cache</li><li>CPU0收到Read消息，返回包含b新值的Cache Line</li><li>CPU1收到包含b的Cache Line，可以继续执行<code>while(b == 0)</code>，终止循环</li><li>CPU1执行<code>assert(a == 1)</code>，此时a不在其Cache中，因此发出Read消息</li><li>CPU0收到Read消息，返回包含a新值的Cache Line</li><li>CPU1收到包含a的Cache Line，断言为真</li></ol><p>上面的步骤看起来很多，其实比较简单，由于内存屏障的存在，导致<code>b=1</code>只能随<code>a=1</code>一起进入到Store Buffer，即b的新值不会先于a的新值出现在CPU0的Cache中，对于应用程序而言，内存屏障前的写入会先于内存屏障后的写入生效。</p><h3 id="Invalid-Queue"><a href="#Invalid-Queue" class="headerlink" title="Invalid Queue"></a>Invalid Queue</h3><p>引入了Store Buffer，再辅以Store Forwarding，Memory Barrier，看起来好像可以自洽了，然而还有一个问题没有考虑: Store Buffer的大小是有限的，所有写入操作的Cache Missing都会使用Store Buffer，特别是出现内存屏障时，后续的所有写入操作(不管是否Cache Miss)都会挤压在Store Buffer中(直到Store Buffer中屏障前的条目处理完)，因此Store Buffer很容易会满，当Store Buffer满了之后，CPU还是会卡在等对应的Invalid Ack以处理Store Buffer中的条目。因此还是要回到Invalid Ack中来，Invalid Ack耗时的主要原因是CPU要先将对应的Cache Line置为Invalid后再返回Invalid Ack，一个很忙的CPU可能会导致其它CPU都在等它回Invalid Ack。解决思路还是化同步为异步: CPU不必要处理了Cache Line之后才回Invalid Ack，而是可以先将Invalid消息放到某个请求队列Invalid Queue，然后就返回Invalid Ack。CPU可以后续再处理Invalid Queue中的消息，大幅度降低Invalid Ack响应时间。此时的CPU Cache结构图如下:</p><p><img src="/assets/image/201904/cache-with-invalid-queue.png" width = "500" height = "400" alt="" align=center /></p><p>和Store Buffer类似，Invalid Queue有两个问题要考虑，一是CPU在处理任何Cache Line的MSEI状态前，都必须先看Invalid Queue中是否有该Cache Line的Invalid消息没有处理。这一点在CPU数据竞争不是很激烈时是可以接受的。这方面的一个极端是<a href="http://wudaijun.com/2015/01/false-sharing/">false sharing</a>。</p><p>Invalid Queue的另一个要考虑的问题是它也增加了破坏内存一致性的可能，即可能破坏我们之前提到的内存屏障:</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 代码片段5</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">foo</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    a = <span class="number">1</span>;</span><br><span class="line">    smp_mb()</span><br><span class="line">    b = <span class="number">1</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">bar</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">while</span> (b == <span class="number">0</span>) <span class="keyword">continue</span>;</span><br><span class="line">    assert(a == <span class="number">1</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>仍然假设a, b的初始值为0，a在CPU0,CPU1中均为Shared状态，b为CPU0独占(Exclusive状态)，CPU0执行foo，CPU1执行bar:</p><ol><li>CPU0执行<code>a = 1</code>，由于其有包含a的Cache Line，将a写入Store Buffer，并发出<code>Invalidate a</code>消息</li><li>CPU1执行<code>while(b == 0)</code>，它没有b的Cache，发出<code>Read b</code>消息</li><li>CPU1收到CPU0的<code>Invalidate a</code>消息，将其放入Invalidate Queue，返回<code>Invalidate Ack</code></li><li>CPU0收到<code>Invalidate Ack</code>，将Store Buffer中的<code>a=1</code>刷新到Cache Line，标记为Modified</li><li>CPU0看到<code>smp_mb()</code>内存屏障，但是由于其Store Buffer为空，因此它可以直接跳过该语句</li><li>CPU0执行<code>b = 1</code>，由于其Cache独占b，因此直接执行写入，Cache Line标记为Modified，</li><li>CPU0收到CPU1发的<code>Read b</code>消息，将包含b的Cache Line写回内存并返回该Cache Line，本地的Cache Line标记为Shared</li><li>CPU1收到包含b(当前值1)的Cache Line，结束while循环</li><li>CPU1执行<code>assert(a == 1)</code>，由于其本地有包含a旧值的Cache Line，读到a初始值0，断言失败</li><li>CPU1这时才处理Invalid Queue中的消息，将包含a旧值的Cache Line置为Invalid</li></ol><p>问题在于CPU1在读取a的Cache Line时，没有先处理Invalid Queue中该Cache Line的Invalid操作，解决思路仍然是内存屏障，我们可以通过内存屏障让CPU标记当前Invalid Queue中所有的条目，所有的后续加载操作必须先等Invalid Queue中标记的条目处理完成再执行。因此我们可以在<code>while</code>和<code>assert</code>之间插入<code>smp_mb()</code>。这样CPU1在看到<code>smp_mb()</code>后，会先处理Invalidate Queue，然后发现本地没有包含a的Cache Line，重新从CPU0获取，得到a的值为1，断言成立。具体操作序列不再赘述。</p><p>前面我们说的内存屏障可以同时作用于Store Buffer和Invalidate Queue，而实际上，CPU0(foo函数)只有写操作，因此只关心Store Buffer，同样的CPU1(bar函数)都是读操作，只关心Invalidate Queue，因此，大多数CPU架构将内存屏障分为了读屏障(Read Memory Barrier)和写屏障(Write Memory Barrier):</p><ul><li>读屏障: 任何读屏障前的读操作都会先于读屏障后的读操作完成</li><li>写屏障: 任何写屏障前的写操作都会先于写屏障后的写操作完成</li><li>全屏障: 同时包含读屏障和写屏障的作用</li></ul><p>因此前面的例子中，foo函数只需要写屏障，bar函数需要读屏障。实际的CPU架构中，可能提供多种内存屏障，比如可能分为四种:</p><ul><li>LoadLoad: 相当于前面说的读屏障</li><li>LoadStore: 任何该屏障前的读操作都会先于该屏障后的写操作完成</li><li>StoreLoad: 任何该屏障前的写操作都会先于该屏障后的读操作完成</li><li>StoreStore: 相当于前面说的写屏障</li></ul><p>实现原理类似，都是基于Store Buffer和Invalidate Queue，不再赘述。</p><h3 id="Instruction-Reordering"><a href="#Instruction-Reordering" class="headerlink" title="Instruction Reordering"></a>Instruction Reordering</h3><p>到目前为止我们只考虑了CPU按照程序顺序执行指令，而实际上为了更好地利用CPU，CPU和编译器都可能会对指令进行重排(reordering):</p><ol><li>编译期间重排: 编译器在编译期间，可能对指令进行重排，以使其对CPU更友好</li><li>运行期间重排: CPU在执行指令的过程中，可能乱序执行以更好地利用流水线</li></ol><p>不管是CPU架构，VM，还是编译器，在对指令进行重排时都要遵守一个约束: 不管指令如何重排，对单线程来说，结果必然是一致的。即不会改变单线程程序的行为。比如:</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 代码片段6</span></span><br><span class="line">a = <span class="number">1</span>;</span><br><span class="line">b = <span class="number">2</span>;</span><br><span class="line">c = a + b;</span><br></pre></td></tr></table></figure><p>编译器/CPU/VM 可以对<code>a = 1;</code>和<code>b = 2;</code>进行对换，而不能将<code>c = a + b</code>与前面两句对换，在实现上来说，对指定地址的操作(读写)序列，CPU是会保证和程序顺序一致的(比如a是先写后读)，并且CPU的读写对自己总是可见的(Store Forwarding)，对于不同的地址，CPU不能解析其依赖关系，可能会乱序执行，比如如果有其它线程依赖于a先于b赋值这个事实，那么就必须要应用程序告诉CPU/编译器，a和b有依赖关系，不要重排。前面提到的内存屏障，一直谈的是它的可见性(visibility)功能，它能够让屏障前的操作(读/写)即时刷新，被其它CPU看到。而内存屏障还有个功能就是限制指令重排(读/写指令)，否则即使在<code>a = 1</code>和<code>b = 2</code>之间加了内存屏障，b也有可能先于a赋值，前面的<code>foo()</code>和<code>bar()</code>的例子也会断言失败。</p><h3 id="Programing"><a href="#Programing" class="headerlink" title="Programing"></a>Programing</h3><p>对应用层而言，各种语言提供的并发相关关键字和工具，底层都会使用内存屏障。</p><h4 id="volatile"><a href="#volatile" class="headerlink" title="volatile"></a>volatile</h4><p>java中可以通过volatile关键字来保证变量的可见性，并限制局部的指令重排。它的实现原理是在每个volatile变量写操作前插入StoreStore屏障，在写操作后插入StoreLoad屏障，在每个volatile变量读操作前插入LoadLoad屏障，在读操作后插入LoadStore屏障来完成。</p><h4 id="atomic"><a href="#atomic" class="headerlink" title="atomic"></a>atomic</h4><p>以C++的atomic为例，atomic本身的职责是保证原子性，与volatile定位不太一样，后者本身是不保证原子性的，C++ atomic允许在保证原子的基础上，指定内存顺序，即使用哪种内存屏障。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 代码片段7</span></span><br><span class="line"><span class="comment">// memory_order_relaxed 松散的内存模型，不要求任何顺序性，只要求原子性。通常用于计数器自增</span></span><br><span class="line"><span class="comment">// Thread A:</span></span><br><span class="line">r1 = y.load(memory_order_relaxed); <span class="comment">// A</span></span><br><span class="line">x.store(r1, memory_order_relaxed); <span class="comment">// B</span></span><br><span class="line"><span class="comment">// Thread B:</span></span><br><span class="line">r2 = x.load(memory_order_relaxed); <span class="comment">// C </span></span><br><span class="line">y.store(<span class="number">42</span>, memory_order_relaxed); <span class="comment">// D</span></span><br></pre></td></tr></table></figure><p>在这种情况下，可能出现全局执行序列为: D A B C，出现r1=r2=42的情况。<code>memory_order_relaxed</code>相当于没有加内存屏障。除了<code>memory_order_relaxed</code>外，还有:</p><ul><li><code>memory_order_acquire</code>: 在该原子变量的读操作前插入LoadLoad屏障，在读操作后插入LoadStore。即Load之后的所有读写操作不能重排到Load之前</li><li><code>memory_order_consume</code>: acquire限制了Load之后的所有读写操作向前重排，而consume则只限制相关联的读写操作(单线程语义内)</li><li><code>memory_order_release</code>: 在该原子变量的写操作前插入LoadStore屏障，在写操作后插入StoreStore屏障。即Store之前的所有读写操作不能重排到Store之后</li><li><code>memory_order_acq_rel</code>: 相当于 <code>memory_order_acquire</code> + <code>memory_order_release</code></li><li><code>memory_order_seq_cst</code>: 最强的顺序一致性，在<code>memory_order_acq_rel</code>的基础上，支持单独全序，即所有线程以同一顺序观测到该原子变量的所有修改</li></ul><p>这里也引申出关于内存屏障的两个常用语义:</p><ul><li>acquire语义：Load 之后的读写操作无法被重排至 Load 之前。即 相当于LoadLoad和LoadStore屏障。</li><li>release语义：Store 之前的读写操作无法被重排至 Store 之后。即 相当于LoadStore和StoreStore屏障。</li></ul><p>注意acquire和release语义没有提到StoreLoad屏障，StoreLoad屏障是四种屏障中开销最大的，这个在后面会提到。</p><h4 id="mutex"><a href="#mutex" class="headerlink" title="mutex"></a>mutex</h4><p>mutex的实现通常是在mutex lock时加上acquire屏障(LoadLoad+LoadStore)，在mutex unlock时加上release屏障(StoreStore+StoreLoad)，例如:</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 代码片段8</span></span><br><span class="line"><span class="comment">// -&gt; LoadLoad</span></span><br><span class="line">mutex_lock(a);</span><br><span class="line"><span class="comment">// -&gt; LoadStore</span></span><br><span class="line">x = x+<span class="number">1</span>;</span><br><span class="line"><span class="comment">// -&gt; StoreStore</span></span><br><span class="line">mutex_unlock(a);</span><br><span class="line"><span class="comment">// -&gt; StoreLoad</span></span><br></pre></td></tr></table></figure><p>由于mutex任意时刻只能被一个线程占有，因此A线程拿到mutex必然在B线程释放mutex之后，由于内存屏障的存在，<code>mutex_lock</code>和<code>mutex_unlock</code>之间的指令只能在mutex里面(无法越过mutex)，并且A线程能即时看到B线程mutex中作出的更改。</p><p>注意，这里列举的volatile, atomic, mutex的具体实现和语义可能在不同的语言甚至同种语言不同的编译平台中有所区别(如C++不同的VS版本对volatile关键字的内存屏障使用有所区别)。对开发者而言，编写并发程序需要关注三个东西: 原子性，可见性和顺序性。</p><ul><li>原子性: 尽管在如今大部分平台下，对一个字的数据进行存取(int,指针)的操作本身就是原子性的，但为了更好地跨平台性，通过atomic操作来实现原子性是更好的方法，并且不会造成额外的开销。C++的atomic还提供可见性和顺序性选项</li><li>可见性: 数据同步相关，前面讨论的CPU Cache设计主要关注的就是可见性，即同一时刻所有CPU看到的某个地址上的值是一致的。Cache一致性主要解决的就是数据可见性的问题</li><li>顺序性: 内存屏障的另一个功能就是可以限制局部的指令重排(一些文章将内存屏障定义为限制指令重排工具，我认为是不准确的，如前面所讨论的，即使没有指令重排，有时也需要内存屏障来保证可见性)。内存屏障保证屏障前的某些操作必定限于屏障后的操作<strong>发生且可见</strong>。但屏障前或屏障后的指令，CPU/编译器仍然可以在不改变单线程结果的情况下进行局部重排。每个硬件平台有自己的基础顺序性(强/弱内存模型)</li></ul><h3 id="Weak-Strong-Memory-Models"><a href="#Weak-Strong-Memory-Models" class="headerlink" title="Weak/Strong Memory Models"></a>Weak/Strong Memory Models</h3><p>不同的处理器平台，本身的内存模型有强(Strong)弱(Weak)之分。</p><ul><li><code>Weak Memory Model</code>: 如DEC Alpha是弱内存模型，它可能经历所有的四种内存乱序(LoadLoad, LoadStore, StoreLoad, StoreStore)，任何Load和Store操作都能与任何其它的Load或Store操作乱序，只要其不改变单线程的行为。</li><li><code>Weak With Date Dependency Ordering</code>: 如ARM, PowerPC, Itanium，在Aplpha的基础上，支持数据依赖排序，如C/C++中的<code>A-&gt;B</code>，它能保证加载B时，必定已经加载最新的A</li><li><code>Strong Memory Model</code>: 如X86/64，强内存模型能够保证每条指令<code>acquire and release</code>语义，换句话说，它使用了LoadLoad/LoadStore/StoreStore三种内存屏障，即避免了四种乱序中的三种，仍然保留StoreLoad的重排，对于代码片段7来说，它仍然可能出现r1=r2=42的情况</li><li><code>Sequential Consistency</code>: 最强的一致性，理想中的模型，在这种内存模型中，没有乱序的存在。如今很难找到一个硬件体系结构支持顺序一致性，因为它会严重限制硬件对CPU执行效率的优化(对寄存器/Cache/流水线的使用)。</li></ul><p>前面说到C++ atomic内存模型属于语言级的约束定义，它建立在处理器平台内存模型之上，如果处理器平台是SC(Sequential Consistency)的，那么语言级无论如何定义也无法将硬件改为更松散的内存模型。语言级的内存模型还有一个重要作用就是限制编译器reorder，即生成编译器屏障(fence)。因此即使处理器平台是SC的，语言层面定义为relaxed也可能因为编译器reorder导致结果不如预期。</p><h3 id="Summary"><a href="#Summary" class="headerlink" title="Summary"></a>Summary</h3><p>本文比较杂乱，前面主要介绍CPU Cache结构和Cache一致性问题，引出内存屏障的概念。后面顺便简单谈了谈指令乱序和内存一致性。</p><p>实际的CPU Cache结构比上面阐述的要复杂得多，其核心的优化理念都是化同步为异步，然后再去处理异步下的一致性问题(处理不了就交给开发者…)。尽管异步会带来更多的问题，但它仍然是达成高吞吐量的必经之路。硬件方面的结构优化到一定程度了，CPU/编译器就开始打应用层代码的主意: 指令重排。</p><p>对开发者来说，应用程序可以通过封装好的mutex完成大部分的并发控制，而无需关注底层用了哪些内存屏障，各平台的内存一致性等细节。但是在使用比mutex更底层的同步机制(如atomic, volatile, memory-barrier, lock-free等)时，就要务必小心。从原子性，可见性，顺序性等方面确保代码执行结果如预期。</p><h3 id="References"><a href="#References" class="headerlink" title="References"></a>References</h3><ol><li><a href="http://wudaijun.com/2018/09/distributed-consistency/">一致性杂谈</a></li><li><a href="http://irl.cs.ucla.edu/~yingdi/web/paperreading/whymb.2010.06.07c.pdf">Memory Barriers: a Hardware View for Software Hackers</a></li><li><a href="https://preshing.com/20120930/weak-vs-strong-memory-models/">Weak vs. Strong Memory Models</a></li><li><a href="https://preshing.com/20120913/acquire-and-release-semantics/">Acquire and Release Semantics</a></li><li><a href="https://www.zhihu.com/question/24301047">如何理解 C++11 的六种 memory order？</a></li></ol>]]></content>
    
    
    <summary type="html">&lt;p&gt;本文主要谈谈CPU Cache的设计，内存屏障的原理和用法，最后简单聊聊内存一致性。&lt;/p&gt;
&lt;p&gt;我们都知道存储器是分层级的，从CPU寄存器到硬盘，越靠近CPU的存储器越小越快，离CPU越远的存储器越大但越慢，即所谓存储器层级(Memory Hierarchy)。以下是计算机内各种存储器的容量和访问速度的典型值。&lt;/p&gt;
&lt;div class=&quot;table-container&quot;&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;存储器类型&lt;/th&gt;
&lt;th&gt;容量&lt;/th&gt;
&lt;th&gt;特性&lt;/th&gt;
&lt;th&gt;速度&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;CPU寄存器&lt;/td&gt;
&lt;td&gt;几十到几百Bytes&lt;/td&gt;
&lt;td&gt;数据电路触发器，断电丢失数据&lt;/td&gt;
&lt;td&gt;一纳秒甚至更低&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Cache&lt;/td&gt;
&lt;td&gt;分不同层级，几十KB到几MB&lt;/td&gt;
&lt;td&gt;SRAM，断电丢失数据&lt;/td&gt;
&lt;td&gt;几纳秒到几十纳秒&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;内存&lt;/td&gt;
&lt;td&gt;几百M到几十G&lt;/td&gt;
&lt;td&gt;DRAM，断电丢失数据&lt;/td&gt;
&lt;td&gt;几百纳秒&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;固态硬盘(SDD)&lt;/td&gt;
&lt;td&gt;几十到几百G&lt;/td&gt;
&lt;td&gt;SSD，断电不丢失数据&lt;/td&gt;
&lt;td&gt;几十微秒&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;机械硬盘(HDD)&lt;/td&gt;
&lt;td&gt;上百G&lt;/td&gt;
&lt;td&gt;磁性介质和磁头，断电不丢失数据&lt;/td&gt;
&lt;td&gt;几毫秒&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;/div&gt;
&lt;p&gt;从广义的概念上来说，所有的存储器都是其下一级存储器的Cache，CPU Cache缓存的是内存数据，内存缓存的是硬盘数据，而硬盘缓存的则是网络中的数据。本文只谈CPU Cache，一个简单的CPU Cache示意图如下:&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;/assets/image/201904/cache-simple.png&quot; alt=&quot;&quot;&gt;&lt;/p&gt;
&lt;p&gt;图中忽略了一些细节，现代的CPU Cache通常分为三层，分别叫L1,L2,L3 Cache, 其中L1,L2 Cache为每个CPU核特有，L3为所有CPU核共有，L1还分为缓存指令的i-cache(只读)和缓存程序数据的d-cache，L2 L3 Cache则不区分指令和程序数据，称为统一缓存(unified cache)。本文主要讨论缓存命中和缓存一致性的问题，因此我们只关注L1 Cache，不区分指令缓存和程序数据缓存。&lt;/p&gt;</summary>
    
    
    
    <category term="os" scheme="http://wudaijun.com/categories/os/"/>
    
    
    <category term="os" scheme="http://wudaijun.com/tags/os/"/>
    
  </entry>
  
  <entry>
    <title>Linux Perf 简单试用</title>
    <link href="http://wudaijun.com/2019/04/linux-perf/"/>
    <id>http://wudaijun.com/2019/04/linux-perf/</id>
    <published>2019-04-04T16:00:00.000Z</published>
    <updated>2019-04-05T01:57:36.000Z</updated>
    
    <content type="html"><![CDATA[<p>Perf(Performance Event)是Linux 2.6.31后内置的性能分析工具，它相较其它Prof工具最大的优势在于与Linux Kernel紧密结合，可以进行内核甚至硬件级的性能分析。我之前只零散地用一些<code>ptrace</code>,<code>strace</code>之类的小工具，与Perf比起来，确实小巫见大巫。也赶紧花了点时间简单了解和试用一下，添加到工具箱，以备不时之需。</p><a id="more"></a><p>几乎所有的性能分析工具的基本原理都是对监测目标进行数据采样，Perf也不例外，Perf可以基于各种Event对目标进行测样。如基于时间点(tick)采样，可以得到程序运行时间的分布，即程序中哪些函数最耗时。基于cache miss采样，可以得到程序的cache miss分布，即cache失效经常发生在哪些代码中。通过<code>perf list</code>可以Perf支持的各种Event:</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><span class="line">$ sudo perf list                                                                                                                                                             List of pre-defined events (to be used in -e):</span><br><span class="line"></span><br><span class="line"> cpu-cycles OR cycles                       [Hardware event]</span><br><span class="line"> instructions                               [Hardware event]</span><br><span class="line"> cache-references                           [Hardware event]</span><br><span class="line"> cache-misses                               [Hardware event]</span><br><span class="line"> branch-instructions OR branches            [Hardware event]</span><br><span class="line"> branch-misses                              [Hardware event]</span><br><span class="line"> bus-cycles                                 [Hardware event]</span><br><span class="line"></span><br><span class="line"> cpu-clock                                  [Software event]</span><br><span class="line"> task-clock                                 [Software event]</span><br><span class="line"> page-faults OR faults                      [Software event]</span><br><span class="line"> minor-faults                               [Software event]</span><br><span class="line"> major-faults                               [Software event]</span><br><span class="line"> context-switches OR cs                     [Software event]</span><br><span class="line"> cpu-migrations OR migrations               [Software event]</span><br><span class="line"> alignment-faults                           [Software event]</span><br><span class="line"> emulation-faults                           [Software event]</span><br><span class="line"></span><br><span class="line"> [...]</span><br><span class="line"></span><br><span class="line"> sched:sched_stat_runtime                   [Tracepoint event]</span><br><span class="line"> sched:sched_pi_setprio                     [Tracepoint event]</span><br><span class="line"> syscalls:sys_enter_socket                  [Tracepoint event]</span><br><span class="line"> syscalls:sys_exit_socket                   [Tracepoint event]</span><br><span class="line"></span><br><span class="line"> [...]</span><br></pre></td></tr></table></figure><p>该列表非常长，以上是简化之后的输出结果，可以将Perf Event大致分为三类:</p><ol><li>Hardware Event: 主要是由CPU或PMU硬件产生的事件，如cache-misses, cpu-cycle, instructions, branch-misses等。</li><li>Software Event: 由内核软件产生的事件，如 page-faults, context-switches, cpu-migrations等。</li><li>Tracepoints: 散布在内核源码中的各种静态的追踪点(Hook)，Perf可以通过挂载Hook来收集这些事件。如kmalloc, syscall, sched_switch等。</li></ol><p>PMU(Performance Monitoring Unit) 是各CPU厂商随CPU提供的硬件，它允许软件针对某种CPU硬件事件(如cache miss, branch-misses, instructions)设置counter，并且统计该事件次数，当次数到达counter值后，产生中断。软件通过捕获这些中断来考察CPU使用情况。</p><h3 id="perf-top"><a href="#perf-top" class="headerlink" title="perf top"></a>perf top</h3><p><code>perf top</code> 用于实时显示当前系统的性能统计信息，用于观察整个系统的当前状态。我们先写一个简单的死循环程序:</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">run_forever</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">long</span> <span class="keyword">int</span> i = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">while</span>(<span class="number">1</span>) &#123;</span><br><span class="line">                i++;</span><br><span class="line">        &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        run_forever();</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>编译并执行它: <code>gcc -o t1 -g t1.c &amp;&amp; ./t1</code>，然后在另一个窗口执行<code>perf top</code>:</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">Samples: 249K of event &#39;cycles:ppp&#39;, Event count (approx.): 59571881744</span><br><span class="line">Overhead  Shared Object             Symbol</span><br><span class="line">  92.24%  t1                        [.] run_forever</span><br><span class="line">   0.49%  [kernel]                  [k] menu_select</span><br><span class="line">   0.44%  cadvisor                  [.] runtime.findObject</span><br><span class="line">   0.36%  [kernel]                  [k] nmi</span><br><span class="line">   0.29%  game                      [.] runtime.scanobject</span><br><span class="line">   0.28%  cadvisor                  [.] runtime.scanobject</span><br><span class="line">   0.25%  cadvisor                  [.] runtime.(*mspan).nextFreeIndex</span><br><span class="line">   0.24%  [kernel]                  [k] vsnprintf</span><br><span class="line">   0.21%  [kernel]                  [k] __switch_to</span><br><span class="line">   ...</span><br></pre></td></tr></table></figure><p>默认情况下，<code>perf top</code>命令将采样cpu-cycles Event，即每个时钟周期进行采样，对所有CPU正在执行的代码(Symbol)进行统计(包括内核代码)，并按照出现次数降序排列。因此<code>perf top</code>打印的是CPU运行时间分布，即哪些函数最耗时。上图清楚指明了目前该机器上最耗时的进程(Shared Object)为t1，以及其热点函数(Symbol)为<code>run_forever</code>。在<code>perf top</code>界面按[h]键可以呼叫帮助菜单，看到所有可用的功能何对应的快捷键。选中t1一行，按[a]键即可启用Annotate(注释)功能，它可以进一步查看当前符号:</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line">run_forever  &#x2F;home&#x2F;docker&#x2F;test&#x2F;t1</span><br><span class="line">Percent│</span><br><span class="line">       │</span><br><span class="line">       │</span><br><span class="line">       │    Disassembly of section .text:</span><br><span class="line">       │</span><br><span class="line">       │    00000000000005fa &lt;run_forever&gt;:</span><br><span class="line">       │    run_forever():</span><br><span class="line">       │    void run_forever() &#123;</span><br><span class="line">       │      push   %rbp</span><br><span class="line">       │      mov    %rsp,%rbp</span><br><span class="line">       │            long int i &#x3D; 0;</span><br><span class="line">       │      movq   $0x0,-0x8(%rbp)</span><br><span class="line">       │            while(1) &#123;</span><br><span class="line">       │                    i++;</span><br><span class="line"> 99.95 │ c:   addq   $0x1,-0x8(%rbp)</span><br><span class="line">  0.05 │    ↑ jmp    c</span><br></pre></td></tr></table></figure><p>可以看到该函数 99.94% 的时间都在执行 i++ 这一行。</p><p>当然，你也通过参数指定<code>perf top</code>采样其它Event(比如cache-misses)并设置其采样速率(比如改为每秒5000次，默认是4000):</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ perf top -e cache-misses -c 5000</span><br></pre></td></tr></table></figure><p><code>perf top</code> 适用于做系统的整体状态统计，然后初步定位到问题进程。当然，由于我们的示例代码太简单，通过<code>perf top</code>就足以分析出问题进程和问题代码。而当情况更复杂时，我们则需要其它perf工具的配合。</p><h3 id="perf-stat"><a href="#perf-stat" class="headerlink" title="perf stat"></a>perf stat</h3><p>当你想要分析指定应用程序各方面性能时，可以用<code>perf stat</code>命令，我们仍然通过一段程序来说明:</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">static char array[10000][10000];</span><br><span class="line">int main (void)&#123;</span><br><span class="line">        int i, j;</span><br><span class="line">        for (i &#x3D; 0; i &lt; 10000; i++)</span><br><span class="line">            for (j &#x3D; 0; j &lt; 10000; j++)</span><br><span class="line">                 array[i][j]&#x3D;i;</span><br><span class="line">                 return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>现在我们通过<code>perf stat</code>来对这个程序的一些事件进行采样:</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">$ gcc -o t2 t2.c</span><br><span class="line">$ sudo perf stat -r 5 -e cache-misses,cache-references,instructions,cycles,L1-dcache-stores,L1-dcache-store-misses .&#x2F;t</span><br><span class="line"></span><br><span class="line"> Performance counter stats for &#39;.&#x2F;t2&#39; (5 runs):</span><br><span class="line"></span><br><span class="line">         3,340,700      cache-misses              #    2.631 % of all cache refs      ( +-  0.25% )</span><br><span class="line">       126,973,736      cache-references                                              ( +-  0.02% )</span><br><span class="line">     1,471,226,871      instructions              #    0.40  insn per cycle                                              ( +-  0.01% )</span><br><span class="line">     3,643,287,243      cycles                                                        ( +-  0.59% )</span><br><span class="line">       219,156,878      L1-dcache-stores                                              ( +-  0.01% )</span><br><span class="line">       102,035,758      L1-dcache-store-misses                                        ( +-  0.01% )</span><br><span class="line"></span><br><span class="line">       1.026604322 seconds time elapsed                                          ( +-  0.58% )                                        ( +-  0.26% )</span><br></pre></td></tr></table></figure><p><code>-r</code>指定重复执行次数，可以保证采样结果的可参考性。<code>-e</code>指定要采样的Event，如果指定了cache-misses/cache-reference,cycles/instructions这类成对的Event时，perf会自动计算相关比例值。另外，cache-references/misses指的是Last Level Cache(LLC) references/misses。</p><p>结果显示程序执行时间为1.02s,CPU每个时钟周期可以执行0.4条指令，看这些你可能无法直观地判断程序优劣，但是如果我们善用数据局部性，将<code>array[j][i] = i</code>改为<code>array[i][j] = i</code>，再来看看结果:</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">Performance counter stats for &#39;.&#x2F;t2&#39; (5 runs):</span><br><span class="line"></span><br><span class="line">        1,693,216      cache-misses              #   96.113 % of all cache refs      ( +-  0.05% )</span><br><span class="line">        1,761,695      cache-references                                              ( +-  0.13% )</span><br><span class="line">    1,470,420,403      instructions              #    1.54  insn per cycle                                              ( +-  0.01% )</span><br><span class="line">      955,043,789      cycles                                                        ( +-  0.03% )</span><br><span class="line">      218,964,664      L1-dcache-stores                                              ( +-  0.01% )</span><br><span class="line">        1,790,183      L1-dcache-store-misses                                        ( +-  0.03% )</span><br><span class="line"></span><br><span class="line">      0.271631085 seconds time elapsed                                          ( +-  0.41% )                                       ( +-  0.27% )</span><br></pre></td></tr></table></figure><p>cache-misses数量降了一倍，L1-dcache-store-misses更是降了几十倍，insn per cycle由0.52提升到了1.54，亦即CPU利用率更高了，程序整体运行速度快了接近5倍。但你可能会注意到，程序改动后，<code>cache-misses/cache-references</code>的比例从2.63%提升到96.113%，cache命中率降低了？花了很长时间Google后(perf对各种Event的文档太稀缺了，基本没有一份相对详细的Event文档)，<a href="https://stackoverflow.com/questions/55035313/how-does-linux-perf-calculate-the-cache-references-and-cache-misses-events">这篇问答</a>中提到如果存取在L1-dcache-stores命中，则不会记入cache-references，因此当程序数据局部性提升后，在L1层就已经能取到了，也就导致cache-references急剧减少，相对也就导致<code>cache-misses/cache-references</code>比例反而上升了。</p><p><code>perf stat</code>也可以通过<code>-p</code>附加到正在运行中的进程</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"># 附加到指定进程采样3秒</span><br><span class="line">$ perf stat -p PID sleep 3</span><br><span class="line"># 附加到指定进程，直到目标进程结束，或Ctrl+C终止</span><br><span class="line">$ perf stat -p PID</span><br></pre></td></tr></table></figure><h3 id="perf-record-report"><a href="#perf-record-report" class="headerlink" title="perf record/report"></a>perf record/report</h3><p><code>perf record</code>可以将指定采样数据采样到文件，参数与<code>perf stat</code>类似，可以接可执行文件或附加到指定进程，只不过没有任何输出，而是将采样数据写入到perf.data文件中。与之相匹配的，<code>perf report</code>可以将文件中采样数据展示出来。我们仍然以t2.c为例:</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">sudo perf record -e cache-misses:u .&#x2F;t2</span><br><span class="line">sudo perf report</span><br><span class="line">Samples: 1K of event &#39;cache-misses:u&#39;, Event count (approx.): 1627272</span><br><span class="line">Overhead  Command  Shared Object     Symbol</span><br><span class="line">  96.48%  t2       t2                [.] main</span><br><span class="line">   3.27%  t2       [kernel]          [k] page_fault</span><br><span class="line">   0.11%  t2       ld-2.27.so        [.] 0x000000000000d305</span><br><span class="line">   0.11%  t2       ld-2.27.so        [.] 0x00000000000109b8</span><br><span class="line">   0.03%  t2       ld-2.27.so        [.] 0x0000000000018d7d</span><br><span class="line">   0.00%  t2       ld-2.27.so        [.] 0x0000000000002082</span><br><span class="line">   0.00%  t2       ld-2.27.so        [.] 0x0000000000001ea0</span><br></pre></td></tr></table></figure><p>96.48的 cache-misses Event都分布在main函数中，同样，可以通过上下键选中main函数Enter进入并选择”Annotate main”，可以看到汇编代码级的cache-misses分布(通过 <code>gcc -g -o t2 t2.c</code> 可以保留汇编到代码的符号映射):</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br></pre></td><td class="code"><pre><span class="line">Percent│    Disassembly of section .text:</span><br><span class="line">       │</span><br><span class="line">       │    00000000000005fa &lt;main&gt;:</span><br><span class="line">       │    main():</span><br><span class="line">       │    static char array[10000][10000];</span><br><span class="line">       │    int main (void)&#123;</span><br><span class="line">       │      push   %rbp</span><br><span class="line">       │      mov    %rsp,%rbp</span><br><span class="line">       │            int i, j;</span><br><span class="line">       │            for (i &#x3D; 0; i &lt; 10000; i++)</span><br><span class="line">       │      movl   $0x0,-0x8(%rbp)</span><br><span class="line">       │    ↓ jmp    4d</span><br><span class="line">       │                for (j &#x3D; 0; j &lt; 10000; j++)</span><br><span class="line">       │ d:   movl   $0x0,-0x4(%rbp)</span><br><span class="line">       │    ↓ jmp    40</span><br><span class="line">       │                     array[j][i]&#x3D;i;</span><br><span class="line">  1.16 │16:   mov    -0x8(%rbp),%eax</span><br><span class="line">       │      mov    %eax,%ecx</span><br><span class="line">       │      mov    -0x8(%rbp),%eax</span><br><span class="line">       │      cltq</span><br><span class="line">  0.68 │      mov    -0x4(%rbp),%edx</span><br><span class="line">  0.10 │      movslq %edx,%rdx</span><br><span class="line">       │      imul   $0x2710,%rdx,%rdx</span><br><span class="line">       │      add    %rax,%rdx</span><br><span class="line">  1.06 │      lea    array,%rax</span><br><span class="line">       │      add    %rdx,%rax</span><br><span class="line">       │      mov    %cl,(%rax)</span><br><span class="line">       │                for (j &#x3D; 0; j &lt; 10000; j++)</span><br><span class="line"> 76.54 │      addl   $0x1,-0x4(%rbp)</span><br><span class="line">  0.48 │40:   cmpl   $0x270f,-0x4(%rbp)</span><br><span class="line"> 19.98 │    ↑ jle    16</span><br><span class="line">       │            for (i &#x3D; 0; i &lt; 10000; i++)</span><br><span class="line">       │      addl   $0x1,-0x8(%rbp)</span><br><span class="line">       │4d:   cmpl   $0x270f,-0x8(%rbp)</span><br><span class="line">       │    ↑ jle    d</span><br><span class="line">       │                     return 0;</span><br><span class="line">       │      mov    $0x0,%eax</span><br><span class="line">       │    &#125;</span><br><span class="line">       │      pop    %rbp</span><br><span class="line">       │    ← retq</span><br></pre></td></tr></table></figure><p>而如果将代码改成局部友好，main函数将不会出现在<code>perf report</code>的列表中。<code>perf record/report</code>用法与现在的主流prof工具类似，这种方式最灵活，对环境的各种要求也低。</p><p>在学习过程中也发现Perf一些不足之处，首先就是文档很少，特别关于各种Event，因为它们很多与具体硬件实现相关。第二就是与Linux内核深度结合，是优点也是缺点，因为这也意味着它的跨平台性不是很好。因此它更像是Linux系统级的Prof工具，对于应用级的分析，Perf可能是有点过于底层和重量级了。</p><p>参考资料:</p><ol><li><a href="https://perf.wiki.kernel.org/index.php/Tutorial">Linux kernel profiling with perf</a></li><li><a href="https://www.ibm.com/developerworks/cn/linux/l-cn-perf1/index.html">Perf — Linux下的系统性能调优工具，第 1 部分</a></li><li><a href="http://web.eece.maine.edu/~vweaver/projects/perf_events/perf_event_open.html">ManPage of PERF_EVENT_OPEN</a></li></ol>]]></content>
    
    
    <summary type="html">&lt;p&gt;Perf(Performance Event)是Linux 2.6.31后内置的性能分析工具，它相较其它Prof工具最大的优势在于与Linux Kernel紧密结合，可以进行内核甚至硬件级的性能分析。我之前只零散地用一些&lt;code&gt;ptrace&lt;/code&gt;,&lt;code&gt;strace&lt;/code&gt;之类的小工具，与Perf比起来，确实小巫见大巫。也赶紧花了点时间简单了解和试用一下，添加到工具箱，以备不时之需。&lt;/p&gt;</summary>
    
    
    
    <category term="linux" scheme="http://wudaijun.com/categories/linux/"/>
    
    
    <category term="linux" scheme="http://wudaijun.com/tags/linux/"/>
    
  </entry>
  
  <entry>
    <title>游戏服务器难在哪？</title>
    <link href="http://wudaijun.com/2019/02/gs-difficulty/"/>
    <id>http://wudaijun.com/2019/02/gs-difficulty/</id>
    <published>2019-02-27T16:00:00.000Z</published>
    <updated>2019-05-08T07:50:32.000Z</updated>
    
    <content type="html"><![CDATA[<p>游戏服务器难吗？不就是处理和保存一些玩家数据么？本文谈谈我对这个问题的一些理解，或者是说，如果说游戏服务器开发很难，那么它究竟难在哪里？</p><h2 id="一-状态性"><a href="#一-状态性" class="headerlink" title="一. 状态性"></a>一. 状态性</h2><p>游戏服务器是后端，做后端的，每天耳濡目染横向扩展，自动伸缩等炫酷的特性，要说放在以前，这些特性还是巨头的”专利”，我们想要自己实现这些东西挑战性是比较大的，但近几年有了容器生态如k8s的加持，只要你实现了一个无状态应用，你几乎马上就可以得到一个可伸缩的集群，享受无状态本身带来的各种好处，机器挂了自动重启，性能不够了就自动扩展等等。而作为一名游戏服务器开发者，自然也想充分享受容器时代的红利，所以我们来捋捋无状态游戏服务器的可行性。</p><a id="more"></a><p>我们将游戏服务器的状态性分为连接状态性和数据状态性。</p><h3 id="1-连接状态性"><a href="#1-连接状态性" class="headerlink" title="1. 连接状态性"></a>1. 连接状态性</h3><p>连接的状态性比较好理解，即我们通常所说的长连接和短连接，游戏服务器通常使用TCP长连接，长连接有如下好处:</p><ul><li>时序性: 指对请求的顺序性保证，即客户端先发出的请求会被先处理，如果服务器是顺序一致性的，那么响应也满足顺序性。</li><li>状态性: 在连接建立时进行鉴权，之后这个连接的所有消息都附带上下文(如玩家ID，权限等)，而不用每次请求都带 Header。</li><li>服务器推送: 这个对游戏来说还是比较重要的，邮件/聊天/广播等功能都依赖于服务器主动推送。</li></ul><p>长连接也有一些问题:</p><ul><li>网络分区: 通常需要做专门的心跳检测/断线重连/网络分区恢复流程</li><li>限制了后端的扩展性: 如服务器透明重启，横向扩展等</li></ul><p>简单来讲，由于端到端的连接是绑定且应用层有感知的，不存在中间层，因此灵活性会受限，需要应用层做更多地事情，如心跳检测，断线重连，消息负载均摊等。</p><p>那么 GS 可以用短链接吗？这个得根据情形具体考虑，对于实时性要求不是很高的游戏，C/S的交互是可以做成短链接的:</p><ol><li>时序性: 客户端对消息时序性的要求没那么高，并且 HTTP 内部通常也是间隙性长连接的。</li><li>状态性: 这个也比较好解决，将认证Token，玩家ID这些上下文放到 HTTP Header，每几分钟重新认证一次。</li><li>推送: 这个可能稍微麻烦点，一种方案是借助 websocket 来实现。</li></ol><p>至于 GS 内部节点间的连接，主要考虑的还是时序性问题，比如DB，与它的连接就不可能是短链接，因为无法保证顺序性，连续两条Insert Delete消息如果顺序倒过来的话，可能造成很严重的后果。一些对顺序性要求不高的节点，如Auth这类偏服务性质的，则可以做成短链接。</p><h3 id="2-数据状态性"><a href="#2-数据状态性" class="headerlink" title="2. 数据状态性"></a>2. 数据状态性</h3><p>数据状态性是指，游戏服务器本身是否包含数据状态。通常 GS 都是会先将数据更新到内存中，再定期存盘，这意味着服务器内存数据状态和数据库中的数据状态有一定的不一致窗口，这就是所谓的数据状态。</p><p>从实现上来讲，无数据状态服务器的逻辑节点本身只是 Handler，真正的数据放到DB(或Redis缓存)等数据服务中，由于逻辑服务通常是不稳定的，而数据服务通常是相对稳定的，并且更容易做扩展或者主从，因此逻辑节点挂掉不会造成数据丢失或不一致，并且可以透明重启(暂不考虑连接状态)。</p><p>那么游戏服务器能否做成无数据状态呢，假设我们将所有的玩家/地图等数据全部存放到外部数据服务如Redis中，每次逻辑处理的时候从Redis 读出数据处理然后再写回。来看看可能产生什么问题。</p><h4 id="2-1-数据耦合"><a href="#2-1-数据耦合" class="headerlink" title="2.1 数据耦合"></a>2.1 数据耦合</h4><p>游戏服务器的特性之一就是数据关系灵活多变，比如一个使用道具的请求就可能涉及到道具，Buff，任务，活动等数据表的更新，即数据耦合重和单次请求相关数据量大，分表或者范式化只能针对前者做优化，而数据量大则会更多地影响服务器吞吐量以及数据服务的压力。</p><h4 id="2-2-数据不一致"><a href="#2-2-数据不一致" class="headerlink" title="2.2 数据不一致"></a>2.2 数据不一致</h4><p>无状态服务器的处理模型无非两种:</p><ul><li>串行模型: 所有的地图相关逻辑在一个线程中处理，优点是数据的一致性，缺点是串行瓶颈。这里其实还分同步和异步: 同步数据存取吞吐量不可接受，而异步数据存取写起来很痛苦，并且可能失去数据的一致性(依赖外部数据服务的一致性)。</li><li>并行模型: 每个请求在单独的线程中执行，优点是并发性和可扩展性(包括节点级的扩展)，缺点是数据一致性。当多个线程在读写同一块数据的时候，可能出现数据不一致，写丢失等问题。</li></ul><p>不用多说，并行处理模型是最优的方向，它能够充分运用无状态特性进行节点级的动态伸缩。但就游戏服务器而言，这一块通常是很难做的，因为游戏有很多类似地图，联盟这类并发访问的数据。而反观大多数的HTTP服务器，绝大部分时候都只是对自己的数据进行增删查改，需要并发读写的数据相对较少，并且在处理这些并发数据块的时候，通常的解决方案要么就是对这部分请求串行化，要么就是用数据服务提供的事务机制。但就游戏服务器而言，这类公共数据块太多，并且对性能的要求也较高，两种方案都不是很适合，需要逻辑层做更多的优化。</p><p>总结一下，游戏服务器的无状态主要受限于连接状态性和数据状态性，连接状态性比较好解决，数据状态则难得多，这是游戏数据的特性决定的，也是游戏服务器和传统HTTP服务器不同的地方。</p><p>由于有状态，那么扩展性就受限，就需要在逻辑层用并发，异步等各种手段来保证服务器的负载能力。而无法利用很多现成的中间件。</p><p>由于有状态，也就难以在意外宕机时透明重启，宕机后果也很严重: 数据不一致，补偿，玩家流失等。</p><p>由于有状态，数据调试也相对麻烦一些，比如要临时在线上执行一些数据修复或实时地数据整合。</p><h2 id="二-架构设计"><a href="#二-架构设计" class="headerlink" title="二. 架构设计"></a>二. 架构设计</h2><h3 id="1-节点拓扑"><a href="#1-节点拓扑" class="headerlink" title="1. 节点拓扑"></a>1. 节点拓扑</h3><p>通常的游戏服务器架构可以大致分为大服和小服两种，小服架构即按照逻辑服务器的概念来划分架构拓扑，最常见的情形是每个小服一个节点，DB也相互隔离。小服架构的优点是容易扩展，当量级起来后也不容易引发各种瓶颈问题。缺点是后期几百上千个服务器节点的运维成本高一些(尽管一些老服可能只有一两百个活跃玩家)，同时做一些批量操作不是很方便。大服架构是指从架构拓扑层面没有逻辑服的概念，而是更多地从功能划分的角度来纵向划分，如地图节点，联盟节点等，逻辑服只是运行于这些节点上的逻辑概念，就像玩家一样。大服架构的好处是运维成本更低，能够更好地利用物理资源，同时做跨服功能更方便，比如合服，开新服，跨服活动等，但缺点是大服架构通常更复杂，因为它面临的量级不一样，会更多地考虑瓶颈问题，当整个游戏有几万同时在线时，会对DB，单点带来不小的考验。</p><p>但不管是大服还是小服，随着游戏后期各种跨服活动，跨服战场开启，节点数量逐渐增多，节点间的连接也越来越多，节点拓扑变得复杂，以下问题就要纳入考虑:</p><ul><li>全联通: 从架构设计上来说，全联通是应该尽量避免的，因为全联通可能导致节点职责和关系不明确，调试困难，并且在某些情况下可能出现雪崩效应。在具体设计中，比如服务器A需要获取服务器B某个玩家的数据，不要让服务器A直连服务器B，而是让它通过第三方节点(跨服节点)去取，或者将这类跨服功能做到跨服节点上，该节点可能根据其业务特性还需要考虑扩展性以避免单点(比如做逻辑切割或设计成无状态的)。节点与节点之间的连接尽可能复用，可以借助grpc/HTTP2这类技术。另一方面，如果节点间的拓扑实在复杂且易变，也可以考虑借用MQ这类中间件来解耦节点依赖，同时也可以避免全联通。另外，在使用一些分布式组件时，也要考虑其全联通特性，如之前用Erlang做大服架构的时候用到了Mnesia，就会导致节点全联通，在架构上是一个隐患。</li><li>节点依赖: 全联通是拓扑复杂之后的连接状态，而从逻辑层的角度来说，这其实也映射出节点依赖问题，如活动服务区依赖玩家服务，玩家服务依赖联盟服务，联盟服务依赖玩家服务等，依赖有强弱之分，但不管是那种，环形依赖是不允许的，各类节点应该有清晰的依赖关系，以方便处理如启服停服这类操作的一致性问题。如果节点间是弱依赖，MQ可以一定程度简化拓扑关系，避免维护复杂的依赖状态。另外，尽可能地拆分那些无状态的服务，将其做成可扩展HTTP服务也是一个思路，毕竟不能全局适用无状态，在局部上用用也能得到一些好处。</li></ul><p>有些同学可能会说，为什么节点拓扑不在架构设计之初就考虑周全，确定下来。确实，一个好的架构师应该尽可能地保留架构的弹性和扩展性，但另一方面，游戏本身的多变性本身也是架构设计的一大挑战，今天策划跟你说我们按小服实现就可以，明天为了拉付费就设计一个跨服功能或活动也是常有的事。特别是对SLG这类游戏，到了后期，除了跨服玩法，通常很难刺激玩家的新鲜感。游戏玩法很多时候都是根据各种运营数据在调整。</p><h3 id="2-节点内部"><a href="#2-节点内部" class="headerlink" title="2. 节点内部"></a>2. 节点内部</h3><p>节点内部而言，通常是多线程(或轻量级线程)的，就线程交互而言，常用的是以下几种并发模式:</p><ul><li>并行: 交互性不强的逻辑尽可能并行，如每个玩家的网络线程</li><li>扇入: 如逻辑线程会接收来自Timer，网络，事件，以及其它线程投来的消息，类似多路复用</li><li>串行: 交互性强的逻辑通常是串行的，如地图线程</li><li>扇出: 如任务派发，发布订阅者模式等</li></ul><p>在线程交互上，也有一些实践细则，如:</p><ul><li>线程与线程之间慎用同步调用</li><li>尽可能将阻塞的事情放到单独的线程中去做，如网络IO，文件读写等</li><li>线程之间投递消息注意值语义，如果用共享内存则注意单写入者原则</li></ul><p>这类的设计细则跟具体情形相关，不再赘述，总的而言，主要是要有异步并发思维。</p><p>另外，不管对于节点间还是节点内，都尽量地异步交互，异步思维是后端开发的必备思维，特别对于游戏服务器来说，变更快速，测试很难完全覆盖，系统峰值难以预估，玩家(外挂)行为不可预测等都会带来不确定因素。我在<a href="http://wudaijun.com/2018/07/gs-async-programing/">这里</a>有详细的讨论。异步编程对开发者的要求本身也更高，同时也会进一步放大数据不一致性。</p><h2 id="三-快速迭代"><a href="#三-快速迭代" class="headerlink" title="三. 快速迭代"></a>三. 快速迭代</h2><p>游戏的需求变更和版本迭代速度很快，很多时候是一天一个版本，为了保证服务器的稳定性，我们需要:</p><ol><li>通过代码规范，测试流程，代码Review等提升代码质量</li><li>通过容器化，CI/CD，DevOps 来提升交付速度</li><li>做好相关的调试工具，监控措施，修复脚本等，接入完善的监控体系，并且具备基础的运维知识，保证对异常情况的快速响应</li></ol><p>这三个方面的内容这里不打算细讲，有的穿插在我的其它文章中，有的后续可能会单独谈谈。总的来讲，游戏后端开发需要掌握的知识栈更广一些，一方面要保证版本的快速迭代，另一方面也要保证交付质量。毕竟前面提到的状态性的问题导致服务器很脆弱，如果出现一些逻辑上的BUG，停服维护的代价是很大的，特别对于静态语言而言，有状态+无热更=如履薄冰。做很多功能的时候一方面要尽可能保证其正确性，另一方面也要考虑到其容错性，如果出错了如何监控/调试/修复。别到时候服务器出现问题了，重启一次来打印Log/上下文，再重启一次来修复Bug。或者是等到玩家已经利用该漏洞刷了大量道具，然后客服找到你，修了Bug还要修数据。</p><h2 id="四-总结"><a href="#四-总结" class="headerlink" title="四. 总结"></a>四. 总结</h2><p>本文从状态性，架构设计和快速迭代三个方面简单谈了谈自己的一些理解，其中状态性讲得比较多，因为我认为状态性是导致游戏服务器比常规HTTP服务器更复杂的直接原因之一，游戏服务器的另一个特性就是需求变更变速，版本迭代快，这需要更灵活地架构设计，更严格的软件工程实践。用一句经常跟同事开玩笑的话来说：唯一不变的就是变化，唯一可信的就是”这个地方不要写死，可能会改”。</p><p>当然，游戏架构的复杂度很大程度取决于游戏类型以及量级，对于一些简单的游戏类型，单线程足矣，而对更复杂的游戏，如果单线程不够，那就多线程，然后就涉及到同异步，数据一致性等问题。如果单节点还不够，那就多节点，那么又要考虑全联通，CAP，分布式一致性等。同样的游戏，不同的量级，也会是完全不同的架构设计。我也只是基于自己的实践谈谈自己的理解，限于所知所见，难免坐井观天。Keep Learning</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;游戏服务器难吗？不就是处理和保存一些玩家数据么？本文谈谈我对这个问题的一些理解，或者是说，如果说游戏服务器开发很难，那么它究竟难在哪里？&lt;/p&gt;
&lt;h2 id=&quot;一-状态性&quot;&gt;&lt;a href=&quot;#一-状态性&quot; class=&quot;headerlink&quot; title=&quot;一. 状态性&quot;&gt;&lt;/a&gt;一. 状态性&lt;/h2&gt;&lt;p&gt;游戏服务器是后端，做后端的，每天耳濡目染横向扩展，自动伸缩等炫酷的特性，要说放在以前，这些特性还是巨头的”专利”，我们想要自己实现这些东西挑战性是比较大的，但近几年有了容器生态如k8s的加持，只要你实现了一个无状态应用，你几乎马上就可以得到一个可伸缩的集群，享受无状态本身带来的各种好处，机器挂了自动重启，性能不够了就自动扩展等等。而作为一名游戏服务器开发者，自然也想充分享受容器时代的红利，所以我们来捋捋无状态游戏服务器的可行性。&lt;/p&gt;</summary>
    
    
    
    <category term="gameserver" scheme="http://wudaijun.com/categories/gameserver/"/>
    
    
    <category term="gameserver" scheme="http://wudaijun.com/tags/gameserver/"/>
    
  </entry>
  
  <entry>
    <title>函数式中的延迟计算及惰性求值</title>
    <link href="http://wudaijun.com/2019/02/lazy-evaluation/"/>
    <id>http://wudaijun.com/2019/02/lazy-evaluation/</id>
    <published>2019-02-16T16:00:00.000Z</published>
    <updated>2019-02-17T07:23:16.000Z</updated>
    
    <content type="html"><![CDATA[<p>本文谈谈在函数式编程中的延迟计算，惰性求值等技术及其应用。</p><h2 id="Racket"><a href="#Racket" class="headerlink" title="Racket"></a>Racket</h2><p>本文将以 Racket 为例，以下是 Racket 的概要:</p><ol><li>更广泛的函数概念: 什么 if define + myfunc 等，统统都是函数</li><li>前缀表达式: 同 Lisp, Scheme 等语言一样，Racket 使用前缀表达式，如(myfunc 1 2)，(+ 1 2 3)等</li><li>支持可变性: 可修改变量值(不建议)，如 (set! x 2)，并且支持修改 Pair，Map 的指定元素</li></ol><p>其它关于 Racket 的具体语法和 API 细节请参考<a href="https://github.com/OnRoadZy/RacketGuideInChinese">Raccket 中文文档</a>。</p><h2 id="Delay-Evaluation"><a href="#Delay-Evaluation" class="headerlink" title="Delay Evaluation"></a>Delay Evaluation</h2><p>Delay Evaluation 意为延迟计算，即表达式只在必要时才求值，而非被赋给某个变量时立即求值。</p><a id="more"></a><p>要理解延迟计算，我们先看一个小例子:</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">(define (my-if e1 e2 e3)</span><br><span class="line">    (if e1 e2 e3))</span><br><span class="line">    </span><br><span class="line">(define (factorial x)</span><br><span class="line">  (my-if (&#x3D; x 0)</span><br><span class="line">         1</span><br><span class="line">         (* x (factorial (- x 1)))))  </span><br></pre></td></tr></table></figure><p>上面的代码实现了一个 <code>my-if</code> 函数，它的作用和 if 一样，以及一个计算阶乘的函数 factorial，只不过使用 <code>my-if</code> 替代了 if，如果你运行<code>(factorial 1)</code>，将陷入死循环，因为在对 <code>(factorial 1)</code> 求值时，将会对<code>(my-if e1 e2 e3)</code>求值，而在大多数语言包括 Racket 中，实参表达式是在被传入时即会被求值的，因为 e3 中递归调用了 factorial，然后又会对 <code>(factorial 0)</code> 求值，如此递归，并且没有终止，因为要对 factorial 函数求值完成后，<code>my-if</code> 表达式才能求值，才能终止递归。而 if 则不同，它只有在 x 不为 0 时才会递归对 <code>(factorial (- x 1))</code> 求值，不会形成无限递归。</p><p>要解决这种情况，我们需要让 <code>my-if</code> 在求值时，先不对 <code>(factorial (- x 1))</code> 求值，等到 <code>my-if</code> 判断 e1 为 false(x 不等于 0)，即确实需要对e3求值时才求值，这就是延迟计算，因此我们需要将以上函数改成这样:</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">(define (my-if e1 e2 e3)</span><br><span class="line">    (if e1 (e2) (e3)))</span><br><span class="line">    </span><br><span class="line">(define (factorial x)</span><br><span class="line">  (my-if (&#x3D; x 0)</span><br><span class="line">         (lambda () 1)</span><br><span class="line">         (lambda () (* x (factorial (- x 1))))))</span><br></pre></td></tr></table></figure><p><code>my-if</code> 的参数 e2, e3 只是个未被求值的表达式，通过不带参的匿名函数代入，由于函数只会在被调用的时候才会被求值，从而达到延迟计算的目的。这种通过函数传入表达式本身而不是具体值从而达成延迟计算的方式有个专业的计算机术语叫做<a href="https://en.wikipedia.org/wiki/Thunk">Thunk</a>(形实转换程序)。Thunk 的关键点在于将裸表达式 <code>e</code> (会被立即求值)包装为 p: <code>(lambda() e)</code>(将表达式包裹在函数中)，并且在使用时调用 p: <code>(p)</code>(在需要时才计算表达式求值)。</p><p>有了 Thunk 之后，我们可以在某些情况下延迟函数参数的计算，但在另一些情况下，Thunk 反而会导致更多的计算:</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">; 未使用 Thunk, 通过累加的方式计算 n*e，n &gt;&#x3D; 1</span><br><span class="line"> (define (my-mult1 e n)</span><br><span class="line">  (cond [(&#x3D; n 0) 0]</span><br><span class="line">       [(&#x3D; n 1) e]</span><br><span class="line">       [#t (+ e (my-mult2 e (- n 1)))]))</span><br><span class="line">      </span><br><span class="line">; 使用 Thunk, 通过累加的方式计算 n*(e)，n &gt;&#x3D; 1</span><br><span class="line"> (define (my-mult2 e n)</span><br><span class="line">  (cond [(&#x3D; n 0) 0]</span><br><span class="line">       [(&#x3D; n 1) (e)]</span><br><span class="line">       [#t (+ (e) (my-mult2 e (- n 1)))]))</span><br></pre></td></tr></table></figure><p><code>my-mult1</code> 和 <code>my-mult2</code> 实现相同的功能，除了一个是直接传入表达式而另一个传入的是 Thunk，在这种情况下，<code>my-mult1</code> 只会计算表达式一次(即使 n==0)，而 <code>my-mult2</code> 会计算表达式 n 次(n==0时无需计算)。在 n 很大时，<code>my-mult1</code> 将明显优于 <code>my-mult2</code>。</p><p>因此实际上我们需要的是这样一种机制，它兼具 <code>my-mult1</code>(不会重复计算表达式) 和 <code>my-mult2</code>(延迟求值，表达式可能无需计算)的优点:</p><ol><li>延迟计算</li><li>避免重复计算，比如计算过一次之后就记住它的值</li></ol><p>这种机制被称为<em>惰性求值(Lazy Evaluation)</em>，天生具备惰性求值特性的语言称为<em>惰性语言(Lazy Language)</em>(如 Haskell)。。</p><h2 id="Lazy-Evaluation"><a href="#Lazy-Evaluation" class="headerlink" title="Lazy Evaluation"></a>Lazy Evaluation</h2><p>下面我们来尝试在 Racket 中自己实现惰性求值，延迟计算的 Thunk 技术前面已经介绍过，现在我们来考虑如何记住表达式的值，避免重复计算，一种可选的方案是在表达式上再封装一层，其中记录了表达式的计算状态和计算值，我们可以用一个 Pair 来记录，其第一个字段为表达式是否已经被计算(bool 值)，另一个字段为计算结果或Thunk。</p><p>在 Racket 中，Pair 分为可变和不可变两种，分别用 cons 和 mcons 构建，在这里，我们应该使用可变 Pair:</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">; 在 thunk 上再一次封装，加上计算状态</span><br><span class="line">(define (my-delay thunk)</span><br><span class="line">  (mcons #f thunk)) ; 将 thunk 封到 Pair 中，并初始化为未被计算(false)</span><br><span class="line"></span><br><span class="line">; 计算 thunk，如果已经被计算，则不会再被计算  </span><br><span class="line">(define (my-force p)</span><br><span class="line">  (if (mcar p) ; 查看表达式是否已经被计算</span><br><span class="line">    (mcdr p)   ; 如果已经被计算，则直接返回 Pair 第二个元素 </span><br><span class="line">    (begin (set-mcar! p #t) ; 如果未被计算，则标记为已计算</span><br><span class="line">          (set-mcdr! p ((mcdr p))) ; 计算 Pair 中的表达式(第二个元素)，执行计算，并将计算结果存为Pair第二个元素</span><br><span class="line">          (mcdr p)))) ; 直接返回 Pair 中第二个元素</span><br></pre></td></tr></table></figure><p>上面代码实现了两个函数 my-delay 和 my-force，与延迟计算中的<code>(lambda () e)</code> 和 <code>(e)</code> 类似，我们将表达式 e 变换为了p: <code>(my-delay (lambda () e))</code> 和 <code>(my-force p)</code>。现在我们可以用 my-delay 和 my-force 来优化 <code>my-mult2</code>，<code>my-mult2</code>无需任何修改，我们只需修改调用处:</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">; 以下两个函数调用均为计算 (+ (factorial 2) (factorial 2))，结果均为 4</span><br><span class="line">; 延迟调用版本，会计算(factorial 2) 两次</span><br><span class="line">&gt; (my-mult2 (lambda () (factorial 2)) 2)</span><br><span class="line">&gt; 4</span><br><span class="line">; 惰性求值版本(延迟调用+避免重复计算)，会计算(factorial 2) 一次</span><br><span class="line">&gt; (my-mult2 (let ([p (my-delay (lambda () (factorial 2)))])</span><br><span class="line">              (lambda () (my-force p))) 2)</span><br><span class="line">&gt; 4              </span><br></pre></td></tr></table></figure><p>惰性求值版本在最好情况下不会对表达式进行计算(n==0)，最差情况下也只会计算一次，兼具<code>my-mult1</code> 和 <code>my-mult2</code>的优点。</p><h2 id="Stream"><a href="#Stream" class="headerlink" title="Stream"></a>Stream</h2><p>延迟计算除了优化作用外，另一个应用就是 Stream(流)，Stream 是指一个无限大小的值队列，比如:</p><ol><li>用户输入事件: 如鼠标点击</li><li>Unix Pipes: <code>cmd1 | cmd2</code>，cmd2 从 cmd1 获取的处理结果，可能就是一个 Stream，如 cmd1为<code>tail -f ...</code></li><li>数学计算中，可能需要一个无穷大的数队列(如斐波那契队列)，消费者可以不断地获取下一个数进行处理</li></ol><p>因此生产者无法事先穷举表示出来，另一方面，用多少数据通常由消费者决定，我们也就没有必要事先计算出所有的数据。因此，可以通过延迟计算实现 Stream。</p><p>现在我们尝试用 Thunk 实现两个简单的整数队列:</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">; 实现一个全是1的Stream [1,1,1,...]</span><br><span class="line">; 正确版本</span><br><span class="line">(define ones (lambda () (cons 1 ones)))</span><br><span class="line">; 错误版本1: 编译错误，在 ones-bad1 定义中使用了 ones-bad1定义</span><br><span class="line">; (define ones-bad1 (cons 1 ones-bad1))</span><br><span class="line">; 错误版本2: 无限递归，ones-bad2的求值需要对ones-bad2求值，没有终止</span><br><span class="line">; (define ones-bad2 (lambda () (cons 1 (ones-bad2))))</span><br><span class="line"></span><br><span class="line">; 实现一个递增的自然数Stream [1,2,3,...]</span><br><span class="line">; 辅助函数，f(x)，返回Pair: (x, Thunk-of-f(x+1))</span><br><span class="line">(define (f x) (cons x (lambda () (f (+ x 1)))))</span><br><span class="line">; 将 f(1) 封装为Thunk</span><br><span class="line">(define nats (lambda () (f 1)))</span><br></pre></td></tr></table></figure><p>以下是使用示例:</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">&gt; (nats) ; 调用 nats 将返回一个 Pair，第一个元素是当前获取到的值，第二个元素是一个新的 Stream</span><br><span class="line">&#39;(1 . #&lt;procedure:...era&#x2F;racket&#x2F;a.rkt:29:22&gt;)</span><br><span class="line">&gt; (car (nats)) ; 取出第一个自然数</span><br><span class="line">1</span><br><span class="line">&gt; (car ((cdr (nats)))) ; 取出第二个自然数</span><br><span class="line">2</span><br></pre></td></tr></table></figure><p>上面实现了两个Thunk, ones 和 nats，前者没有内部状态(每次返回值是一样的)，后者有内部状态。那么 nats 是如何保存状态的呢？函数式也可以有内部状态？不，函数式中的函数当然没有内部状态，状态是通过辅助函数 f 的参数来传递的，第 n 次调用 nats 会返回一个新的 f(n+1) Thunk，对外表现为调用 Stream 返回的 Pair 的第二个元素始终都是个Thunk(无参匿名函数)，但其实 Stream 每次返回的 Thunk 都是不一样的(封装了不同的 f 调用)，这也是为什么 Stream 每次都要返回 Thunk 的原因。</p><p>如果是非函数式语言如 C，我们可以很方便地实现一个 <code>Counter()</code> 函数，通过静态局部变量来保存当前计数，而无需返回一个新的函数。但同时也失去了函数式的诸多好处，如延迟计算，因为程序员和编译器都很难判断<code>Counter()</code>是否有副作用(或者以后会改为具备副作用，比如修改了全局变量)，也就不能保证延迟计算是无痛的。</p><h2 id="Memoization"><a href="#Memoization" class="headerlink" title="Memoization"></a>Memoization</h2><p>我最初接触<a href="https://zh.wikipedia.org/wiki/%E8%AE%B0%E5%BF%86%E5%8C%96">Memoization</a>是在动态规划中，指的是如果一个问题具有最优子结构，并且会对多个子问题重复计算，那么我们应该将子问题的结果保存下来(Memoization)，避免对子问题的重复计算。Memoization 适用于很多使用递归并且会有大量重复计算的问题，比如背包问题，找零钱问题，最短路径问题，斐波那契数列问题等等。</p><p>这里我们以最简单的斐波那契数列为例，以下是几个求第 n 个斐波那契数的函数:</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br></pre></td><td class="code"><pre><span class="line">; fib1: 原始方案，直接自上而下递归</span><br><span class="line">(define (fib1 x)</span><br><span class="line">  (if (or (&#x3D; x 1) (&#x3D; x 2))</span><br><span class="line">      1</span><br><span class="line">      (+ (fib1 (- x 1))</span><br><span class="line">         (fib1 (- x 2)))))</span><br><span class="line"></span><br><span class="line">; fib2: 自下而上递归，避免子问题重复计算</span><br><span class="line">; 将子问题的计算结果通过函数参数传到上层问题，思路类似于迭代</span><br><span class="line">(define (fib2 x)</span><br><span class="line">  (letrec ([f (lambda (acc1 acc2 y)</span><br><span class="line">                (if (&#x3D; y x)</span><br><span class="line">                    (+ acc1 acc2)</span><br><span class="line">                    (f (+ acc1 acc2) acc1 (+ y 1))))])</span><br><span class="line">    (if (or (&#x3D; x 1) (&#x3D; x 2))</span><br><span class="line">        1</span><br><span class="line">        (f 1 1 3))))</span><br><span class="line"></span><br><span class="line">; fib3: 自上而下递归，使用我们前面的 my-delay my-force 技术</span><br><span class="line">(define (fib3 x)</span><br><span class="line">  (my-delay (lambda () (if (or (&#x3D; x 1) (&#x3D; x 2))</span><br><span class="line">      1</span><br><span class="line">      (+ (my-force (fib3 (- x 1)))</span><br><span class="line">         (my-force (fib3 (- x 2))))))))</span><br><span class="line"></span><br><span class="line">; fib4: 自上而下递归，使用 Memoization 技术</span><br><span class="line">; 用一个 map 保存计算过的子问题结果</span><br><span class="line">(define fib4</span><br><span class="line">  (let ((memo (make-hash &#39;((0 . 0) (1 . 1))))) ; 创建 map，key 为fib4函数参数 n</span><br><span class="line">    (lambda (n)</span><br><span class="line">      (unless (hash-has-key? memo n) ; 查看 memo map，是否已经计算过该子问题</span><br><span class="line">        (hash-set! memo n (+ (fib4 (- n 1)) (fib4 (- n 2))))) ; 如果没有计算过，则计算，并将结果存入 memo map</span><br><span class="line">      (hash-ref memo n)))) ; 此时肯定已经计算过了，直接取出 memo map 中的结果</span><br><span class="line"></span><br><span class="line">; 测试代码执行时间</span><br><span class="line">&gt; (time (fib1 35))</span><br><span class="line">cpu time: 518 real time: 519 gc time: 0</span><br><span class="line">9227465</span><br><span class="line">&gt; (time (fib2 35))</span><br><span class="line">cpu time: 0 real time: 0 gc time: 0</span><br><span class="line">9227465</span><br><span class="line">&gt; (time (my-force(fib3 35)))</span><br><span class="line">cpu time: 2784 real time: 2957 gc time: 912</span><br><span class="line">9227465</span><br><span class="line">&gt; (time (fib4 35))</span><br><span class="line">cpu time: 0 real time: 0 gc time: 0</span><br><span class="line">9227465</span><br></pre></td></tr></table></figure><p>现在我们来分析测试结果，fib1 和 fib2 的结果没什么意外，毕竟原始的 fib1 函数是一个指数增长的函数，其复杂度为即O(2^n)，而 fib2 通过自下而上求解，用迭代的思路将其优化为 O(n)，提升效果明显。 </p><p>fib3 的运行时间有些意外，它竟然比原始的 fib1 还慢了数倍，my-delay 和 my-force 不是只会计算一次结果么？需要注意的是，我们的 fib3 虽然用了 my-delay，但是却没有真正实现避免重复计算，因为我们没有将 <code>my-delay(fib(x))</code> 作为所有递归 <code>fib(x)</code> 的替代， 而是在每次调用 <code>fib(x)</code> 是生成了一个新的my-delay，已有的计算过的 <code>my-delay(fib(x))</code> 并没有保存下来，因此仍然会递归再次求解，再加上 my-delay 和 my-force 本身的开销，也就出现了比 fib1 还慢的结果。严格意义上说，fib3 不只是没有避免重复计算，就连延迟计算的优势也没有发挥出来，因为 my-force 总是在封装返回后立即被 my-force 使用。</p><p>fib4 通过 Memoization 技术来避免重复计算，性能表现和 fib2 一样优秀，当然，实际上会比 fib2 慢一些，毕竟多了 hash map 存取。但 fib2 本身是基于斐波那契数列特性的优化，对类似找零钱问题，背包问题等，自下而上的递归就不那么好用了，并且也会传递更多的计算上下文，而 Memoization 贵在它是一种更通用，甚至可以做进编译器的优化方案，具备更好的可读性和扩展性。</p><p>Memoization 和惰性求值虽然都可以避免重复计算，但惰性求值是依靠将单个表达式封装后的结果(如 my-delay 的 Pair)到处传递来实现的，Memoization 则考虑在表达式外部的上下文(闭包)中，对计算结果进行保存，因此对复杂的问题来说，实现 Memoization 要比惰性求值更简单直观。</p><h2 id="Lazy-Language"><a href="#Lazy-Language" class="headerlink" title="Lazy Language"></a>Lazy Language</h2><p>我们前面讨论的惰性求值都是以在 Racket 中手动实现来阐述的，而事实上像柯里化一样，某些语言可能内置惰性求值，这类语言被称为惰性语言。最出名的惰性语言如 Haskell，在 Haskell 中你可以用 <code>[1..]</code> 来表示1到无穷大的列表，当你需要时，可以通过<code>[1..] !! 999</code>取出其中第1000个元素(值即为1000)。另外，假如我有一个列表 xs，doubleMe 会将 xs 中的元素<code>*2</code> ，那么在Haskell中，<code>doubleMe(doubleMe(doubleMe(xs)))</code> 只会遍历列表一次。</p><p>另外，Racket 语言虽然不是天生的惰性语言，和 Scheme 一样，它也通过内置 delay 和 force 来支持手动惰性，并且使用比my-delay 更简单快捷:</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">&gt; (define laz (delay (fib1 10))) ; delay 后面只需跟表达式即可，无需是个 Thunk</span><br><span class="line">&gt; laz</span><br><span class="line">#&lt;promise:laz&gt;</span><br><span class="line">&gt; (promise? laz) ; 判断 laz 是否是个 promise，即惰性表达式</span><br><span class="line">#t</span><br><span class="line">&gt; (force laz)</span><br><span class="line">55</span><br></pre></td></tr></table></figure><p>有意思的是，Racket 将 delay 封装后的惰性表达式(类似my-delay返回的 Pair) 称作 promise，我猜它的意思是，我在创建时不会立即求值，但是我给你个”承诺”，当你需要值时我会计算给你，并且内部保证不会重复计算。我在<a href="http://wudaijun.com/2018/07/web-async-programing/">Web 中的异步编程</a>一文中提到，JS 异步编程中，用于保存异步执行结果，状态以及回调的结构体也叫 Promise，它的意思是: 你发起一个异步调用，我承诺将来一定会给出个结果(成功/失败)，你挂载上去的回调函数我也会在将来结果揭晓后进行调用。这两个 Promise 的”命名撞衫”非常有意思，它们虽然出自不同的语言，用于不同的领域，但都有延迟意思，只不过本文的 delay 由创建者决定何时取值，而 JS 中的 Promise 由异步过程决定何时返回值。</p><h2 id="At-Last"><a href="#At-Last" class="headerlink" title="At Last"></a>At Last</h2><p>前面的讨论各种技术本质上是为了延迟计算和避免重复计算，这些技术绝大部分(除了 Memoization) 都只应用在函数式语言中，因为只有函数式语言才能更好地确保函数没有副作用，从而确保函数求值的时机和次数对整个计算的正确性不会有影响，更进一步甚至将这些技术做到了语言内部。而对于过程式来说，由于其本身的可变性和副作用，这方面的路还很长。本文用 Racket 实现的 my-delay 和 Memozation 技术都用到了可变语义，因为我们需要这样一个上下文来辅助我们优化计算，使用了这些可变语义的函数从计算语义上来说仍然是”无副作用的”(PS: 严格意义上的无副作用不存在，CPU，内存都是有状态的全局上下文)。这主要是指，虽然函数有了自己的状态(通过闭包实现)，但对函数使用者来说，从计算结果上来讲并无区别。</p><p>换句话说，过程式羡慕函数式的无状态无副作用带来的优化空间和健壮性，而函数式有时候也未尝不羡慕过程式可变性带来的灵活性和便利。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;本文谈谈在函数式编程中的延迟计算，惰性求值等技术及其应用。&lt;/p&gt;
&lt;h2 id=&quot;Racket&quot;&gt;&lt;a href=&quot;#Racket&quot; class=&quot;headerlink&quot; title=&quot;Racket&quot;&gt;&lt;/a&gt;Racket&lt;/h2&gt;&lt;p&gt;本文将以 Racket 为例，以下是 Racket 的概要:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;更广泛的函数概念: 什么 if define + myfunc 等，统统都是函数&lt;/li&gt;
&lt;li&gt;前缀表达式: 同 Lisp, Scheme 等语言一样，Racket 使用前缀表达式，如(myfunc 1 2)，(+ 1 2 3)等&lt;/li&gt;
&lt;li&gt;支持可变性: 可修改变量值(不建议)，如 (set! x 2)，并且支持修改 Pair，Map 的指定元素&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;其它关于 Racket 的具体语法和 API 细节请参考&lt;a href=&quot;https://github.com/OnRoadZy/RacketGuideInChinese&quot;&gt;Raccket 中文文档&lt;/a&gt;。&lt;/p&gt;
&lt;h2 id=&quot;Delay-Evaluation&quot;&gt;&lt;a href=&quot;#Delay-Evaluation&quot; class=&quot;headerlink&quot; title=&quot;Delay Evaluation&quot;&gt;&lt;/a&gt;Delay Evaluation&lt;/h2&gt;&lt;p&gt;Delay Evaluation 意为延迟计算，即表达式只在必要时才求值，而非被赋给某个变量时立即求值。&lt;/p&gt;</summary>
    
    
    
    <category term="programing" scheme="http://wudaijun.com/categories/programing/"/>
    
    
    <category term="programing" scheme="http://wudaijun.com/tags/programing/"/>
    
  </entry>
  
  <entry>
    <title>GS 中的 Mesh 寻路</title>
    <link href="http://wudaijun.com/2019/02/gs-mesh-nav/"/>
    <id>http://wudaijun.com/2019/02/gs-mesh-nav/</id>
    <published>2019-02-06T16:00:00.000Z</published>
    <updated>2019-02-17T07:24:07.000Z</updated>
    
    <content type="html"><![CDATA[<p>本文简单讨论如何在服务端实现基于三角形网格的寻路算法。</p><h3 id="地图表示"><a href="#地图表示" class="headerlink" title="地图表示"></a>地图表示</h3><h4 id="1-基于路点"><a href="#1-基于路点" class="headerlink" title="1. 基于路点"></a>1. 基于路点</h4><p>基于路点是最原始的寻路方案，比如要让你实现一个迷宫，你会很自然地想到用0代表通路，1代码障碍物，整个迷宫地图由0和1构成的点阵来表示。这就是基于路点，本质上是将地图上可以通过的位置(坐标状态)都记录下来。</p><a id="more"></a><p><img src="/assets/image/201902/nav_point_1.jpeg" alt=""></p><center> 图1 基于路点的地图 </center><h4 id="2-基于网格"><a href="#2-基于网格" class="headerlink" title="2. 基于网格"></a>2. 基于网格</h4><p>基于网格是指对地图进行预处理，将地图边界拐点和障碍物外围拐点所形成的离散点集合转换为一个由多边形(通常是三角形)构成的网格，然后去掉障碍物内部的多边形，剩下即为所有可达地点的网格，称作寻路网格。或者换个角度来讲，将地图上的障碍物和边界之外去掉，然后将剩下的部分通过多边形切割，得到寻路网格。</p><p><img src="/assets/image/201902/nav_mesh_1.jpeg" alt=""></p><center> 图2 基于网格的地图 </center><p>以下简单对比基于路点和基于网格的寻路:</p><ol><li>基于路点要比基于网格存储更多的信息</li></ol><p><img src="/assets/image/201902/nav_point_2.jpeg" alt=""></p><center> 图3 基于路点存储的数据量 </center><p><img src="/assets/image/201902/nav_mesh_2.jpeg" alt=""></p><center> 图4 基于路点存储的数据量 </center><ol><li>基于路点寻路，即使没有障碍物，也可能走折线</li></ol><p><img src="/assets/image/201902/nav_point_3.jpeg" alt=""></p><center> 图5 基于路点寻路，从 A 点到 B 点 </center><p><img src="/assets/image/201902/nav_mesh_3.jpeg" alt=""></p><center> 图6 基于网格寻路，从 A 点到 B 点 </center><ol><li><p>基于网格可以更好地处理平滑转弯，角色大小等细节，如体型过大的角色可能无法通过某个小通道</p></li><li><p>基于网格可以通过网格保存更多的地表信息，如水中移动速度更慢，可用于寻路算法计算权重</p></li></ol><h3 id="寻路算法"><a href="#寻路算法" class="headerlink" title="寻路算法"></a>寻路算法</h3><p>不管是基于网关还是基于路点，最终的地图都是图结构，只不过基于路点的图结构基本单位是点，而基于网格的基本单位是网格。基于路点的寻路比较简单，通常通过 A* 或 Dijkstra 算法即可直接算出。这里讨论基于三角形的网格寻路。</p><h4 id="Step-1-导出网格数据"><a href="#Step-1-导出网格数据" class="headerlink" title="Step 1. 导出网格数据"></a>Step 1. 导出网格数据</h4><p>网格地图需要对地图进行预处理生成地图元数据，这里不讨论生成过程，假设我们已经得到了地图元数据，其中包含地图上所有的三角形信息。</p><h4 id="Step-2-数据预处理"><a href="#Step-2-数据预处理" class="headerlink" title="Step 2. 数据预处理"></a>Step 2. 数据预处理</h4><p>预处理有两个目的:</p><ol><li>给定一个点，能快速找到其所在的三角形</li><li>给定一个三角形，能快速索引到与其相连的其它三角形</li></ol><p>第一点可以通过将整个地图(假设为方形)组织成四叉树，起初所有的三角形都在根节点下，当节点下的三角形超过某个阈值时，将该节点进一步分为四个节点，各个节点保存其 Rect 区域内的所有三角形，那些在边界处的三角形(存在于多个 Rect 区域内)，则继续挂在根节点上，这样在查找点时，可通过该点是否在节点区域内不断递归缩小查找范围，找到所在三角形。</p><p>如下图，我们将三角形网格地图分为了四部分，当我们要寻找 A 点所在的三角形时，只需遍历左上角黑线包围的三角形即可，在地图较大，四叉树层数较深时，能够极大优化三角形查找。</p><p><img src="/assets/image/201902/nav_mesh_quad_tree.jpg" alt=""></p><center> 图7 组织为四叉树的网络地图 </center><p>现在来解决如何存储三角形相邻关系，如果将三角形看做点，本质上我们要做的其实是要保存一张图，保存图的方式通常有邻接矩阵，邻接图等，一个地图可能有上万网格，而每个三角形相邻的三角形通常是个位数，在这种情况下，用邻接表是个不错的选择，具体的实现不再罗列，本质上该部分预处理数据提供网格的通路信息。</p><h4 id="Step-3-寻路"><a href="#Step-3-寻路" class="headerlink" title="Step 3. 寻路"></a>Step 3. 寻路</h4><p>到这里我们开始阐述寻路的具体过程，我们有了以上的预处理数据，并且给定地图上的两个点 A，B，求 A, B 之间的寻路路径:</p><p><img src="/assets/image/201902/nav_mesh_demo.jpg" alt=""></p><center> 图8 找 A 点到 B 点的寻路路径 </center><p>我们将寻路算法分为如下步骤:</p><h5 id="3-1-找到-A-B-两点所在的三角形"><a href="#3-1-找到-A-B-两点所在的三角形" class="headerlink" title="3.1 找到 A, B 两点所在的三角形"></a>3.1 找到 A, B 两点所在的三角形</h5><p>基于前面我们创建的四叉树，我们可以得到 A,B 两点所在的三角形</p><h5 id="3-2-找到两个三角形之间的最短三角形路径"><a href="#3-2-找到两个三角形之间的最短三角形路径" class="headerlink" title="3.2 找到两个三角形之间的最短三角形路径"></a>3.2 找到两个三角形之间的最短三角形路径</h5><p>前面提到，如果将三角形看做基本单位，那么我们的网格图本质上就是个无向图，基于图的寻路算法主要有 A* 和 Dijkstra 算法，通常启发式的 A* 是更好地选择，我们可以用三角形的中心点(三条边中线的交点)到目标三角形的中心点的距离作为启发函数 h值。</p><h5 id="3-3-将三角形路径转换为最短线路径"><a href="#3-3-将三角形路径转换为最短线路径" class="headerlink" title="3.3 将三角形路径转换为最短线路径"></a>3.3 将三角形路径转换为最短线路径</h5><p>假设我们现在得到了A，B 间的网格路径:</p><p><img src="/assets/image/201902/nav_mesh_grid_path.jpg" alt=""></p><center> 图9 A B 两点间的网格路径 </center><p>现在我们要将其转换为角色最终行走的线路径，一种简单的算法是直接将沿途三角形的中心点联结起来，得到如上图所示的线路，但一来是路线存在不必要的折现，不是最短线路。二来返回给客户端的点过多，不适合作为服务器寻路结果。理想的最短路线如下图所示:</p><p><img src="/assets/image/201902/nav_mesh_line_path.jpg" alt=""></p><center> 图10 A B 两点间的最短线路径 </center><p>其中只有三个拐点，我们只需要将这三个拐点返回给客户端即可(这里不讨论平滑转弯)。接下来我们来尝试将网格路径推演为最短线路径。</p><p>首先我们知道，拐点必然出现在三角形路径上某两个三角形共同边的端点，因此我们尝试遍历三角形路径上所有的共同边，得到拐点。如上图所示，A，B 两点间一共有9条共同边，我们称这9条边为临边或者穿出边，我们以 A 点为起点，对第一条临边作两个向量:</p><p><img src="/assets/image/201902/nav_mesh_line_path_1.jpg" alt=""></p><p>我们将这两个向量通过向量叉乘的方式分为左向量和右向量，设为 vLeft1，vRight1，我们可以非正式地将 vLeft1 和 vRight 1之间的夹角看做角色的当前视野(之后我们用<code>&#123;vLeft, vRight&#125;</code>表示)。然后对第二条临边做向量，vLeft2 和 vRight2，发现新视野{vLeft2，vRight2}在当前视野{vLeft1，vRight1}之间，因此更新当前视野为{vLeft1, vRight2}:</p><p><img src="/assets/image/201902/nav_mesh_line_path_2.jpg" alt=""></p><p>同样，继续从出发点对第三条临边锻炼做向量得到新视野 {vLeft3, vRight3}，仍然在当前视野{vLeft1, vRight2}内，如此迭代:</p><p><img src="/assets/image/201902/nav_mesh_line_path_3.jpg" alt=""></p><p><img src="/assets/image/201902/nav_mesh_line_path_4.jpg" alt=""></p><p>直到我们对第五条临边做向量得到的视野{vLeft4, vRight4}，发现vRight4不在当前视野{vLeft1, vRight4}内，即视野不是包含关系:</p><p><img src="/assets/image/201902/nav_mesh_line_path_5.jpg" alt=""></p><p>此时忽略第五条临边，当前视野不变，仍然是{vLeft1, vRight4}，继续对第六条临边作视野:</p><p><img src="/assets/image/201902/nav_mesh_line_path_6.jpg" alt=""></p><p>发现新的视野{vLeft6, vRight6}在当前视野{vLeft1, vRight4}左侧，没有重叠部分，即 vLeft6 和 vRight6 均在 vLeft1 的左侧，换句话说，我们无法从起点直接到达第6条临边，需要在某个点转弯，这个点就是拐点，也就是这里的 Left1 的非起点端点。此时记录该拐点 C，将当前起点从 A 点更新到 C 点，重新找到与 C 点不相邻的第一条临边，也就是整条路径的第5条临边，重新初始化当前视野，递归寻找从 C 点到达 B 的路径拐点。</p><p><img src="/assets/image/201902/nav_mesh_line_path_7.jpg" alt=""></p><p>如此递归，直到找到最后一个拐点P:</p><p><img src="/assets/image/201902/nav_mesh_line_path_8.jpg" alt=""></p><p>从 P 点的视野可以直接连到最后一个三角形，也就是与目标点B所在的三角形，由于 B 在 P 点的视野内，因此 P，B 可直接连线到达。如果 B 点不在 P 的视野内，则还需一个拐点，通常我们将目标点作为一条特殊的边来处理即可，即长度为0的边，这样可以用同样的拐点算法来计算。至此，我们就得到了 A, B 两点间的最短路径。</p><h3 id="其它细节"><a href="#其它细节" class="headerlink" title="其它细节"></a>其它细节</h3><p>到此我们就得到了基于拐点的寻路路径，本文只讨论了大概思路，实际上寻路还有很多有意思的问题值得探讨，如:</p><ol><li>如何在寻路时考虑到单位的半径？Tips: 在寻路算法找出三角形路径时，比较三角形的临边和角色大小</li><li>如何在寻路时考虑到各种地形的权重？Tips: 将地形信息保存在三角形网格中，在 A* 算法的代价函数中考虑三角形权重</li><li>如何动态更新寻路？Tips: 可选方案: a. 定时检查 b. 每 N 步检查一次 c. 有障碍物时触发相关寻路更新 d. 寻路局部更新优化</li></ol><p>由于懒且画工不好，本文图片均出自以下参考文章:</p><ol><li><a href="https://zhuanlan.zhihu.com/p/24112879">https://zhuanlan.zhihu.com/p/24112879</a></li><li><a href="http://gad.qq.com/article/detail/10042">http://gad.qq.com/article/detail/10042</a></li></ol>]]></content>
    
    
    <summary type="html">&lt;p&gt;本文简单讨论如何在服务端实现基于三角形网格的寻路算法。&lt;/p&gt;
&lt;h3 id=&quot;地图表示&quot;&gt;&lt;a href=&quot;#地图表示&quot; class=&quot;headerlink&quot; title=&quot;地图表示&quot;&gt;&lt;/a&gt;地图表示&lt;/h3&gt;&lt;h4 id=&quot;1-基于路点&quot;&gt;&lt;a href=&quot;#1-基于路点&quot; class=&quot;headerlink&quot; title=&quot;1. 基于路点&quot;&gt;&lt;/a&gt;1. 基于路点&lt;/h4&gt;&lt;p&gt;基于路点是最原始的寻路方案，比如要让你实现一个迷宫，你会很自然地想到用0代表通路，1代码障碍物，整个迷宫地图由0和1构成的点阵来表示。这就是基于路点，本质上是将地图上可以通过的位置(坐标状态)都记录下来。&lt;/p&gt;</summary>
    
    
    
    <category term="gameserver" scheme="http://wudaijun.com/categories/gameserver/"/>
    
    
    <category term="gameserver" scheme="http://wudaijun.com/tags/gameserver/"/>
    
  </entry>
  
  <entry>
    <title>GS 中的 ACID 一致性</title>
    <link href="http://wudaijun.com/2019/01/gs-acid-consistency/"/>
    <id>http://wudaijun.com/2019/01/gs-acid-consistency/</id>
    <published>2019-01-10T16:00:00.000Z</published>
    <updated>2019-03-20T15:41:16.000Z</updated>
    
    <content type="html"><![CDATA[<p>前段时间又和同事讨论到 GS 中的 ACID 一致性，在这里唠叨几句。ACID 一致性即系统内部的数据一致性，而非分布式系统对外体现的一致性(CAP 中的 C)。</p><p>假设我们有一个业务逻辑叫做行军，在单线程下，其逻辑如下:</p><a id="more"></a><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">if !check_money(cost) &#123;</span><br><span class="line">    return error_money_not_enough</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">if !check_march(troopId) &#123;</span><br><span class="line">    return error_troop_can_not_march</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">deduct_money(cost)</span><br><span class="line">start_march(troopId)</span><br></pre></td></tr></table></figure><p>如果现在我们由于性能问题，将 Play(玩家数据逻辑) 和 Map(大地图玩法) 分为了两个 Actor (如goroutine,线程,节点)，玩家金币由 Play Actor 管理，部队数据由 Map Actor 管理，那么我们现在的逻辑变成了分布式中最常见的 Check-Do 模型:</p><div class="table-container"><table><thead><tr><th>Play</th><th>Map</th></tr></thead><tbody><tr><td>check_money</td><td>check_march</td></tr><tr><td>deduct_money</td><td>start_march</td></tr></tbody></table></div><p>现在我们讨论如何在这种情形下尽可能提升数据一致性，假设 Play 和 Map 以异步消息的方式交互，然后我们来考虑如下执行流:</p><p>执行流A:</p><div class="table-container"><table><thead><tr><th>Steps</th><th>Play</th><th>Map</th></tr></thead><tbody><tr><td>1</td><td>check_money</td><td></td></tr><tr><td>2</td><td>deduct_money</td><td></td></tr><tr><td>3</td><td></td><td>check_march</td></tr><tr><td>4</td><td></td><td>start_march</td></tr></tbody></table></div><p>该执行流的异步交互少(理想情况下只需要一次)，但问题显而易见，当执行到<code>check_march</code>检查失败时，需要回滚扣除的金币，即再向 Play 发消息将扣除的钱加回来。如果考虑到回滚操作(业务逻辑的 Check 是很可能失败的)，该模型并不简单，并且可能如对玩家可能带来一些不好的体验(比如看着钱扣除了又增加了)。</p><p>为了减少数据回滚的可能性，我们需要遵循第一条 Rule: 先 Check 再 Do，</p><p>执行流B:</p><div class="table-container"><table><thead><tr><th>Steps</th><th>Play</th><th>Map</th></tr></thead><tbody><tr><td>1</td><td>check_money</td><td></td></tr><tr><td>2</td><td></td><td>check_march</td></tr><tr><td>3</td><td></td><td>start_march</td></tr><tr><td>4</td><td>deduct_money</td></tr></tbody></table></div><p>这个执行流稍微要复杂一些，但通过先 check 再 do 的方式避免了逻辑检查(<code>check_march</code>)导致需要回滚的问题。但异步交互本身的不一致问题仍然存在，比如如果在行军逻辑之后，Play 立马收到了一条购买消息，然后在 Step 1-4之间，Play 处理了这条消息，扣除了金币，导致行军扣除金币时，金币不够了，此时就麻烦了: 玩家做了事，但没扣(够)钱，回滚行军的代价也可能很大(体力回滚，广播，任务统计等等)，因此，我们可以梳理出第二条 Rule: 先 Deduct 再 Do。</p><p>执行流C:</p><div class="table-container"><table><thead><tr><th>Steps</th><th>Play</th><th>Map</th></tr></thead><tbody><tr><td>1</td><td>check_money</td><td></td></tr><tr><td>2</td><td></td><td>check_march</td></tr><tr><td>3</td><td>deduct_money</td><td></td></tr><tr><td>4</td><td></td><td>start_march</td></tr></tbody></table></div><p>现在这个执行流异步交互最复杂，如果 Step 1,3 发生不一致，Step 3失败，行军逻辑无法继续。但如果 Step 2,4 发生不一致，Step 4失败，此时金币已经扣除，可以通过 Step 5 发消息给 Play 把金币加回来，也可以通过日志手动 Fix(当逻辑回滚比较复杂时)。</p><p>到目前为止，我们来理理前面提到的:</p><ol><li>先 Check 再 Do，避免逻辑数据检查导致的不一致</li><li>先 Deduct 再 Do，保证数据安全性(如玩家刷道具)以及回滚的可行性</li><li>简单的数据不一致可以逻辑回滚，复杂的数据不一致通过日志来手动修复</li></ol><p>下面是你可能会问的几个问题:</p><p>Q1. 为什么不通过事务来保证一致性？</p><p>在分布式中，常见的2PC，3PC都不能完全解决分布式一致性问题，2PC 其实和我们的执行流C有点类似，都是先询问各个参与者(Play, Map)是否可以提交(CanCommit)，再执行提交(DoCommit)。事实上，2PC，3PC 都不能完全解决分布式中的一致性问题，并且会引入更多的复杂度，如协调者单点，超时机制，锁等。随着业务逻辑变动频繁，并发实体不断增多，想要保证任何操作的事务性是非常困难且难以维护的。因此在游戏服务器中，通常我们认为可用性大于一致性，对于重要的逻辑，如充值，购买，可以做尝试逻辑回滚，但对于大部分的普通逻辑，我们是选择忍受这部分不一致性的。</p><p>Q2. 为什么不用同步调用？</p><p>为了保证<code>check_money</code>和<code>deduct_money</code>，以及<code>check_march</code>和<code>start_march</code>的不一致性，我们可以让 Map <code>check_march</code> 后，直接同步调用 Play 的<code>deduct_money</code>，然后根据扣除是否成功执行后续操作。这样就像写同步代码一样避免了不一致性。然而同步调用可能会带来更多的问题，我在<a href="http://wudaijun.com/2018/07/gs-async-programing/">GS中的异步编程</a>中有详细讨论。</p><p>Q3. 关于异步超时?</p><p>考虑这样一种情况，当执行流C Step3 <code>deduct_money</code>之后，Map 因为各种原因(网络波动，甚至节点挂掉)没有处理到 <code>start_march</code> 这条消息，然后整个执行流就断掉了，就没有下文了(这也是2PC 协调者单独存在的意义)。那么我们是否应该给异步调用一个超时，让发起者可以对对端无响应有所感知加以处理？我们目前的处理方式是，节点内的 Actor 交互无需超时机制，节点间的异步交互通过 gRPC 超时来做。我认为超时机制适用于 RPC 这种机制，而不适用于异步消息，因为异步消息本身就不包含调用上下文语义，同样的消息可能在不同的场景对超时有不同的需求(或者不需要超时)。</p><p>Q4. 通过更细粒度的 Actor 化异步为同步？</p><p>由于所有玩家都跑在 Play 中，Play 的任何外部 IO(网络IO，DB，与 Map 的交互等)都必须是非阻塞的，因此必然会产生很多异步逻辑，那么换一种思路: 如果我们进一步地将每个玩家都跑在一个单独的 Actor 中，这样玩家 Actor 就可以将一些原本 Play 需要异步的操作变成同步(阻塞一个玩家 Actor 是可接受的)，可以简化部分逻辑(比如登录状态机)，但也会带来玩家与玩家之间的异步交互，对于 SLG 这类玩家数据交互频繁的游戏来说，这未必是简化。通常我们做拆分都是基于性能瓶颈或扩展性，性能拆分要考虑到整体瓶颈，最好是基于实测，比如我们的例子中可能实际的瓶颈是 Map 而不是 Play，扩展性是指 Actor 之间的交互边界是否清晰，不清晰的交互边界会放大系统的数据不一致性，环形阻塞等问题。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;前段时间又和同事讨论到 GS 中的 ACID 一致性，在这里唠叨几句。ACID 一致性即系统内部的数据一致性，而非分布式系统对外体现的一致性(CAP 中的 C)。&lt;/p&gt;
&lt;p&gt;假设我们有一个业务逻辑叫做行军，在单线程下，其逻辑如下:&lt;/p&gt;</summary>
    
    
    
    <category term="gameserver" scheme="http://wudaijun.com/categories/gameserver/"/>
    
    
    <category term="gameserver" scheme="http://wudaijun.com/tags/gameserver/"/>
    
  </entry>
  
  <entry>
    <title>再谈调度</title>
    <link href="http://wudaijun.com/2018/11/scheduler-blabla/"/>
    <id>http://wudaijun.com/2018/11/scheduler-blabla/</id>
    <published>2018-11-29T16:00:00.000Z</published>
    <updated>2018-12-19T06:24:47.000Z</updated>
    
    <content type="html"><![CDATA[<p>调度的概念这里就不赘述了，调度本质上就是一个资源分配算法，本文谈谈调度的基础策略，常见模型，以及 Go 和 Erlang 的一些调度特性。</p><h2 id="调度策略"><a href="#调度策略" class="headerlink" title="调度策略"></a>调度策略</h2><h3 id="抢占-vs-协作"><a href="#抢占-vs-协作" class="headerlink" title="抢占 vs 协作"></a>抢占 vs 协作</h3><p>从调度机制上来讲，调度可以分为抢占式和协作式。</p><p>非抢占方式是指一旦将调度资源(如 CPU)分配给某任务 后，便让该任务一直执行，直到该任务完成或阻塞或主动让出CPU控制权，非抢占调度又称为协作式调度，它实现简单，并且对共享资源的访问也更安全(如允许使用不可重入函数)。非抢占算法常见的如 FIFO，STCF(Short time to complete first)等。</p><a id="more"></a><p>抢占方式则允许调度程序根据某种规则，剥夺当前进程的调度资源，将其分配给其它进程。常见的抢占策略有:</p><ol><li>基于时间片: 给每个进程分配时间片，当时间片用完后，则停止该进程重新调度下一个进程，这种均分CPU 的算法，又叫轮转调度算法</li><li>基于优先级: 给每个进程分配一个优先级，一旦出现一个优先级更高的进程，则停止当前进程并切换到该高优先级进程，这种调度算法又叫优先级抢占</li><li>轮转调度和优先级抢占结合: 即相同优先级的进程使用轮转调度，如果遇到更高优先级的进程，则可抢占CPU。现代 OS 如 Linux 通常都使用这种混合调度策略。</li></ol><p>PS: 基于优先级抢占容易出现优先级反转的问题: 优先级低的任务持有一个被优先级高的任务所需要的共享资源，这种情况下，优先级低的任务有资源而得不到CPU，优先级高的资源有CPU而得不到资源，从而阻塞(导致其它中优先级的任务获得执行)或者忙等(可能永远无法获得资源)。</p><p>解决优先级反转的方案:</p><ol><li>给临界区一个高优先级，所有进入该临界区的任务将获得该高优先级，避免其被随意抢占</li><li>当高优先级任务在等待低优先级进程持有资源时，低优先级进程将暂时获得高优先级进程的优先级，VxWorks采用的方式。</li><li>禁止中断，也就是在临界区不可被抢占，Linux 采用的就是这种方式，在 thread_info.preeempt_count 记录每个进程当前持锁计数。</li></ol><p>另外，调度器是不能在进程指令流的任意一点执行打断的，因为进程可能此时正在做任何事情，如系统调用，死循环，锁操作等，要实现任意状态的可抢占性代价是很大的，需要 OS 和 App 的通力配合，特别是在涉及在内核态的时候，目前所有OS都不能在进程执行的任意点进行抢占。只是说不断让抢占区更大，抢占点尽可能地密集。</p><h3 id="实时-vs-非实时"><a href="#实时-vs-非实时" class="headerlink" title="实时 vs 非实时"></a>实时 vs 非实时</h3><p>从系统需求或用户角度而言，调度系统可以分为实习系统和非实时系统。</p><p>在实时操作系统中，系统必须在特定的时间内完成指定的应用。实时通常分为软实时(soft real-time)和硬实时(hard real-time)，硬实时是指系统要有确定的最坏情况下的服务时间，即对于事件的响应时间截止期限无论如何都必须得到满足，通常应用在军工航天领域。而软实时只提供统计意义上的实时，比如应用要求在95%的情况下都会确保在规定的时间内执行某个任务，而不一定要求100%。实时系统通常采用抢占调度，实现一个硬实时系统的代价是很高的，要做到进程可以在任意时刻被抢占，在现代OS上来讲，基本是不可能的，因为OS的很多系统调用都是不可被打断的，并且很多操作具备时间的随机性，比如 CPU Cache Miss，Page Fault，CPU Branch Predictor 等等。</p><p>BTW, 为什么现代OS不尽可能地去掉这些不稳定性(如虚拟内存，CPU多级Cache，分支预测等)，从而为成为实时系统打好基础呢？对桌面操作系统而言，对实时性的要求没有那么高，应用切换偶尔卡一卡并无大碍，桌面系统更关注的一方面是对内核的统一抽象，屏蔽硬件差异化，接口丰富易用，让上层应用易于开发，比如虚拟内存，文件描述符等。另一方面，桌面系统在选择牺牲部分的实时性来提高吞吐量，比如多级Cache，分支预测。因此尽管如 Linux 这类桌面系统支持实时优先级和多种调度机制，但仍然最多只能实现软实时，这是设计目标决定的。</p><p>而非实时系统，则没有对最低任务处理时延的要求，比如简单的非抢占调度模型。</p><h2 id="调度结构"><a href="#调度结构" class="headerlink" title="调度结构"></a>调度结构</h2><p>这里我们讨论基于OS之上的几种常见的应用层调度。对应用层而言，所谓调度器(scheduler)便是OS线程，在此之上应用实现自己的任务，任务队列，以及调度算法。</p><h3 id="single-scheduler"><a href="#single-scheduler" class="headerlink" title="single scheduler"></a>single scheduler</h3><p><img src="/assets/image/201811/scheduler-1-1.png" alt=""></p><p>这是最简单的模型，实际上，几乎所有调度器最开始就是这样的，如Go,Erlang,甚至OS(单核年代)的早期版本，复杂项目的通用最佳实践是先跑起来再优化(“First make it work, then measure, then optimize”.)。这种调度模型只有一个调度器和一个任务队列，由于不需要锁，所以单核吞吐量很高，主要的缺点在于无法充分利用调度资源(如多核)，并且容易出现任务饥饿(多调度器本质也会有这种情况，只不过被掩盖了一些)。</p><h3 id="multi-scheduler-with-global-queue"><a href="#multi-scheduler-with-global-queue" class="headerlink" title="multi scheduler with global queue"></a>multi scheduler with global queue</h3><p>为了最大化多核收益，我们需要多个调度器，理想情况下是调度器的数量和CPU核心数一致。因此有了如下调度模型:</p><p><img src="/assets/image/201811/scheduler-n-1.png" alt=""></p><p>如图，多个调度器共享一个全局任务队列，Erlang OTP R11B 便使用这种模型，该模型主要的瓶颈在于全局任务队列的操作需要加锁，并且CPU核心数越多，调度瓶颈越明显，从而限制了调度算法在多核下的扩展性。</p><h3 id="multi-scheduler-with-local-queue"><a href="#multi-scheduler-with-local-queue" class="headerlink" title="multi scheduler with local queue"></a>multi scheduler with local queue</h3><p>为了优化全局任务队列带来的瓶颈问题，借鉴”Cache思维”，可以给每个调度器分配一个本地的任务队列:</p><p><img src="/assets/image/201811/scheduler-n-n.png" alt=""></p><p>这样调度器可以无锁操作本地任务队列，显著减少锁竞争，提升多核下的调度效率。但这样又引入了新的问题: 如何尽可能地让各个调度器都随时有事情做(任务分配尽可能均衡)。比如如果给每个调度器本地任务队列分配了10个任务，执行最快的调度器A执行完这10个任务时，执行最慢的调度器B可能才执行完第一个，在这种情况下，调度器A是否应该去调度B的任务队列steal(窃取)一部分任务过来，以让整体调度效率最高。这个过程叫任务迁移(Task Migration)或任务窃取(Task Stealing)。Erlang R13B+ 和 Go 调度都是基于此结构，但有一些区别。</p><p>以上三种模型是大多数调度器历经的三个阶段，最终演化得到的是一个多层次，局部性的结构(类似 CPU Cache层级)。</p><h2 id="现实中的调度器"><a href="#现实中的调度器" class="headerlink" title="现实中的调度器"></a>现实中的调度器</h2><p>就调度器结构而言，各个调度算法大同小异，各个调度算法的根本差异还是在调度策略上，基于设计目标，在复杂性，吞吐量，实时性之间去做取舍，这里我仍然想以 Erlang 和 Go来举例说明。</p><p>基于Erlang的产生背景(通信领域)，Erlang 在设计之初就对实时性(低延迟)非常看重，为了达成软实时性，Erlang的调度必然是抢占式的，它通过给每个Erlang进程设定规约(Reduction)来作为一个进程的虚拟时间片，进程调用函数，BIF，GC，ETS操作，发送消息等，都会消耗规约，甚至用Erlang自带的用C写的正则表达式处理，也添加了扣除规约的代码，每个Erlang进程默认有2000规约，在Erlang的设计理念中，天下没有”免费”的操作，它重度依靠规约来衡量进程何时被换出。但理想和现实往往有差别，NIF的出现打破了这个定律，NIF是用户用C实现的可供Erlang调用的函数，它是不可被调度的，对此，Erlang打出了如下补丁:</p><ol><li>官方建议NIF不要超过1ms(相信开发者对自己写的代码心里有数…)</li><li>在NIF中给Erlang虚拟机手动汇报当前执行时间，并手动记录上下文和恢复(抢占式变成了协作式，强制变成了自愿…)</li><li>将NIF放到自己创建的OS线程中执行，通过消息的方式将结果返回到Erlang进程(走开，别脏了我的公平调度器)</li><li>将NIF放到OTP为你准备的脏调度器(OTP R17引入，需要在启动时通过参数开启)中执行(好吧，我给你提供脏调度器)</li></ol><p>PS: Erlang OTP R17之后提供了脏调度器功能，这也是继R13B之后对调度结构的又一次改进，虚拟机调度器就变成了M+N个。</p><p>以上方案可以说都是治标不治本，所以写纯Erlang代码，你可以享受到它很多的便利，但是一旦你因为性能问题，或要对接外部库等各种原因需要用到C的时候，一切都开始不美好了。</p><p>Erlang 的另一个问题也来自于其”公平调度”，我们假设有10000个逻辑进程，它们会将日志数据发到一个logger进程，那么系统上一共有10001个进程(忽略其它)，理想情况下，我们当然希望这个logger有任务就处理，避免消息堆积，最大化吞吐量，但Erlang的公平调度会想尽办法让这个logger进程和其它逻辑进程平起平坐，哪怕它有很多事情要做，然后导致导致logger进程消息队列膨胀，内存随之增长，甚至VM随之挂掉。针对这个问题通常的处理方案是:</p><ol><li>提升logger进程的优先级(没有具体测试过)</li><li>开多个logger进程争取更多的执行权(笨办法)</li><li>从设计上尽量避免这种扇入扇出模型，同一节点尽可能跑相同类型的任务，比如将logger放到其它节点(Erlang的”并发思维”)</li><li>通过NIF来绕过时间片限制(骚操作)</li></ol><p>因此，我们通常说Erlang在进程数少的时候表现不怎么样，而在进程多的却有很好的低延时表现。Erlang 调度器还有一些其它细节没有提到，比如:</p><ol><li>通过三个优先级队列(low/normal,high,max)来实现进程的优先级调度</li><li>当调度器idle时，会自旋一段时间，如果没有新任务到达，则关闭该调度器(节能减排)</li><li>除了Process外，Erlang还处理Port任务，Port是Erlang与外部通信(文件，网络，C等)的一种机制</li><li>Erlang对网络IO进行了特别的优化(System Level Activities)，即将所有的套接字设置为非阻塞，然后通过epoll机制去轮询并唤醒调用进程，通过应用层的阻塞/唤醒模拟系统调用。</li></ol><p>反过来谈 Go，如果说 Erlang 为实时性殚精竭虑的话，那么Go则要实务得多，更加偏向于吞吐量和实用性，我之前谈过<a href="http://wudaijun.com/2018/01/go-scheduler/">Go GPM模型</a>，Go没有时间片或规约的概念，它的抢占也不是完全抢占的，它通过一个后台线程sysmon来监控并决定何时发起抢占，何时 GC 等，比如一个goroutine执行超过了10ms，sysmon则会向其发出抢占请求。抢占的方式也很简单，给goroutine打一个标记，goroutine在调用函数分配函数栈时会检查该标记，来决定当前G是否应该让出调度权，因此它没办法抢占死循环。可以看到，Go的调度模型实现相对比较简单，一方面可能调度器仍然还很年轻(STW的痛还历历在目)，另一方面Go的设计哲学就是简单。如果要解释得再官方一些: Go 更看重吞吐量，而非实时性。</p><p>另外，由于Erlang和Go都是为服务器设计的语言，因此它们都是网络IO进行了优化，将所有套接字设置为 nonblock，通过轮询/唤醒来向应用层屏蔽系统调用，通过IO复用+应用层阻塞来避免调度器阻塞在系统调用上。</p><h2 id="最后"><a href="#最后" class="headerlink" title="最后"></a>最后</h2><p>要理解一个调度器，需要结合其产生背景，设计目标，变更历史等，万丈高楼平地起，调度器这类复杂的系统，通常都是”First make it work, then measure, then optimize”，比如 Erlang 的 NIF，Go 的 STW 等，都在逐渐优化。</p><p>实时性和吞吐量是不可兼得的，这已经在其它系统上得到了验证。实时系统通常实现都比较复杂，并且由于现代 OS 最多满足软实时特性，因此应用层的调度也最多实现软实时。严格意义上的硬实时系统通常由特定领域的嵌入式系统实现。同时，也因为应用层的调度受更底层的OS调度的影响，要达到一个系统的整体最优，需要协调不同的调度层级，比如 Erlang 提供一个<code>+sbt</code>参数可以将调度器与 CPU 核心绑定，这样可以更好地利用 CPU 缓存和局部性，以提升整体性能。</p><p>相关资料:</p><ol><li><a href="http://wudaijun.com/2018/01/go-scheduler/">Go 调度模型</a></li><li><a href="https://blog.csdn.net/gatieme/article/details/51872618">Linux用户抢占和内核抢占详解</a></li><li><a href="http://erlang.org/euc/08/euc_smp.pdf">Inside the Erlang VM</a></li><li><a href="http://www.cnblogs.com/zhengsyao/p/how_erlang_does_scheduling_translation.html">Erlang的调度原理(译文)</a></li></ol>]]></content>
    
    
    <summary type="html">&lt;p&gt;调度的概念这里就不赘述了，调度本质上就是一个资源分配算法，本文谈谈调度的基础策略，常见模型，以及 Go 和 Erlang 的一些调度特性。&lt;/p&gt;
&lt;h2 id=&quot;调度策略&quot;&gt;&lt;a href=&quot;#调度策略&quot; class=&quot;headerlink&quot; title=&quot;调度策略&quot;&gt;&lt;/a&gt;调度策略&lt;/h2&gt;&lt;h3 id=&quot;抢占-vs-协作&quot;&gt;&lt;a href=&quot;#抢占-vs-协作&quot; class=&quot;headerlink&quot; title=&quot;抢占 vs 协作&quot;&gt;&lt;/a&gt;抢占 vs 协作&lt;/h3&gt;&lt;p&gt;从调度机制上来讲，调度可以分为抢占式和协作式。&lt;/p&gt;
&lt;p&gt;非抢占方式是指一旦将调度资源(如 CPU)分配给某任务 后，便让该任务一直执行，直到该任务完成或阻塞或主动让出CPU控制权，非抢占调度又称为协作式调度，它实现简单，并且对共享资源的访问也更安全(如允许使用不可重入函数)。非抢占算法常见的如 FIFO，STCF(Short time to complete first)等。&lt;/p&gt;</summary>
    
    
    
    <category term="os" scheme="http://wudaijun.com/categories/os/"/>
    
    
    <category term="erlang" scheme="http://wudaijun.com/tags/erlang/"/>
    
    <category term="go" scheme="http://wudaijun.com/tags/go/"/>
    
  </entry>
  
  <entry>
    <title>go mod 依赖管理</title>
    <link href="http://wudaijun.com/2018/10/go-mod-intro/"/>
    <id>http://wudaijun.com/2018/10/go-mod-intro/</id>
    <published>2018-10-24T16:00:00.000Z</published>
    <updated>2019-06-10T10:52:31.000Z</updated>
    
    <content type="html"><![CDATA[<p>go module 是 go1.11 引入的新概念，为 go  当前饱受诟病的 GOPATH 和依赖管理提供了更好的解决方案。在理解 go module 之前，先回顾下当前 Go 的项目结构和依赖管理都有什么问题:</p><p>1.GOPATH</p><p>GOPATH一定程度上简化了项目构建，但是给了开发者过多的限制，你的项目必须位于 GOPATH 下，否则编译器找到它，想要使用你自己的项目组织结构，要么你需要为每个项目设置一个 GOPATH，要么使用软链接来实现。大多数 go 新手都会纠结于应该使用一个 GOPATH 还是为每个项目创建一个 GOPATH，还是为所有依赖创建一个 GOPATH，受到一堆限制，却并没有得到便利。</p><a id="more"></a><p>2.依赖管理</p><p>依赖管理？对不起，go 没有依赖管理，go1.11之前所有的<a href="https://github.com/golang/go/wiki/PackageManagementTools">依赖管理方案</a>都是基于 Go 没有依赖管理这个事实之上的一些变通方案(tricks)。据说这个起因要追溯到 Google 内部，其使用巨大的单个仓库来维护项目代码，没有第三方仓库，也就不需要版本控制。go1.5开始关于依赖管理的最大变化就是 vendor 机制，它鼓励你将所有的依赖作为项目代码的一部分去管理，其实也是 “Google 风格” 的延续，只不过你可以选择使用某个版本的依赖，然后将其”冻结”到项目的 vendor 目录下，这一方面保证了可重现的构建，另一方面，解决了不同项目使用同一个依赖的不同版本的问题，但单片仓库和版本管理的问题仍然存在，依赖缺乏明确的版本定义，如果没有指定要使用的依赖版本，那么将是依赖这个 branch，然后再通过 hash 来校验，如果版本冲突了，你很难定位版本，并且解决冲突。</p><h4 id="go-module-初试"><a href="#go-module-初试" class="headerlink" title="go module 初试"></a>go module 初试</h4><p>一个 module 是指一组相关的 package，通常对应于一个 git 仓库，module 是代码交换和版本管理的基本单位，即 module 的依赖也是一个 module，go 命令现在直接支持 module 相关操作。由于现在还是试验阶段，go1.11 通过一个临时的环境变量 GO111MODULE 来控制启动和停用go module，该环境变量有三个值:</p><pre><code>auto: 默认值，即在 GOPATH 目录下，使用传统的 GOPATH 和 vendor 来查找依赖，在非 GOPATH 下则使用 go module on: module-aware mode，在任何目录都启用 go module，使用 go module 来控制依赖管理，在这种模式下，GOPATH 不再作为构建时的imports路径，只是作为存放下载的依赖($GOPATH/pkg/mod)和安装二进制文件的目录($GOPATH/bin，如果 GOBIN 未设置)off: GOPATH mode，停用 go module，和 go1.11之前一样，使用 GOPATH 和 vendor 来定位依赖，并使用 dep 之类的依赖版本管理工具来冻结依赖</code></pre><p>在介绍<code>go mod</code>命令前，我们先简单看下 go module 长啥样。假设我们设置<code>GO111MODULE=on</code>，现在开始对 GOPATH/src/ngs 库(之前由 dep 管理依赖)进行移植。</p><p>执行 <code>go mod init ngs</code>，会在 ngs 根目录下生成一个 go.mod 文件，该文件和 dep 的 Gopkg.toml 文件一样，用于记录当前module所依赖的版本，对新项目而言，只会生成一行<code>module packagename</code>，如果是已有项目，go module 会自动从 Gopkg.toml 等已有的依赖版本信息中导入生成依赖版本信息，比如以下是项目的 Gopkg.toml 文件内容:</p><pre><code>[[constraint]]  branch = &quot;master&quot;  name = &quot;github.com/yuin/gopher-lua&quot;[[constraint]]  name = &quot;google.golang.org/grpc&quot;  version = &quot;1.12.2&quot;[[constraint]]  branch = &quot;v2&quot;  name = &quot;gopkg.in/mgo.v2&quot;</code></pre><p>对应生成的 go.mod 为:</p><pre><code>module ngsrequire (    github.com/yuin/gopher-lua v0.0.0-20180611022520-ca850f594eaa    google.golang.org/grpc v1.12.2    gopkg.in/mgo.v2 v2.0.0-20160818020120-3f83fa500528)</code></pre><p>注意，go.mod 中每个依赖都有严格的版本信息，这也是不同于之前 dep 等工具的地方，比如在 Gopkg.toml 中， gopher-lua 使用的 master 分支，go.mod 中，则会为依赖自动生成一个伪版本号<code>v0.0.0-20180611022520-ca850f594eaa</code>，你可以通过命令来手动升级它。这种自动冻结的特性不仅保证了可重现的构建，并且版本冲突的显式清晰的，你可以很直观地在 go.md 中看到冲突的原因，那个版本更新或者更稳定。</p><blockquote><blockquote><p>go module 使用<a href="https://semver.org/lang/zh-CN/">语义化版本(semantic versions)</a>标准来作为描述依赖版本的格式，这样版号可以通过直接比较来决定那个版本更新。版号通常通过 git tag 来标注。对于没有通过 tag 打版本号的提交，go module 使用伪版本号(pseudo-version) 来标记，伪版号的格式为 [之前最近一次的版本号]-[提交时间]-[提交哈希值]，比如 v0.0.0-20180611022520-ca850f594eaa，这样伪版本号也可以用于比较，伪版本号不需要手动输入，会由 go module 在冻结版本时自动生成。</p></blockquote></blockquote><p>现在我们的 ngs 已经成为了一个 go module，现在我们为其添加一个依赖: <code>go get github.com/gorilla/websocket@v1.3.0</code>，可以看到 go.mod 中多出一行: <code>github.com/gorilla/websocket v1.3.0</code>，但 GOPATH 和 vendor 下都没有看到 websocket，其被下载到了 <code>$GOPATH/pkg/mod/github.com/gorilla/websocket@1.3.0</code>目录，在 module-aware mode 下，<code>$GOPATH/pkg/mod</code> 目录会作为依赖被下载后的缓存目录，这里的依赖会将版本作为路径的一部分(与之前依赖管理本质区别)。我们可以以下方式对其进行升级/维护:</p><pre><code>go get -u 将会升级到最新的次要版本或者修订版本(x.y.z, z是修订版本号， y是次要版本号)go get -u=patch 将会升级到最新的修订版本go get package@version 将会升级到指定的版本号version</code></pre><p>当然，我们这里在代码中并没有用到 websocket，可以运行 <code>go mod tidy</code> 来新增被漏掉的，或者删除多余的依赖，运行之后 go.mod 恢复如初。</p><p>可以看到，在 go module 模式下，我们不再需要 vendor 目录来保证可重现的构建，而是可以通过一个 go.mod 来基于每个依赖的精确管理号，并通过<code>go get</code> 等命令即可管理依赖升级/降级等。当然，如果你仍然想保留 vendor 目录，可以通过 <code>go mod vendor</code> 命令将项目用到的依赖拷贝到 vendor 目录下(为了保证兼容性，vendor 目录下的依赖目录名是不包含版本号的)。除了 go.mod 外，go module 还会生成一个 go.sum 来记录每个依赖版本的哈希值，用于校验版本正确性。通常情况下，你不需要手动编辑 go.mod，通过 <code>go get</code>，<code>go mod</code> 等命令来完成依赖管理的同时，go.mod 也会自动更新。</p><p>以下是 go module 相关的一些命令:</p><pre><code>go mod init: 初始化 go modulego mod download: 下载 go.mod 中的依赖到本地 Cache ($GOPATH/pkg/mod 下)go mod vendor: 将项目依赖拷贝到 vendor 下go mod tidy: 相当于 dep ensure，增加缺失的依赖(module)，丢掉没用的依赖(module)go mod verify: 校验依赖go mod edit: 编辑依赖，通过命令行手动升级或获取依赖go list -m all: 列出当前项目(main module)的构建列表</code></pre><p>go 的任何构建命令都可以判断依赖缺失并决定是否需要添加到 go.mod，比如如果你在你的项目代码没有依赖redis，然后你在代码中加入<code>import &quot;github.com/go-redis/redis&quot;</code> ，然后直接执行go build，将会导致 redis 被发现为缺失依赖，被自动添加进 go.mod 中。你可以通过 <code>-mod</code> 构建选项来控制这一行为，该选项有如下几个值:</p><ul><li><code>-mod=readonly</code>: 构建过程中，如果发现需要更改 go.mod，将会构建失败，即 go.mod 在构建过程中为只读的。 注: <code>go get</code> 命令不受此限制，<code>go mod</code> 命令不接受 -mod 选项</li><li><code>-mod=vendor</code>: go 命令假设所有的依赖都存放在 vendor 目录下，并且忽略 go.mod 中的依赖描述</li></ul><p>大概了解了 go module 之后，我们回顾它是如何解决我们前面提出的两个问题的:</p><ol><li>go module 通过 go.mod 来定位当前 module(也叫做 main module) 的 root path，即从当前执行命令的目录向上查找，直到找到go.mod，而不再通过 <code>$GOPATH/src</code> 来定位项目。</li><li>go module 对每个依赖生成严格的版本号，并且将同一个依赖的不同版本以目录区分开来，以 $GOPATH/pkg/mod 作为不同 module 共享依赖的路径。</li></ol><p>现在 go module 已经有一些依赖版本管理的雏形了，离 <a href="http://wudaijun.com/2016/09/erlang-rebar3/">Erlang Rebar3</a> 这种成熟的依赖管理虽然还有一些距离，但确实实用性，易用性都要好很多。</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;go module 是 go1.11 引入的新概念，为 go  当前饱受诟病的 GOPATH 和依赖管理提供了更好的解决方案。在理解 go module 之前，先回顾下当前 Go 的项目结构和依赖管理都有什么问题:&lt;/p&gt;
&lt;p&gt;1.GOPATH&lt;/p&gt;
&lt;p&gt;GOPATH一定程度上简化了项目构建，但是给了开发者过多的限制，你的项目必须位于 GOPATH 下，否则编译器找到它，想要使用你自己的项目组织结构，要么你需要为每个项目设置一个 GOPATH，要么使用软链接来实现。大多数 go 新手都会纠结于应该使用一个 GOPATH 还是为每个项目创建一个 GOPATH，还是为所有依赖创建一个 GOPATH，受到一堆限制，却并没有得到便利。&lt;/p&gt;</summary>
    
    
    
    <category term="go" scheme="http://wudaijun.com/categories/go/"/>
    
    
    <category term="go" scheme="http://wudaijun.com/tags/go/"/>
    
  </entry>
  
  <entry>
    <title>Linux CGroup 基础</title>
    <link href="http://wudaijun.com/2018/10/linux-cgroup/"/>
    <id>http://wudaijun.com/2018/10/linux-cgroup/</id>
    <published>2018-10-13T16:00:00.000Z</published>
    <updated>2018-10-14T12:46:04.000Z</updated>
    
    <content type="html"><![CDATA[<h3 id="CGroup-V1"><a href="#CGroup-V1" class="headerlink" title="CGroup V1"></a>CGroup V1</h3><h4 id="1-CGroup-概念"><a href="#1-CGroup-概念" class="headerlink" title="1. CGroup 概念"></a>1. CGroup 概念</h4><ul><li>Task: 任务，也就是进程，但这里的进程和我们通常意义上的 OS 进程有些区别，在后面会提到。</li><li>CGroup: 控制组，一个 CGroup 就是一组按照某种标准划分的Tasks。这里的标准就是 Subsystem 配置。换句话说，同一个CGroup 的 Tasks 在一个或多个 Subsystem 上使用同样的配置。</li><li>Hierarchy: 树形结构的 CGroup 层级，每个子 CGroup 节点会继承父 CGroup 节点的子系统配置，每个 Hierarchy 在初始化时会有默认的 CGroup(Root CGroup)。</li><li>Subsystem: 子系统，具体的物理资源配置，比如 CPU 使用率，内存占用，磁盘 IO 速率等。一个 Subsystem 只能附加在一个 Hierarchy 上，一个 Hierarchy 可以附加多个 Subsystem。</li></ul><a id="more"></a><p><img src="/assets/image/201810/cgroup-base.png" alt=""></p><h4 id="2-CGroup-文件系统"><a href="#2-CGroup-文件系统" class="headerlink" title="2. CGroup 文件系统"></a>2. CGroup 文件系统</h4><p>在具体实现中，CGroup 通过虚拟文件系统实现，一个 CGroup 就是一个文件夹，Hierarchy 层级结构通过文件夹结构实现，而每个 CGroup 的 Subsystem 配置和 Tasks 则通过文件来配置。在 Ubuntu 下，可通过<code>lssubsys -m</code>(需要安装cgroup-tools包)，查看已有的 Subsystem:</p><pre><code>root# lssubsys -mcpuset /sys/fs/cgroup/cpusetcpu,cpuacct /sys/fs/cgroup/cpu,cpuacctblkio /sys/fs/cgroup/blkiomemory /sys/fs/cgroup/memorydevices /sys/fs/cgroup/devicesfreezer /sys/fs/cgroup/freezernet_cls,net_prio /sys/fs/cgroup/net_cls,net_prioperf_event /sys/fs/cgroup/perf_eventhugetlb /sys/fs/cgroup/hugetlbpids /sys/fs/cgroup/pids</code></pre><p>这些是 Ubuntu16.04 上已实现的 Subsystem 和对应 Hierarchy。各个Subsystem 的作用可参考 <a href="https://access.redhat.com/documentation/en-us/red_hat_enterprise_linux/6/html/resource_management_guide/ch01">RedHat CGroup Doc</a>。在其它系统，你可以需要手动挂载虚拟文件系统并建立 Subsystem 和 Hierarchy 的关系:</p><pre><code>root# mount -t tmpfs cgroup_root /sys/fs/cgrouproot# mkdir /sys/fs/cgroup/cpuroot# mount -t cgroup cpu -ocpu /sys/fs/cgroup/cpu</code></pre><p><code>/sys/fs/cgroup/cpu</code> 即成为附加(attach)了 CPU Subsystem 的 Hierarchy 的根目录，即 Root CGroup，我们可以在该 CGroup 下创建一个 Child CGroup:</p><pre><code>root# mkdir /sys/fs/cgroup/cpu/demoroot# ls /sys/fs/cgroup/cpu/democgroup.clone_children  cgroup.procs  cpuacct.stat  cpuacct.usage  cpuacct.usage_percpu  cpu.cfs_period_us  cpu.cfs_quota_us  cpu.shares  cpu.stat  notify_on_release  tasks  </code></pre><p>在创建 CGroup 时，就已经生成了一堆文件，一个 CGroup 目录中的内容大概可以分为四类: </p><ul><li>Subsystem Conf: 如附加了 CPU Subsystem 的 CGroup 目录下的 cpu* 文件均为 CPU Subsystem 配置</li><li>Tasks: 在该 CGroup 下的 Tasks，分为两个文件，tasks 和 cgroup.procs，两者记录的都是在该进程 PID 列表，但是有所区别。</li><li>CGroup Conf: CGroup 的一些通用配置，比如 notify_on_release 用于在 CGroup 结构变更时执行 release_agent 中的命令，cgroup.clone_children 用于在 Child CGroup 创建时，自动继承父 Child CGroup 的配置，目前只有 cpuset SubSystem 支持</li><li>Child CGroups: 除以上三种文件外的子目录，如Ubuntu16.04中，每个 Root CGroup 下都有个 docker 目录，它由 Docker 创建，用于管理Docker容器的资源配置</li></ul><p>关于 tasks 和 cgroup.procs，网上很多文章将 cgroup 的 Task 简单解释为 OS 进程，这其实不够准确，更精确地说，cgroup.procs 文件中的 PID 列表才是我们通常意义上的进程列表，而 tasks 文件中包含的 PID 实际上可以是 <a href="https://zh.wikipedia.org/wiki/%E8%BD%BB%E9%87%8F%E7%BA%A7%E8%BF%9B%E7%A8%8B">Linux 轻量级进程(LWP)</a> 的 PID，而由于 Linux pthread 库的线程实际上轻量级进程实现的(Linux 内核不支持真正的线程，可通过<code>getconf GNU_LIBPTHREAD_VERSION</code>查看使用的 pthread 线程库版本，Ubuntu16.04上是NPTL2.23(Native Posix Thread Lib)，简单来说，Linux 进程主线程 PID = 进程 PID，而其它线程的 PID (LWP PID)则是独立分配的，可通过<code>syscall(SYS_gettid)</code>得到。LWP 在 ps 命令中默认是被隐藏的，在/proc/目录下可以看到。为了区分方便，我们将以 Proc 来表示传统意义上的进程，以 Thread 表示 LWP 进程。</p><p>我们可以通过 ps 命令的 -T 参数将 LWP 在 SPID 列显示出来:</p><pre><code>root# ps -ef | wc -l218root# ps -efT | wc -l816root# ps -p 28051 -lfT                                                  F S UID        PID  SPID  PPID  C PRI  NI ADDR SZ WCHAN  STIME TTY          TIME CMD               0 Z root     28051 28051 26889  0  80   0 -     0 exit   10:30 pts/10   00:00:00 [a.out] &lt;defunct&gt; 1 R root     28051 28054 26889 99  80   0 - 12409 -      10:30 pts/10   00:00:10 [a.out] &lt;defunct&gt; 1 R root     28051 28055 26889 99  80   0 - 12409 -      10:30 pts/10   00:00:10 [a.out] &lt;defunct&gt;</code></pre><p>以上示例中，Proc 28051 下有两个 Thread (28054,28055)，即开了两个子线程。总的来说，Linux 下这种通过 LWP 来实现线程的方式，在一些时候会给用户一些困惑，比如如果我 <code>kill -9 28055</code>(默认在 ps 下看不到)，按照 POSIX 标准，28055 “线程”所在的进程会被 Kill掉，因此28051,28054,28055三个进程都会被杀掉，感觉就很诡异。感兴趣的可以看看<a href="https://blog.csdn.net/tianyue168/article/details/7403693">这篇文章</a>)。</p><p>当要向某个 CGroup 加入 Thread 时，将Thread PID 写入 tasks 或 cgroup.procs 即可，cgroup.procs 会自动变更为该 Task 所属的 Proc PID。如果要加入 Proc 时，则只能写入到 cgroup.procs 文件(未解)，tasks 文件会自动更新为该 Proc 下所有的 Thread PID。可以通过<code>cat /proc/PID/cgroup</code>查看某个 Proc/Thread 的 CGroup 信息，</p><h4 id="3-一个实例"><a href="#3-一个实例" class="headerlink" title="3. 一个实例"></a>3. 一个实例</h4><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> _GNU_SOURCE         <span class="comment">/* See feature_test_macros(7) */</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/stat.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/types.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/syscall.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> <span class="keyword">int</span> NUM_THREADS = <span class="number">5</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> *<span class="title">thread_main</span><span class="params">(<span class="keyword">void</span> *threadid)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">long</span> tid;</span><br><span class="line">    tid = (<span class="keyword">long</span>)threadid;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;sub thread#%ld, pid #%ld!\n&quot;</span>, tid, syscall(SYS_gettid));</span><br><span class="line"></span><br><span class="line">    <span class="keyword">int</span> a=<span class="number">0</span>;</span><br><span class="line">    <span class="keyword">while</span>(<span class="number">1</span>) &#123;</span><br><span class="line">        a++;</span><br><span class="line">    &#125;</span><br><span class="line">    pthread_exit(<span class="literal">NULL</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span> <span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> *argv[])</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;main thread, pid #%ld!\n&quot;</span>, syscall(SYS_gettid));</span><br><span class="line">    <span class="keyword">int</span> num_threads;</span><br><span class="line">    <span class="keyword">if</span> (argc &gt; <span class="number">1</span>)&#123;</span><br><span class="line">        num_threads = atoi(argv[<span class="number">1</span>]);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (num_threads&lt;=<span class="number">0</span> || num_threads&gt;=<span class="number">100</span>)&#123;</span><br><span class="line">        num_threads = NUM_THREADS;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">pthread_t</span>* threads = (<span class="keyword">pthread_t</span>*) <span class="built_in">malloc</span> (<span class="keyword">sizeof</span>(<span class="keyword">pthread_t</span>)*num_threads);</span><br><span class="line">    <span class="keyword">int</span> rc;</span><br><span class="line">    <span class="keyword">long</span> t;</span><br><span class="line">    <span class="keyword">for</span>(t=<span class="number">0</span>; t&lt;num_threads; t++)&#123;</span><br><span class="line">        rc = pthread_create(&amp;threads[t], <span class="literal">NULL</span>, thread_main, (<span class="keyword">void</span> *)t);</span><br><span class="line">        <span class="keyword">if</span> (rc)&#123;</span><br><span class="line">            <span class="built_in">printf</span>(<span class="string">&quot;ERROR; return code from pthread_create() is %d\n&quot;</span>, rc);</span><br><span class="line">            <span class="built_in">exit</span>(<span class="number">-1</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    pthread_exit(<span class="literal">NULL</span>);</span><br><span class="line">    <span class="built_in">free</span>(threads);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这段代码简单创建了四个死循环线程，运行:</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">root# gcc -pthread t.c &amp;&amp; .&#x2F;a.out</span><br><span class="line">main thread, pid #30354</span><br><span class="line">sub thread#0, pid #30355</span><br><span class="line">sub thread#2, pid #30357</span><br><span class="line">sub thread#3, pid #30358 </span><br><span class="line">sub thread#1, pid #30356</span><br></pre></td></tr></table></figure><p>通过 htop/top(top 默认不会显示 LWP) 看到现在四个 CPU 会被吃满，为了限制资源，我们创建一个 CGroup:</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br></pre></td><td class="code"><pre><span class="line">root# mkdir &#x2F;sys&#x2F;fs&#x2F;cgroup&#x2F;cpu&#x2F;wdj</span><br><span class="line"># 创建 CGroup 之后，会自动生成相关文件</span><br><span class="line">root# ls &#x2F;sys&#x2F;fs&#x2F;cgroup&#x2F;cpu&#x2F;wdj&#x2F;</span><br><span class="line">cgroup.clone_children  cgroup.procs  cpuacct.stat  cpuacct.usage  cpuacct.usage_percpu  cpu.cfs_period_us  cpu.cfs_quota_us  cpu.shares  cpu.stat  notify_on_release  tasks</span><br><span class="line"># 配置该 CGroup，CPU 使用率不能超过 50%</span><br><span class="line">root# echo 50000 &gt; &#x2F;sys&#x2F;fs&#x2F;cgroup&#x2F;cpu&#x2F;wdj&#x2F;cpu.cfs_quota_us</span><br><span class="line"># 将 Proc PID 写入 procs</span><br><span class="line">root# echo 30354 &gt; &#x2F;sys&#x2F;fs&#x2F;cgroup&#x2F;cpu&#x2F;wdj&#x2F;cgroup.procs</span><br><span class="line"># tasks 已经自动更新, # 此时 CPU 占用率会立即下降到 50%</span><br><span class="line">root# cat &#x2F;sys&#x2F;fs&#x2F;cgroup&#x2F;cpu&#x2F;wdj&#x2F;tasks</span><br><span class="line">30355</span><br><span class="line">30356</span><br><span class="line">30357</span><br><span class="line">30358</span><br><span class="line"></span><br><span class="line"># 同样的方式，再来限制下 CPU 核的使用</span><br><span class="line">root# mkdir &#x2F;sys&#x2F;fs&#x2F;cgroup&#x2F;cpuset&#x2F;wdj</span><br><span class="line"># 限制只能使用 CPU 2,3 两个核</span><br><span class="line"># 在使用前需要先执行，参考https:&#x2F;&#x2F;stackoverflow.com&#x2F;questions&#x2F;28348627&#x2F;echo-tasks-gives-no-space-left-on-device-when-trying-to-use-cpuset</span><br><span class="line">#root echo 0 &gt; cpuset&#x2F;wdj&#x2F;cpuset.mems</span><br><span class="line">#root echo 0 &gt; cpuset&#x2F;wdj&#x2F;cpuset.cpus</span><br><span class="line">echo 2,3 &gt; &#x2F;sys&#x2F;fs&#x2F;cgroup&#x2F;cpuset&#x2F;wdj&#x2F;cpuset.cpu</span><br><span class="line"># 这次我们将 Thread PID 写入 cgroup.procs</span><br><span class="line">root# echo 30355 &gt; &#x2F;sys&#x2F;fs&#x2F;cgroup&#x2F;cpu&#x2F;wdj&#x2F;cgroup.procs</span><br><span class="line"># tasks 会自动更新为该 Proc下所有的 Thread PID</span><br><span class="line">root# cat &#x2F;sys&#x2F;fs&#x2F;cgroup&#x2F;cpu&#x2F;wdj&#x2F;tasks</span><br><span class="line">30355</span><br><span class="line">30356</span><br><span class="line">30357</span><br><span class="line">30358</span><br></pre></td></tr></table></figure><p>最终我们通过 htop 得到的效果如下:</p><p><img src="/assets/image/201810/cgroup-example-htop.png" alt=""></p><p>如果我们只将 Thread PID 如30356写入 /sys/fs/cgroup/cpu/wdj/tasks，则只会限制thread#1的CPU使用率。总结一下:</p><ul><li>将 Thread PID 写入 tasks: 仅对该”线程”(LWP) 生效</li><li>将 Thread PID 写入 cgroup.procs: 会加入整个 Proc PID</li><li>将 Proc PID 写入 tasks: 没有效果，写不进去</li><li>将 Proc PID 写入 cgroup.procs: 会加入整个 Proc PID</li></ul><p>表现有点怪异，还没找到具体原因，总的来说，目前的 CGroup 还有点乱:</p><ol><li>Subsystem, Hierarchy, CGroup 三者的结构有点乱，将对进程的分组和对资源的控制混在了一起</li><li>由于 Linux 通过 LWP 实现 Thread，导致 CGroup 看起来可以对线程实现控制，但这方面机制不够健全，比如前面提到的加入机制</li></ol><h3 id="CGroup-V2"><a href="#CGroup-V2" class="headerlink" title="CGroup V2"></a>CGroup V2</h3><p>CGroup V2 在 Linux Kernel 4.5中被引入，并且考虑到其它已有程序的依赖，V2 会和 V1 并存几年。针对于 CGroup V1 中 Subsystem, Herarchy, CGroup 的关系混乱，CGroup V2 中，引入 unified hierarchy 的概念，即只有一个 Hierarchy，仍然通过 mount 来挂载 CGroup V2:</p><pre><code>mount -t cgroup2 none $MOUNT_POINT</code></pre><p>挂载完成之后，目录下会有三个 CGroup 核心文件:</p><ul><li>cgroup.controllers: 该文件列出当前 CGroup 支持的所有 Controller，如: cpu io memory</li><li>cgroup.procs: 在刚挂载时，Root CGroup 目录下的 cgroup.procs 文件中会包含系统当前所有的Proc PID(除了僵尸进程)。同样，可以通过将 Proc PID 写入 cgroup.procs 来将 Proc 加入到 CGroup</li><li>cgroup.subtree_control: 用于控制该 CGroup 下 Controller 开关，只有列在 cgroup.controllers 中的 Controller 才可以被开启，默认情况下所有的 Controller 都是关闭的。</li></ul><p>这三个文件在所有的 CGroup 中都会生成，除此之外，在非 Root CGroup 下，还会有一个 cgroup.events 文件，该文件的 populated 字段会指出当前 CGroup 下的所有存活的 Proc PID，为1则表示其下存活的 Proc PID 数量&gt;1，否则populated为0。这用于 CGroup V1的 release_agent 等事件通知，因为当最后一个进程退出 CGroup 时，cgroup.events 文件会被修改，从而触发事件。</p><pre><code># 查看当前 CGroup 支持的所有 Controllersroot# cat cgroup.controllerscpu io memory# 开启和关闭 Controllerroot# echo &quot;+cpu +memory -io&quot; &gt; cgroup.subtree_control</code></pre><p>在 CGroup V2 中，A CGroup 开启了某个 Controller，则其直接子 CGroup B会生成对应的 Controller 接口文件(如 <code>cpu.cfs_quota_us</code>)，并且B CGroup 的 cgroup.controllers 会更新。B也可以选择开启或关闭该 Controller，但影响的是 B 的直接子 CGroup。并且只有没有 Tasks 的 CGroup 即中间节点可以开关 Controller，只有叶子节点(和根节点)可以执行资源配置。这样每个节点要么控制子 CGroup 的 Controller 开关(中间节点)，要么控制其下 Tasks 的资源配置(叶子节点)，结构更清晰。</p><p>另外，CGroup V2 去掉了 Tasks 文件，增加了 cgroup.threads 文件，用于管理 LWP(仍然没有放弃对”线程”的支持)，但语义上会清晰一些。</p><p>站在进程的角度来说，在挂载 CGroup V2时，所有已有Live Proc PID 都会加入到 Root CGroup，之后所有新创建的进程都会自动加入到父进程所属的 CGroup，由于 V2 只有一个 Hierarchy，因此一个进程同一时间只会属于一个 CGroup:</p><pre><code>root# cat /proc/842/cgroup...0::/test-cgroup/test-cgroup-nested</code></pre><p>总的来说，CGroup V2去掉了多个 Hierarchy 结构，使用 unified Hierarchy，对 Hierarchy 内部层级结构作出一些限制以保证层级逻辑清晰，并且优化了 CGroup 的文件组织(如 cgroup.events, cgroup.threads)。由于目前手头暂时没有 Kernel 4.5，只能通过文档大概了解下，还是要找机会实际体验一下。</p><p>Reference:</p><ol><li><a href="https://www.kernel.org/doc/Documentation/cgroup-v1/cgroups.txt">CGroup V1 Document</a></li><li><a href="https://www.kernel.org/doc/Documentation/cgroup-v2.txt">CGroup V2 Document</a></li><li><a href="https://access.redhat.com/documentation/zh-cn/red_hat_enterprise_linux/7/html/resource_management_guide/chap-introduction_to_control_groups">RedHat: 控制群组简介</a></li><li><a href="https://lwn.net/Articles/679786/">RedHat: Understanding the new control groups API</a></li><li><a href="https://coolshell.cn/articles/17049.html">CoolShell: DOCKER基础技术：LINUX CGROUP</a></li></ol>]]></content>
    
    
    <summary type="html">&lt;h3 id=&quot;CGroup-V1&quot;&gt;&lt;a href=&quot;#CGroup-V1&quot; class=&quot;headerlink&quot; title=&quot;CGroup V1&quot;&gt;&lt;/a&gt;CGroup V1&lt;/h3&gt;&lt;h4 id=&quot;1-CGroup-概念&quot;&gt;&lt;a href=&quot;#1-CGroup-概念&quot; class=&quot;headerlink&quot; title=&quot;1. CGroup 概念&quot;&gt;&lt;/a&gt;1. CGroup 概念&lt;/h4&gt;&lt;ul&gt;
&lt;li&gt;Task: 任务，也就是进程，但这里的进程和我们通常意义上的 OS 进程有些区别，在后面会提到。&lt;/li&gt;
&lt;li&gt;CGroup: 控制组，一个 CGroup 就是一组按照某种标准划分的Tasks。这里的标准就是 Subsystem 配置。换句话说，同一个CGroup 的 Tasks 在一个或多个 Subsystem 上使用同样的配置。&lt;/li&gt;
&lt;li&gt;Hierarchy: 树形结构的 CGroup 层级，每个子 CGroup 节点会继承父 CGroup 节点的子系统配置，每个 Hierarchy 在初始化时会有默认的 CGroup(Root CGroup)。&lt;/li&gt;
&lt;li&gt;Subsystem: 子系统，具体的物理资源配置，比如 CPU 使用率，内存占用，磁盘 IO 速率等。一个 Subsystem 只能附加在一个 Hierarchy 上，一个 Hierarchy 可以附加多个 Subsystem。&lt;/li&gt;
&lt;/ul&gt;</summary>
    
    
    
    <category term="os" scheme="http://wudaijun.com/categories/os/"/>
    
    
    <category term="linux" scheme="http://wudaijun.com/tags/linux/"/>
    
  </entry>
  
  <entry>
    <title>一致性杂谈</title>
    <link href="http://wudaijun.com/2018/09/distributed-consistency/"/>
    <id>http://wudaijun.com/2018/09/distributed-consistency/</id>
    <published>2018-09-22T16:00:00.000Z</published>
    <updated>2020-06-30T06:30:09.894Z</updated>
    
    <content type="html"><![CDATA[<h3 id="什么是一致性？"><a href="#什么是一致性？" class="headerlink" title="什么是一致性？"></a>什么是一致性？</h3><p>在谈到一致性这个词时，你会想到CAP理论的 consistency，或者 ACID 中的 consistency，或者 cache 一致性协议的 coherence，还是 Raft/Paxos 中的 consensus？</p><p>一致性大概是分布式系统中最容易造成困惑的概念之一，因为它已经被用烂了。在很多中文文献中，都将consistency，coherence，consensus 三个单词统一翻译为”一致性”。因此在谈一致性之前，我认为有必要对这几个概念做一个区分:</p><a id="more"></a><h4 id="Consensus"><a href="#Consensus" class="headerlink" title="Consensus"></a>Consensus</h4><p>更准确的翻译是共识，关注的是多个提议者达成共识的过程。比如 Paxos，Raft 共识算法本质上解决的是如何在分布式系统下保证所有节点对某个结果达成共识，其中需要考虑节点宕机，网络时延，网络分区等分布式中各种问题。共识算法通常应用在复制状态机中，比如 etcd，zookeeper，用于构建高可用容错系统。在这种应用情景下，Raft/Paxos 共识算法被用来确保各个复制状态机(节点)的日志是一致的。类似的，区块链中非常重要的一部分也是共识算法，但通常使用是 POW(Proof of Work) 或 POS(Proof of Stake)，这类共识算法通常适合用在公网，去中心化的情形下。</p><h4 id="Coherence"><a href="#Coherence" class="headerlink" title="Coherence"></a>Coherence</h4><p>Coherence 通只出现在 <a href="https://en.wikipedia.org/wiki/Cache_coherence">Cache Coherence</a> 一词中，作为”缓存一致性”被提出。我们知道现代的多核 CPU Cache 都是多层结构，通常每个 CPU Core 都有一个私有的 LB/SB, L1, L2 级 Cache，多个 CPU Core 共享一个 L3 Cache。比如 CPU1 修改了全局变量 g 并写入了 CPU1 L1 Cache，此时 CPU2 要读取变量 g，然而 CPU2 L1 Cache 中的 g 仍然是旧值，这里就需要 Cache Coherence (以下简称 CC)机制来保证 CPU2 读取到的一定是 g 的最新值。因此，CC 的本质是让多组 Cache 看起来就像一组 Cache 一样。现代 CPU 都已经实现了 CC，通常程序员也不需要关心 CC 的具体实现策略，目前大部分 CPU 采用的是基于 MESI(Modified-Shared-Invalid-Exclusive) 协议的 CC，这里有一篇<a href="http://www.infoq.com/cn/articles/cache-coherency-primer">参考文章</a>。</p><p>解释完了Consensus 和 Coherence，剩下 ACID 和 CAP，两者的 C 都叫做 Consistency，你可能以为这两者应该就是我们常提到的分布式中的一致性了吧。其实并不是，两者也是完全不同的概念。</p><h4 id="ACID-Consistency"><a href="#ACID-Consistency" class="headerlink" title="ACID Consistency"></a>ACID Consistency</h4><p>ACID 中的一致性是指数据库的一致性约束，ACID 一致性完全与数据库规则相关，包括约束，级联，触发器等。在事务开始之前和事务结束以后，都必须遵守这些不变量，保证数据库的完整性不被破坏，因此 ACID 中的 C 表示数据库执行事务前后状态的一致性，防止非法事务导致数据库被破坏。比如银行系统 A 和 B 两个账户的余额总和为 100，那么无论 A, B 之间怎么转换，这个余额和是不变，前后一致的。</p><h4 id="CAP-Consistency"><a href="#CAP-Consistency" class="headerlink" title="CAP Consistency"></a>CAP Consistency</h4><p>CAP 理论中的 C 也就是我们常说的分布式系统中的一致性，更确切地说，指的是分布式一致性中的一种: 线性一致性(Linearizability)，也叫做原子一致性(Atomic consistency)。</p><p>谈到 CAP，和一致性一样，CAP 理论也是个被滥用的词汇，关于 CAP 的正确定义可参考<a href="https://www.the-paper-trail.org/page/cap-faq/">cap faq</a>。</p><p>简单总结CAP理论，在一个分布式的存储系统中，只能Consistency, Availability, Partition三选二，而由于在大规模的分布式系统中，网络不可靠几乎是不可避免的，即Partition网络分区容忍是必选项，因此对系统设计需要在AP(舍弃一致性，可能读出不一致)和CP(发生网络分区时系统不可用，即无法写入)之间权衡。</p><p>很多时候我们会用 CAP 模型去评估一个分布式系统，但<a href="https://blog.the-pans.com/cap/">这篇文章</a>会告诉你 CAP 理论的局限性，因为 CAP 理论是一个被过度简化的理论(一个只能读写单个数据的寄存器)，按照 CAP 理论，很多系统包括 MongoDB，ZooKeeper 既不满足一致性(线性一致性)，也不满足可用性(任意一个工作中的节点都要可以处理请求)，但这并不意味着它们不是优秀的系统，而是 CAP 定理本身并没有考虑处理延迟(为了做到强一致性的事务同步开销)，硬件故障，可读/可写的部分可用性等。这里不再展开，推荐阅读原文。</p><p>正因为 CAP 中对C和A的定义过度理想化，后来又有人提出了BASE 理论，即基本可用(Basically Available）、软状态(Soft State)、最终一致性(Eventual Consistency)。BASE的核心思想是即使无法做到强一致性，但每个应用都可以根据自身的业务特点，采用适当的方法来使系统达到最终一致性(关于最终一致性，这篇亚马逊CTO Werner Vogels的<a href="https://www.allthingsdistributed.com/2008/12/eventually_consistent.html">博客</a>值得一读)。显然，最终一致性弱于 CAP 中的 线性一致性。很多分布式系统都是基于 BASE 中的”基本可用”和”最终一致性”来实现的，比如 MySQL/PostgreSQL Replication 异步复制。</p><p>以下我们不再纠结CAP理论本身，而聚焦于分布式一致性的定义和基本概念。</p><h3 id="分布式一致性模型"><a href="#分布式一致性模型" class="headerlink" title="分布式一致性模型"></a>分布式一致性模型</h3><p>在分布式共享内存系统或者分布式数据存储中，数据一致性模型指定程序员和系统之间的契约，系统保证如果程序员遵守规则，则存储器将是一致的，并且读取，写入或更新存储器的结果将是可预测的。我们最常用于讨论的便是复制状态机(Replicated state machines)的一致性问题，比如 etcd, zookeeper，当进程 A 执行 <code>set x 3</code> 时，进程 B 是否能在 A 执行完成后立即读到 x 的新值。这个问题在单节点上是显而易见的，但在分布式中，对进程 来说，则需要通过一致性模型来确保server 给出的结果可预测。分布式系统中的一致性有强弱之分，理想情况下，当进程写入 replicate 中的某个节点，其改变会被立即同步到集群其它节点，其它进程 能立即读取到该值。弱一些的一致性是最终一致性，即操作完成之后的某段时间内可能各个节点的数据副本不一致(导致不同进程 访问到的数据会不一致)，但最终会是一致的。比如 DNS 服务。</p><p>因此，当别人在谈到”一致性”时，可能需要花些时间来确认他究竟在说什么，有可能你们所谈论的不是一个东西。本文主要讨论的是分布式中的一致性。</p><h4 id="分布式一致性基本概念"><a href="#分布式一致性基本概念" class="headerlink" title="分布式一致性基本概念"></a>分布式一致性基本概念</h4><p>要理解分布式一致性，先来谈谈分布式中几个必要的概念: 时间，事件，和顺序。</p><p>分布式系统的时间主要分为物理时间和逻辑时间两种，物理时间是指程序能够直接获取到的 OS 时间，在分布式系统中，由于光速有限，你永远无法同步两台计算机的时间。想要在分布式中仅依靠物理时间来决定事件的客观先后顺序是不可能的。因此分布式系统中，通常使用<a href="https://my.oschina.net/fileoptions/blog/1821958">逻辑时间</a>来处理事件的先后关系。</p><p>分布式中的事件不是瞬间的概念，它有一个起始和结束时间，因此不同进程 发起的事件可能发生重叠，对于发生重叠的事件，我们说它们是并发执行的，在物理时间上是不分先后的。</p><p>理想情况下，我们希望整个分布式系统中的所有事件都有先后顺序，这种顺序就是全序，即整个事件集合中任意两个事件都有先后顺序。但 1.物理时间是很难同步的，2. 网络是异步的，因此在分布式系统中，想要维持全序是非常困难的。不同的节点对两个事件谁先发生可能具有不同的看法，并且大部分时候我只需要知道偏序关系，用于确保因果关系。所谓偏序关系是指:</p><ol><li>如果a和b是同一个进程中的事件，并且a在b前面发生，那么 a-&gt;b </li><li>如果a代表了某个进程的消息发送事件，b代表另一进程中针对这同一个消息的接收事件，那么a-&gt;b </li><li>如果 a-&gt;b且b-&gt;c，那么a-&gt;c (传递性)</li></ol><p>逻辑时间中的 Lamport Clock和Vector Clock等都可以用于建立偏序关系。</p><h4 id="顺序一致性-Sequential-Consistency"><a href="#顺序一致性-Sequential-Consistency" class="headerlink" title="顺序一致性(Sequential Consistency)"></a>顺序一致性(Sequential Consistency)</h4><p>Leslie Lamport 在1979年提出了Sequential Consistency, Leslie Lamport的定义如下:</p><blockquote><blockquote><p>A multiprocessor is said to be sequentially consistent if the result of any execution is the same as if the operations of all the processors were executed in some sequential order, and the operations of each individual processor appear in this sequence in the order specified by its program.[How to Make a Multiprocessor Computer That Correctly Executes Multiprocess Programs by Leslie Lamport ,1979]</p></blockquote></blockquote><p>这个定义比较晦涩，通常来讲，他可以分为两个部分:</p><ol><li>单个节点的事件历史在全局历史上看符合程序顺序</li><li>事件历史在各个节点上看全局一致</li></ol><p>约束1保证了单个节点的所有事件是按照程序中指定的顺序执行的，约束2保证了所有的事件是原子的，不会相互干扰。更通俗的理解是，将整个分布式系统想象成一个调度器，将多个进程 的请求看做是多个FIFO请求队列， 调度器依次处理队列中的请求，并在多个队列中不断切换。是的，这和<a href="http://wudaijun.com/2018/01/go-scheduler/">Go调度器</a>原理类似，只不过我们探讨一致性模型时，会将系统规约为一个黑盒，从进程的角度来看这个黑盒对外表现出的一致性强弱。</p><p>下面举个例子来加深理解，假设我们有个分布式 KV 系统，以下是四个进程 对其的操作顺序和结果:</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">A: --W(x,1)--</span><br><span class="line">B:  --W(x,2)--</span><br><span class="line">C:                      -R(x,1)-   --R(x,2)-</span><br><span class="line">D:                 -R(x,1)-      --R(x,2)--</span><br></pre></td></tr></table></figure><p>其中<code>W(x,1)</code>表示更新 x 的值为1，<code>R(x,1)</code>表示读出 x 的值为2，横向为物理时间，事件两边的’-‘代表事件持续时间，那么按照我们前面对顺序一致性的描述，显然这个 KV 系统是满足顺序一致性的。</p><p>如果我们将进程 C 和 D 的 <code>R(x,1)</code> 和 <code>R(x,2)</code> 对换一下，也就是如果 C 和 D 都是先读出 x 为 2，再读出 x 为 1，那么还满足顺序一致性么，答案是肯定的，因为仍然能够找出一个全局的执行顺序: <code>B W(x,2) -&gt; A W(x,1) -&gt; C R(x,2) -&gt; D R(x,2) -&gt; C R(x,1) -&gt; D R(x,1)</code>，这个顺序满足前面提到的两点约束，即使直观上看来，这是”错误的一致性”。</p><p>如果我们只是把进程 D 的两个 R 操作对换，那么我们说这个系统是不满足顺序一致性的，因为你无法找出一个全局顺序满足以上两点约束。同样，如果 <code>W(x,1)</code> 和 <code>W(x,2)</code> 都发生在进程 A 中，那么系统也不满足顺序一致性。</p><h4 id="线性一致性-Linearizability"><a href="#线性一致性-Linearizability" class="headerlink" title="线性一致性(Linearizability)"></a>线性一致性(Linearizability)</h4><p>也叫做strong consistency或者atomic consistency，于 1987年提出，线性一致性强于顺序一致性，是程序能实现的最高的一致性模型，也是分布式系统用户最期望的一致性。 与顺序一致性相比，线性一致性只多了一条约束：<strong>如果事件A开始时间晚于事件B结束时间，则在最终事件历史中，要求B在A前。</strong>或者换句话说:<strong>事件生效时间是在事件开始时间到结束事件之间的某个时刻。</strong> 举个例子:</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">A: --W(x,1)--</span><br><span class="line">B:  ---W(x,2)---</span><br><span class="line">C:         --R(x,2)-- -R(x,1)-</span><br><span class="line">D:               --R(x,1)-</span><br></pre></td></tr></table></figure><p>这个案例是满足线性一致性的，因为<code>W(x,1)</code>和<code>W(x,2)</code>两个事件是同时发生的，因此事件生效点可能出现在开始和结束之间的任意点，就这个例子来讲，全局事件历史为: <code>B-W(x, 2), C-R(x,2), A-W(x,1), D-R(x,1), C-R(x,1)</code>，其中<code>D-R(x,1), C-R(x,1)</code>可以对换。</p><p>如果将 D 的 <code>R(x,1)</code>改为 <code>R(x,2)</code>，则不满足线性一致性，因为 C 的 <code>R(x,1)</code> 和  D 的 <code>R(x,2)</code>都发生在 A, B 的 W 事件之后，因此它们应该对 x 的值结果具有相同的视角，不会读出不一致的结果。</p><p>如果说顺序一致性只保证单节点事件先后顺序的话，线性一致性还保证节点间的事件先后顺序，因此线性一致性的实现是非常困难的，一方面它需要一个全局同步的时钟(顺序一致性不需要全局同步的时钟)，另一方面，越严格的一致性，会让分布式系统的复杂度更高，并且与之对应的，性能也会更差。</p><p>除了线性一致性和顺序一致性外，还一些其它更弱的一致性模型，比如因果一致性，最终一致性等，这里不再赘述。理解一致性本身和一些基础概念之后，理解其它一致性都不难。</p><h3 id="内存一致性模型"><a href="#内存一致性模型" class="headerlink" title="内存一致性模型"></a>内存一致性模型</h3><p>在前面我将顺序一致性简单描述为调度器模型，这可能会让你想到操作系统的进程/线程调度，在进程/线程的视角下，多路 CPU 本身不就是个”微型分布式系统”么。是的，不过多核体系是早于分布式的，因此它的一致性模型叫做<a href="https://zh.wikipedia.org/wiki/%E5%86%85%E5%AD%98%E4%B8%80%E8%87%B4%E6%80%A7%E6%A8%A1%E5%9E%8B">内存一致性模型</a>。那问题来了，现代多核体系的内存一致性模型满足顺序一致性么？再来看个例子:</p><p>初始状态下，x = 0, y = 0</p><div class="table-container"><table><thead><tr><th>进程 A</th><th>进程 B</th></tr></thead><tbody><tr><td>x=1</td><td>y=1</td></tr><tr><td>r1=y</td><td>r2=x</td></tr></tbody></table></div><p>按照顺序一致性的定义，我们将四条指令交叉任意排列，最终可能得到三种结果: <code>r1==1,r2==0</code>,<code>r1==0,r2=1</code>, <code>r1=1,r2=1</code> 三种结果，但不可能得到 <code>r1=0,r2=0</code> 的结果，因为这意味着全局执行顺序为 <code>r1=y, r2=x, x=1, y=1</code>，并不符合单个进程内部的执行顺序。那么实际上，这段程序会输出<code>r1=0,r2=0</code>的结果么？答案是会的，原因如下:</p><ol><li>编译器指令重排，编译器会在不影响程序语义的情况下，调整代码中的指令顺序。但编译器只能够解析显示语义，即单线程上下文，它无法(或者说非常难)解析程序的隐式语义，即程序的多线程上下文依赖。</li><li>CPU指令乱序执行，由于内存读取非常慢，CPU在不影响单线程语义的情况下，会将数据提前加载到缓存，提高执行效率。这就可能造成CPU指令处理顺序和程序指令顺序不一致，由于CPU 乱序只保证单线程语义，因此同样无法解析程序逻辑隐私因果关系，也可能造成结果不符程序预期。</li><li>缓存一致性，由于 LB/SB 的存在，缓存一致性是有极短延迟的，可能某个共享数据被CPU更新并写入到 SB(Store Buffer)中，其它 CPU 并不能即时看到。</li></ol><p>基于以上三点，现代 CPU 架构基本都是不支持顺序一致性的，因为其需要非常高昂的代价，严重限制编译器和硬件的优化能力。比如顺序一致性要求处理器按照程序序(program order)来执行程序，但在大部分情况下，这是没必要的。因此，现代硬件体系遵循的其实是: sequential consistency for data race free programs，即对没有 data race 的程序来说，是满足顺序一致性的(编译器能够分析上下文相关性)，但如果涉及到 data race，程序员需要使用 compile barrier 和 memory barrier 来限制编译器和 CPU 的乱序能力，以保证多线程下顺序一致性。这方面推荐一些优秀的文章:</p><ol><li><a href="http://www.parallellabs.com/2010/03/06/why-should-programmer-care-about-sequential-consistency-rather-than-cache-coherence/">为什么程序员需要关心顺序一致性而不是Cache一致性？</a></li><li><a href="http://dreamrunner.org/blog/2014/06/28/qian-tan-memory-reordering/">浅谈Memory Reordering</a></li><li><a href="http://0xffffff.org/2017/02/21/40-atomic-variable-mutex-and-memory-barrier/">聊聊原子变量、锁、内存屏障那点事</a></li></ol><h3 id="最后"><a href="#最后" class="headerlink" title="最后"></a>最后</h3><h4 id="分布式一致性模型的局限性"><a href="#分布式一致性模型的局限性" class="headerlink" title="分布式一致性模型的局限性"></a>分布式一致性模型的局限性</h4><p>分布式一致性模型是被简化过的模型，它将整个分布式系统简化为single-object(如 k-v store, queue)，single-op(如 Read/Write, Enqueue/Dequeue)，因此有些东西是它没有讨论到的:</p><ol><li>single-object: 分布式系统有多个节点，不同的节点可能提供的一致性并不相同，比如连 master 节点可能满足线性一致性，而 slave 节点则不是。</li><li>single-op: 前面的例子中，我们简单将事件当做原子性的操作，而在实践中，往往需要事务(2PC, 3PC)来保证整个分布式系统的内部一致性，这个内部一致性和我们前面讨论的外部一致性是有区别的。同样，事务，串行化这些东西和一致性模型也是不同的东西。</li></ol><p>因此一些分布式系统在不同的配置选项或不同的连接状态下，可能体现出不同的一致性。</p><h4 id="微观-vs-宏观"><a href="#微观-vs-宏观" class="headerlink" title="微观 vs 宏观"></a>微观 vs 宏观</h4><p>在理解顺序一致性的时候，我引入了内存一致性模型，这其实是一个很大的课题，寥寥几句不可能交代清楚。我之所以要放到这里， 是因为两者可以相互启发，分布式设计中很多难题都可以从计算机体系结构中借鉴，比如分布式 Cache 同步 和 CPU Cache 同步，或者内存一致性和分布式一致性。计算机体系结构就是一本历史书，里面包含很多经验教训和千锤百炼的最佳实践，如果能够将其抽象泛化，应用到实际问题中，将是非常宝贵的。比如你要设计一个安全的消息加密，那么 https 会让你少走很多弯路，如果你要实现微服务间的流量控制，tcp 拥塞控制算法会给你很多启发，等等。学底层知识，可能最终学的就是这些”套路”。</p>]]></content>
    
    
    <summary type="html">&lt;h3 id=&quot;什么是一致性？&quot;&gt;&lt;a href=&quot;#什么是一致性？&quot; class=&quot;headerlink&quot; title=&quot;什么是一致性？&quot;&gt;&lt;/a&gt;什么是一致性？&lt;/h3&gt;&lt;p&gt;在谈到一致性这个词时，你会想到CAP理论的 consistency，或者 ACID 中的 consistency，或者 cache 一致性协议的 coherence，还是 Raft/Paxos 中的 consensus？&lt;/p&gt;
&lt;p&gt;一致性大概是分布式系统中最容易造成困惑的概念之一，因为它已经被用烂了。在很多中文文献中，都将consistency，coherence，consensus 三个单词统一翻译为”一致性”。因此在谈一致性之前，我认为有必要对这几个概念做一个区分:&lt;/p&gt;</summary>
    
    
    
    <category term="distributed" scheme="http://wudaijun.com/categories/distributed/"/>
    
    
    <category term="distributed" scheme="http://wudaijun.com/tags/distributed/"/>
    
  </entry>
  
</feed>
